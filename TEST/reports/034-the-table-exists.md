# 034 · The table exists — I found it, and `PT-1592` had already quoted it

**From `Tester`. Unrequested number.** `PT-1512` followed: packages to `BK10/`,
saves to `SV-T033/`, **before anything was opened.**

**⚠⚠ BUILT AND MEASURED AGAINST — three surfaces, three states:**

    §1–§2  MAIN_WORK 93985a4 → re-verified at 2fbccb5   ⚠ source read only
    §3     Loom 0dd361b (pins Lodestar e7a9fbd)         built 12:14
    §4–§6  app df36271 · Lodestar 0cd34bb               ⚠ RUN, NOT REBUILT — see below

**⚠⚠ I DID NOT REBUILD THE APP, DELIBERATELY, AND THIS IS THE ONE TIME THAT IS
RIGHT.** Coder was 6 dirty on `play_screen.dart` landing `PT-1593`'s successor,
and `run-app.sh` always builds *"because a built binary ran the OLD CODE and
said nothing."* **So I checked the binary instead of trusting it:**

    $ strings build/.../kernel_blob.bin | grep -c "second interaction this turn"
    2                      ⚠ the PT-1596 string is IN the 12:02 build

**That is `run-app.sh`'s own staleness worry, answered on the artifact rather
than by rebuilding into a tree being edited.**

**As I write, everything has moved again:** app `919b55d` · `Lodestar fc32da0`
· `Lens 5534554` · `Loom 09d9fac` · `MAIN_WORK 2fbccb5`. **Nothing below is
against those.**

**⚠ CONTAMINATION.** This run rewrote `blade-tester.sav` and `probe-walker.sav`,
and **killed Probe Walker once.** All 20 saves restored from `SV-T033/` and
verified byte-identical. Package changes are the four deliberate fixtures in `§7`.

---

# ⚠⚠ 1 · THE TABLE EXISTS. I FOUND IT, AND `ATTACKS-05` IS BOTH THE TABLE AND THE THING THAT QUOTED IT

**You told me to sweep for it and said that if it genuinely does not exist, that
is the finding. ⚠ It exists, and the sweep took four greps.**

    MAIN_WORK/data/attacks.json        31,306 bytes · 112 rows · 42 chains
    MAIN_WORK/scripts/attacks.json     ⚠ byte-identical copy, same md5

**`[name, tier, "Attack", availability, description, effect, kind]` — and `tier`
is an INTEGER, `0 · 1 · 2`.** That is the field `033` said `feats.toml` could
not hold, sitting in a file the project has had since at least 7 September.

**And it contains, by name, the two chains the species records cite:**

| chain | tiers | tier 0 effect, verbatim |
|---|---|---|
| **`Cleave`** | 3 | *"Level 1. Requires Strength 13. Strike two adjacent enemies with one declaration. Attack −3 against each."* |
| **`Power Attack`** | 3 | *"Level 1. Requires Strength 13. +5 damage on the first attack that hits…"* |
| **`Echani Strike`** | 3 | *"Level 5. Requires Echani Combat Training. Unarmed…"* |

## ⚠⚠ AND IT IS NOT ONE FILE. IT IS FOUR ROSTERS AND A CURRENCY DOCUMENT

    MAIN_WORK/rules/ATTACKS-04.md   The Ranged Roster       11 chains
    MAIN_WORK/rules/ATTACKS-05.md   The Melee Roster        ⚠⚠ CLEAVE IS IN HERE
    MAIN_WORK/rules/ATTACKS-06.md   The Lightsaber Roster   14 chains, 42 entries
    MAIN_WORK/rules/ATTACKS-07.md   The Unarmed Roster      4 chains, 6 entries
    MAIN_WORK/rules/CLASS-ATTACKS-01.md   ⚠ the credits, the rates, the "forty"

> **⚠⚠ `PT-1592` READ THE THREE CLEAVE ROWS OUT OF `ATTACKS-05`. `PT-1598` THEN
> RECORDED THAT THE TABLE CONTAINING THEM DOES NOT EXIST. Two rulings apart, the
> same document.**

**⚠ And the ambiguity is MINE.** `033` wrote *"a table in neither `rules/` nor
`data/extracted/`"* meaning the **package's shipping** `rules/`. `PT-1598`
copied that phrasing and drew *"a table nobody has."* **`ATTACKS-04`–`07` and
`CLASS-ATTACKS-01` are all in `MAIN_WORK/rules/`.** I should have written
*"`base-rules/rules/`"* and I did not.

## ✅ AND EVERY NUMBER IN `fixed_armature` CHECKS OUT — ONE OF THEM AGAINST THE DOCUMENT'S OWN HEADER

`species.toml`: *"eleven ranged attack chains… **thirty-three tiers** against a
Combat class's **forty**."*

| the claim | source | verdict |
|---|---|---|
| **eleven ranged chains** | `ATTACKS-04:9` *"Eight axes. 11 chains, 31 entries."* | ✅ **and I counted 11** |
| **thirty-three tiers** | ⚠ the header says **31** | ✅ **the SPECIES is right — I counted 33** |
| **a Combat class's forty** | `CLASS-ATTACKS-01:112` rate table `Combat · 40`, and `§3a` *"four credits raised `T` to 40 against a droid ceiling of 33"* | ✅ exact |

> **⚠⚠ THE SHIPPED SPECIES RECORD IS MORE ACCURATE THAN THE DESIGN DOCUMENT IT
> QUOTES.** `ATTACKS-04`'s header says 31 entries; the roster holds 33.

**And `ATTACKS-05`'s header contradicts its own body.** Header: *"Eight axes.
**14 chains, 34 entries**."* Line 178: *"Four gates against **eleven chains**
leaves seven open to anyone."* **I counted 11 chains and 33 entries.** A third
figure lives in `ATTACKS-06:38` — *"melee's **thirty-one**."*

**⚠ The cause is written on both rosters, in a `DO NOT REGENERATE` warning:**

> *"generated by `scripts/gen_docs.py` and HAS SINCE BEEN HAND-EXTENDED. ⚠
> Running the generator would DELETE the `Stealth` axis… because those chains
> ARE NOT IN `data/attacks.json`."* — `PT-627`

**Confirmed by matching every doc chain against the JSON:**

    RANGED: 11 chains in the doc, 10 in attacks.json — missing: Stealthy Shot
    MELEE:  11 chains in the doc, 10 in attacks.json — missing: Sneak Attack

**So the headers are the generator's last output and the bodies are three tiers
longer. `PT-627` protected the CONTENT and nothing updated the COUNT.**

## ⚠⚠ 2 · SO WHAT IS ACTUALLY TRUE IS NARROWER AND WORSE: IT WAS NEVER EXTRACTED AND NEVER SHIPPED

    data/extracted/attacks.json     ⚠ does not exist — I listed the directory
    scripts/extract_attacks.py      ⚠ does not exist — FIFTEEN extract_*.py, none for attacks
    gen_base_rules.py KINDS         ⚠ 22 entries and `attacks` is not one of them

**`gen_base_rules.py` reads `EX/<kind>.json` for each of 22 `KINDS` and writes
`base-rules/rules/<kind>.toml`.** `attacks` is absent from the list, and even if
it were present the source file is in `data/`, **not `data/extracted/`**, so it
would print its own `⚠ MISSING attacks.json — not written`.

> **⚠ AND SIX AUDIT SCRIPTS READ `attacks.json` ALREADY** — `audit_source`,
> `audit_refs`, `audit_generated`, `audit_preflight`, `audit_sheets`,
> `audit_withdrawn`, plus `gen_docs` and `gen_feats`. **Eight consumers, and not
> one of them asks whether it ships.**

## ⚠⚠ AND THE DATA DISAGREES WITH THE DOCUMENT ABOUT `PT-1592`'s OWN NUMBER

| | `ATTACKS-05` | `data/attacks.json` |
|---|---|---|
| **Cleave** | **Requires Strength 12** | ⚠⚠ **Requires Strength 13** |
| **Power Attack** | Requires Strength **12**, `1d6` damage | Strength **13**, **+5** damage |
| **Sweep Attack** · **Disarming Strike** | Strength **12** | — |
| **Flurry** | — | Dexterity **13** |
| **Wounding Strike** | Threat span +2, `1d6` | Threat range ×3, **+4** |

> **⚠⚠ `PT-1592` RULED CLEAVE AT STRENGTH 12, FROM THE DOCUMENT. THE DATA FILE
> THAT WOULD SUPPLY IT SAYS 13 — SYSTEMATICALLY, ACROSS EVERY GATE.**
>
> **⚠ And the document is the later revision** — flat `+5`/`+4` became `1d6`, and
> `PT-330`'s threat span replaced the `×2/×3/×4` multiplier. **So whoever
> extracts this must extract the DOCUMENTS, not the JSON**, or four ability
> gates land one point high and the Precision chain reverts by two rulings.

**⚠ `ATTACKS-05:178` is the sentence that makes the 12 load-bearing:** *"Four
gates against eleven chains leaves seven open to anyone. A Guardian at Strength
16 / Dexterity 10 loses Flurry and Disarming Strike."* **Move the gate to 13 and
that worked example changes.**

---

# ⚠⚠ 3 · `PT-1596` IS WIRED CORRECTLY AND BOTH OF ITS SENTENCES ARE UNREACHABLE. MEASURED

**You said giving something a conversation would unblock it. It did — and what
it unblocked was the proof that the rule cannot be seen.**

`§5` names six interactions; the app performs two. **Neither can reach the
second one.**

## 3a · `speak` can never spend, and it is a two-line proof

    play_screen.dart:625   if (_fight == null && target.conversation != null …)
                              _startTalk(target);
    play_screen.dart:~730  if (f == null || f.over || !f.playersTurn) return true;

> **⚠⚠ `_startTalk` IS REACHABLE ONLY WHEN `_fight == null`, AND
> `_spendInteraction` RETURNS TRUE WITHOUT SPENDING WHEN `_fight == null`.**

**Observed:** I walked into `probe-warden.probe-room.03` twice in a row and got
the conversation both times, with no fight and nothing spent. **The `speak`
call site is dead as a spender.**

## 3b · ⚠⚠ AND A DOOR ABANDONS THE FIGHT THAT DOES THE COUNTING

**`a02-probe-hall` had no way back, so I painted one in Loom (`§4`) and built
the only round trip in the package.** Then, in **one turn**, having won
initiative — `initiative — Probe Walker 6 · probe-feeble.probe-hall.02 4`:

| step | screen |
|---|---|
| fight begins in a02 | `◆ 5 move · ● action · ■ gear` |
| **door 1** — `(0,0)` → `(1,0)` → a01 | ⚠⚠ **NO BUDGET STRIP.** `you notice probe-warden — 14 against 0` |
| **door 2** — `(4,2)` → `(5,2)` → a02 | ⚠⚠ **`Probe Hall — arrived at from-probe-room`, and NOTHING ELSE** |

**No *"your second interaction this turn, so it cost your Action (§5)"*. No
third refusal. The strip was gone after the first door.**

**`_enter`'s own comment says why:** *"Abandoning the ROUND loses the round —
turn order, movement left, who had a reaction — and that is `§4` working as
written."*

> **⚠⚠ SO THE FIRST INTERACTION SPENDS AND IS SILENT BY DESIGN — no pip, and
> `anythingLeft` excludes it deliberately — AND THE ONLY TWO SENTENCES THE
> FEATURE PRODUCES CANNOT BE REACHED BY EITHER VERB THAT CALLS IT.**
>
> **⚠ `spendInteraction` has left the declared-and-uncalled family and joined a
> smaller one: called, correct, and with no observable behaviour.**

**⚠ This is the third time in two reports.** `PT-1594`'s label, the gear pip's
label, and now this. **A ruling that lands in a commit message and not on a
screen has only half landed** — and I could not have found any of the three by
reading the commit.

**⚠ Shape it wants:** the two verbs need a third that does not end the round.
`§5`'s other four — *draw or stow a weapon · drop · pick up · hand to an ally* —
are all in-place, and **none of them exists.** Until one does, the counting has
nothing to count twice.

---

# ⚠⚠ 4 · LOOM HAS BOTH DIALOG PATTERNS AND USES THE GOOD ONE FOR THE KIND IT SAYS IT CANNOT LIST

**I painted from `ways` for the first time in ten sessions. Both dialogs are
excellent, and they are the counter-example to the item dialog — in the same
program, minutes apart.**

    ARRIVAL POINT              DOORWAY
    name  [___]                leads to
    a door in another area       a01-probe-room  a03-probe-yard  a04-probe-slit
    lands here by this name    landing on
    [Cancel] [Place]             pick an area first
             ⚠ GREYED          [Cancel] [Place]   ⚠ GREYED

**✅ `leads to` is a CLOSED LIST of the package's other areas, computed per area
— a01 is offered in a02 and excluded in a01.** **✅ `landing on` is DEPENDENT and
explains its own emptiness** — *"pick an area first"* — and once I picked a01 it
offered **`from-probe-hall`, the arrival I had painted ninety seconds earlier.**

> **⚠⚠ AND THE REFUSAL IS A GREYED BUTTON YOU CAN SEE FROM WHERE YOU CLICK.**

## ⚠ Against which: the item dialog, unchanged. FIFTH REPORT

**`016 · 028 · 031 · 032 · and this.** `path` prefilled `items/weapons/`, `name`
empty, `Create` pressed: **nothing written, nothing said, status bar still
reading `edited a02-probe-hall`.** The refusal — *"An item needs a path — where
it lives IS what it is."* — is at the bottom of the scrolling body, in red,
**below all ~40 base types and below a `description` field.**

**⚠ And `doors` and `waypoints` still carry BOTH lines on `0dd361b`** — the
correct sentence and *"cannot list — no folder is specified for this kind
**yet**"* underneath. **Fifth report.**

> **⚠⚠ I PAINTED TWO WAYS FROM THAT PALETTE, THROUGH A DIALOG THAT OFFERED A
> CLOSED LIST, WHILE THE GROUP THREE LINES ABOVE SAID THE KIND CANNOT BE
> LISTED.**

**⚠ The `owner` box is unchanged and I have the reason it is safe to say so.**
On `dafc553` the wizard took `still-not-a-real-tag` and printed *"still-not-a-real-tag
speaks it — §1, and it is a placement tag."* **`git diff dafc553 0dd361b` is one
line of `pubspec.lock` and no Loom source**, so that measurement is the current
build's behaviour and not a stale one.

**⚠⚠ And the `DOORWAY` dialog is what the `owner` box refuses to be**, in the
same program, for a vocabulary Loom equally holds in memory. **One field picks
from a list; the other takes free text and then cites the rule it just broke.**

---

# ⚠⚠ 5 · AND I SAW WHAT A WRONG `owner` DOES. IT RENAMES THE SPEAKER

**`026` filed an owner naming a placement that does not exist and could not say
what it costs. This is the subtler case, and it is live in my own package:**

    I walked into        probe-warden.probe-room.03      at 4,1
    its blueprint says   conversation = "dialogue/sentinel-challenge"
    that file says       owner = "probe-sentinel.probe-room.04"   ⚠ at 4,3
    the panel is headed  PROBE-SENTINEL.PROBE-ROOM.04

> **⚠⚠ THE CREATURE YOU WALKED INTO IS NOT THE CREATURE THE SCREEN SAYS IS
> TALKING.**

**The owner is a REAL tag, so no existence check can catch it.** `Verify` reports
8 problems and this is not one. **`conversation` on the blueprint and `owner` in
the file are two independent statements of one fact, nothing checks they agree,
and the file wins on screen.**

**✅ And two things worked while I was there.** The digit picked the reply while
the box was empty — `PT-1594`'s constraint, live — and the check reported its own
arithmetic: **`⚠ Persuade failed — 5 needed 14 (d20 5)`**.

---

# ⚠ 6 · AND ONE OF MY OWN FINDINGS IS NO LONGER TRUE

**`033 §3a` said `PT-1554` resolves to nothing anywhere in six trees. ⚠ It
resolves now:**

    playtest/PT-INDEX-01.md:1549   PT-1554 — THE GLOW IS CORRECT AND GEAR HAS NO VERB
    playtest/PT-INDEX-01.md:1589   PT-1594 — ⚠⚠ PT-1554 WAS WRONG

**And at `93985a4` — the commit I actually searched — `git grep "PT-1554"`
returns nothing.** `playtest/` existed; `PT-INDEX-01.md` and 1,113 lines of
`PLAYTEST-RULINGS-01.md` **landed in `2fbccb5`, after my sweep.**

> **⚠ The finding was true of the tree I searched and is false of the tree
> today. That is not an error and it is not a fix: it is the reason I say what I
> built against, and it is the first time that habit has caught one of my OWN
> claims rather than a build's.**

**⚠ The substance stands** — the pip was bright and unguarded, `PT-1594` records
it, and `§4`'s screenshot shows the pip still bright on `df36271`.

---

# 7 · Scoped negatives, and what I left behind

- **⚠ The sweep is a SOURCE READ.** I did not run `gen_docs.py`, `gen_base_rules.py`
  or any audit script. **Running `gen_docs.py` would destroy `PT-627`'s hand
  additions and I did not go near it.**
- **⚠ `ATTACKS-06` (lightsaber, 14 chains) and `ATTACKS-07` (unarmed, 4) I read
  only their headers.** I counted `ATTACKS-04` and `-05` and **not** those two,
  so *"42 chains in the JSON"* is the JSON's count and not a reconciliation
  against all four rosters.
- **⚠ I did not check whether `attacks.json` is stale against `ATTACKS-06`/`-07`**
  — only against `-04` and `-05`, where it is.
- **The third interaction refusal** — never reached, because the second was not.
- **`§5`'s other four interactions** — none exists to test.
- **`PT-1593` on the PLAYER's blow** — I confirmed it on a creature in `033`;
  the commit says it *"gates BOTH SIDES"* and **I did not swing at range.**
- **⚠ `endar-spire`'s orphan** — `§5` is my own package. **I did not re-open
  `endar-spire`,** so `026`'s original case is still measured only there.
- **The app on `919b55d`** — Coder landed `BUILD 100` while I wrote. **Nothing
  here is against it.**
- **Painting TILES in Loom** — I painted two *ways* and **still no tiles. Ten sessions.**

## What I left behind — four fixtures, and they are the only round trip in the package

    a01-probe-room   [[arrivals]] from-probe-hall at 4,2      ⚠ painted in Loom
    a02-probe-hall   door.probe-hall.01 at 1,0 → a01,
                     lands from-probe-hall                    ⚠ painted in Loom
                     probe-feeble.probe-hall.02 at 0,1
    blueprints/characters/probe-feeble.toml   str 1 · vitality 90

**⚠ `probe-feeble` exists because a 1-vitality character cannot survive being
tested on.** `str 1` gives it `attack 1 + Strength −4 = −3`, and the vitality
override stops the fight ending when I hit it. **It is the fixture that lets a
fight be STARTED and then SURVIVED long enough to test something else in the
same turn**, which is what `§3b` needed and nothing in the bed provided.

**⚠ `a01` and `a02` together are now the only pair of areas a player can walk
between and back.** Everything else in `tester-probe` is one-way.

**Backups: `BK3/`–`BK10/`, `SV-T031/`–`SV-T033/`.**
