# CODING-AGENDA-01

Standing, continually-updated list of open work. Updated every time something closes or something new surfaces — struck off (moved to CLOSED) the moment it's confirmed built and verified, not when it's merely ruled. New items get added the moment they're found, not batched. This file is the answer to "what's left" at any point in the project; nobody should have to reconstruct it from the ledger.

---

## OPEN

- **Store console variant is a different paradigm, not a resolution twin — standing note.** Unlike Terminal's `_x`/`_p` pair, `store_x.gui` uses fixed slot buttons and controller prompts, a genuinely different interaction model. The "_x is the reference" precedent from Terminal does not transfer here — `store_p` is the correct comparison for this app's mouse/scrollable-list build.

- **Character-menu live comparison — held, needs Aaron's own direction.** Environment correctly refused direct input to the live session. Needs either Aaron navigating himself while Coder screenshots, or an explicit permission grant.

- **Loom store-creation wizard — opened, held until Store's screen lands (PT-2549).** Investigate the real Aurora Toolset's store/merchant authoring workflow directly (run the toolset, don't just infer from shipped data), then build Loom's own equivalent creation wizard, matching whatever pattern Loom already uses for other content types. Becomes the standard way this project authors real store/merchant content and test fixtures going forward.

- **AI-recreation debt for the three extracted terminal textures — real, held.** Per `ASSET-REPLACEMENT-01`'s standing mechanism, the extracted `uibit_brdr_16we`/`16wc`/`uibit_fill_circ` textures are marked `extracted` with the AI-recreation replacement owed, matching every other placeholder-to-recreation debt already tracked this way in this project.

### Smaller TEST 129 findings — four closed at PT-2544, one real finding ruled
- **Rulebook era-privileging language sweep — opened, real scope, not urgent (PT-2534).** Distinct from `PT-1288`'s already-settled engine-level naming question. The rulebook prose itself may imply a fixed "current era" (e.g. treating K2's own setting as the default present) rather than treating the timeline as something each campaign's own table decides. Sweep for era-privileging phrases ("currently," "the present day," etc.) across the corpus. Measure scope before proposing fixes — likely touches multiple documents.

- **Player-vs-companion portrait sizing — real, confirmed source difference, held, not urgent.** K2's source gives the player a larger portrait (57 units) than companions (31 units); ours are uniform. Correctly not touched without being asked — touches PT-1132's own separately-locked layout. Reads fine as-is; worth a real look eventually, not now.

- **PT-1249's footer right-side furniture — not started.** Portrait with name/class/level, red vitality edge, level-up arrow badge. Flagged, not built.
- **Nav-bar guard pattern — worth a standing note.** Confirmed twice now (the arc guard, this nav bar): a guard for "X doesn't happen" needs to separately confirm "Y does happen instead" — the negative half alone isn't the whole claim.

- **Full active/inactive control-switching concept — real gap, held, own future thread.** `PT-1134`/`PT-1142`'s active/inactive model doesn't exist anywhere in the codebase — confused with the existing but semantically different shared board-targeting marker (`PT-1957`). Genuine architectural collision: two correct rulings claim the same tap gesture, only one built. Resolved by tap: `PT-1957`'s targeting stays on primary tap unaffected; the full control-switching logic belongs on secondary tap once properly built. Not a sidebar tweak — needs its own measurement and proposal.

- **Powers chaining closed for the in-game Abilities screen (PT-2511).** Derived from `prerequisites`, matching K2's own proven method. Verified against all 106 real powers with four structural invariants, directly answering the derivation objection raised at `PT-2508`. Fork decision (3 powers with no K2 precedent) resolved by repeating the shared prefix per path — approved as built.
- **Two powers gate at the level cap (30) — small, noted, no ruling needed.** Spear of Midnight Black, Arrow of White Dawn. Technically reachable, worth having on record if it turns out to matter in practice.
- **Vacuous-pass power fix — closed, one loose end needs Tester (PT-2508).** Fixed the conflation of "not authored" with "authored but withheld." Three guards now prove names/costs reach the screen for a character who knows real powers — but Coder couldn't reproduce Tester's original blank-name observation for a genuinely known power. Needs Tester's specific character or save before this closes fully.
- **Placeholder slot icon (not real art) and unlabelled feat chain tiles — both small, held.**
- **Crush Opposition tier VI (power) — corrected scope, ruled, building (PT-2572).** Corrects PT-2562's premise: Inspire Followers already closed (reclassified out of the power system entirely per PT-220/221/224, shipped as a real three-step feat chain — nothing to author). Crush Opposition: real contradiction between two governing documents resolved by checking the shipped data and an already-live system (PT-2432's alignment-drift, which reads power-cast events with tiers) — both confirm it stays a power. Author tier VI in FORCE-POWERS-01.md, extending tier V's own real curve, wired through the same pipeline as the Sith feat schedules.
- **FEATS-UNIVERSAL-01 §4.1's stale Inspire Followers line — small, held, own future correction.** Describes a plan that was ruled but superseded without the document being corrected. Not blocking Crush Opposition VI.

- **Animated datapad-style frame — future item, held, not started (PT-2499).** Current recreated frame is an explicit placeholder. Eventually wants an AI-developed replacement in a Star Wars datapad aesthetic — glowing edge accents, a living feel rather than static art — possibly with one or two small interactive Easter-egg elements. For later discussion, not a current build item.



- **Sith feat schedule missing entirely — ruled priority, building now (PT-2553).** `sith_inquisitor`, `sith_warrior`, `sith_assassin` — three of six Force base classes — carry no per-level `feat_levels` at all. `grantsAtLevel` correctly reports `feat: false` at every level 1–30; the code is correct, the data is missing. First-level feats confirmed working via a separate mechanism — this is specifically the ongoing per-level schedule. Ruled priority over the tier-VI gap and the Loom wizard — three classes with zero feats at any level is more severe.

- **Feats/Powers catalogue can't answer "what can this character buy at level N" — small, held, real content work.** `buyableAtFirstLevelFor` is chargen-only; a level-9 character should be offered deeper tiers than first-level chains. Not a wiring gap — the catalogue itself doesn't yet answer this question.
- **`chargenData` fallback to empty lists is thin — small, held.** Honest but worth a real pass once something plays through a feat level.
- **Two overlapping full-screen states when Level Up opens over the Character Sheet — small, held.** `esc` has two meanings in this state; same area `PT-2465` already spent a slice on.
- **Chargen re-entry would discard a level-up's feat — genuinely unreachable today, correctly not defended against.** A step-scoped clear actually clears the whole field; the hub is entered from New Game only and a levelled character never returns there. Named rather than patched — building a defence for an input nothing can produce would be worse than recording the gap.


### ⚠⚠⚠ MAJOR THREAD: player-facing UI screens — measured, ruled, building
- **Alignment slider rebuilt from real game art, two more render-and-look defects closed (PT-2478).** Colour corrected — was a genuine sampling error (fixed column, ribbon curves away from it), not a taste note. Asset confirmed neither an arc nor an arrow — a bowed ribbon with two lines, recreated to the pixel from the real source texture. Slot/item subtitle inversion (a real, severe defect — slot view showed no selection when it should) fixed, guard added. "Worn item always among its slot's choices" rule moved from caller into the widget itself.
- **Options/Settings — measured, resolved two real contradictions, building (PT-2523).** Source confirmed to already match the ruling exactly (Exit as a listed entry, footer only closes the screen). Two named occupants both had real problems, both ruled: the companion AI preset picker contradicted `PT-1143` (which correctly places it on the Character Sheet instead) — stale contradicting line corrected in the design doc; the colour theme toggle can't be functional without a real token-layer rearchitecture, deferred rather than built inert. Building the shell, nav bar, escape routing, Save/Load/Exit Game entries, and the description pane — the content this pass actually supports.
- **Inert package-menu Options entry — small, held.** `PackageMenuItem.options('Options')` exists on the package main menu with nothing dispatching it.



- **Force Confusion's missing range limit — small, held.** Reachable at 9-10 squares; the row itself states no cap.

- **Gundark's Acute Hearing (12) and Togruta's Montral Echo (5) — no creature carries either value.** The field now exists and reads correctly from authored traits; neither ability is on a shipped blueprint yet. Own separate content decision.
- **Damaged-save reporting confirmed working correctly — noted, not a defect.** The product's own corruption message ("holds 569 bytes... file has 529") reported accurately by the game itself, found incidentally during TEST 119, predates this session's work.


- **Walking into a confused companion — small, held, genuinely unreachable today.** Under the faction-change override, walking into a confused ally would theoretically hit the existing walk-into-a-party-member refusal. Nothing in the corpus currently confuses a companion, so this branch is unreachable — held until something actually authors a companion-confusing effect.

### ⚠⚠⚠ SEVERE: alignment tracking opens first, `REST-AND-MEDITATION-01` follows (real dependency confirmed)
- **Day clock — closed (PT-2430).** Found already fully settled (`CLOCK-01 §5/§6`): day-only granularity, GM-declared elapsed time, no separate "narrative clock" object. Built as a logged event + a ledger fold (`elapsedDays`), matching the same pattern as `partyIn`/`flagsFrom` — no stored counter.
- **Session window and meditation's two tiers — closed (PT-2431).** Session built as `sinceSessionStart` fold, with a genuinely dangerous edge case caught: no marker yet must mean the whole log, not an empty window, or passive recovery would silently self-grant to a character who used dark powers all day before any session boundary existed. Meditation built as two discrete event kinds (`meditation.short`/`.long`), capped by count not duration. "75% of lost" verified as the only reading that keeps `§4.4`'s own claim true (short meditation never fully restores) — two plausible wrong readings would have lowered the ceiling instead. Rounds down, flagged as a reading not a stated fact.
- **Alignment drift accounting — correctly deferred, own next slice.** No power-cast event exists in the ledger yet to fold drift from. Correctly not invented as a side effect of the foundation work — the cast event's shape (power id, tier, side) belongs to the alignment slice's own design. Foundation is genuinely complete and testable; the fold is "one line once the event exists."
- **Alignment drift — CLOSED, verified directly against the source document (PT-2432).** `ALIGNMENT-01 v2`'s core mechanism built and confirmed: `powerWasCast` event, seven bands with directional hysteresis, per-encounter drift at highest tier, Neutral reduction, Wisdom resistance, session-scoped passive recovery. Two more fixture-found defects fixed (unclosed encounter, Wisdom's minimum-1 floor). One real interpretive call (recovery requires a completed session window, not an open one) verified sound against the actual text. A genuine `Alignment`/Flutter naming collision broke three consumer repos while all 1,583 engine tests stayed green — renamed to `AlignmentStanding`, kept as a standing lesson (a pure-Dart engine's clean suite says nothing about its consumers). Deferred, correctly: cost multipliers, `§2.4` story shifts.

### `§10`'s reaction chain (Parry, Snap Shot, Overwatch) — small, held, own future question
- **Reaction refresh cadence — not yet a question, will become one.** Deflection was the only thing ever spending the per-encounter reaction pool; now that it's Force-only, nothing does. The day `§10`'s chain is built, whether its members refresh per round or share the same per-encounter pool is the identical question just resolved for Deflection — flagged now so it isn't rediscovered from scratch later. Nothing broken today.

### Small items found along the way
- **Explosive/Ion/Plasma Rocket's damage-secondary gap — small, held, different kind of gap.** Three items state a Secondary and extract no effect, but their secondary is damage data, not a condition — already correctly visible in an existing broader census (548 across 144 shapes), not silently hidden the way the seven were. Damage modelling, not this defect.



### Unrepresentable-concept spinoffs — held, deliberately
- **`entangled` — held, own source ambiguity.** `require_check`, the third `§7` field, deliberately left unbuilt. Its own spec states `forbid: move # or halve, per source` — the source itself doesn't commit to one behavior. One real application (Adhesive Grenade).
- **Force Push's size-viability gate — held, own separate question.** Four real conditions (size, not-turret, not-a-specific-droid-shield-exception, not-immobile), over creature size — a concept that doesn't exist anywhere in the corpus at all, not just for this one power. Same treatment as named-character gates. `mayTarget` stays correctly permissive; not urgent, doesn't block anything.
- **Named-character gates (17 items)** — correctly unrepresentable, this format has no canonical-named-companion concept.
- **Authored-creature feats** — own separate unforced format question, no urgency.
- **Personal Crystal (45 items)** — own small future item, variant selection not an equip-gate.

### Held, deliberately, with real reasons
- **`heal`'s silence on fallen allies — small, held.** Neither its row nor its prose says whether it may target a fallen ally. Correctly not guessed at during the ally-power targeting sweep (`PT-2379`). Doesn't block anything.
- **Force Camouflage's deactivation affordance — general mechanism still genuinely absent, but no longer blocking Camouflage itself.** `§4.2`'s ten-round bound now reaches the table, closing Camouflage's own need for it. A real, standing toggle/sustained-state mechanism still doesn't exist anywhere in the product; any future power stating "until deactivated" still meets this same gap.
- **Dark Fury's kill-driven ramping bonus** — a third distinct "ramp clock" shape, held deliberately per `PT-2189`'s reasoning against building new infrastructure for one power.
- **`Pinning Fire`** — the entire `ATTACKS-04` tree has no code at all. Would collide with the already-existing `Overwatch` and is the middle rung of a three-tier chain that needs rethinking alongside it. Not urgent — none of the tree is implemented regardless.
- **Membership question** — should perception drive who joins a fight (currently contact-based). Its own deliberately-held future thread.
- **`NotReorienting`** — 3 of 301 creatures carry it. No author for the field yet in our own content, correctly not built ahead of any real use.
- **AbilityDrain's opposed roll** — held, refused.
- **Thrown condition/damage-over-time grenades (e.g. Adhesive Grenade's Entangle)** — held, correctly not built yet. `_grenadeLands` explicitly refuses anything that isn't `EffectKind.damage`. Confirmed by TEST 106 (own routing error on my part, owned and corrected — this had already been disclosed as not-yet-built). Not to be routed to Tester again until actually built.

### Standing, low-priority
- **21 orphaned upgrade components under `PT-2184`** — 18 K1 lightsaber crystals and 3 K1 weapon parts, standing ruling (K2 is the standard). Confirmed correct, stays unplaced.
- **16 alignment-conditional damage terms** — needs target-alignment tracking that doesn't exist.
- **"Bonus" mechanic granted by nothing — small, held.** Nothing anywhere in the product grants a Bonus, making `slowed`'s "Bonus lost" and `cowering`'s "Bonus kept" currently unfalsifiable. Not a defect in the condition work; a pre-existing absence elsewhere.

---

## CLOSED

- Credits defect fully closed across all three readers (Store, Inventory, Dialogue), plus the shortfall warning fix. Reproduction built directly against Tester's own tester-purse fixture, replaying PT-2569's exact sequence — two real fixture problems found and properly fixed (package-specific start point, correct return target after closing Store), rather than routed around with a shortcut. Every mutated location confirmed to fail without the fix. Unrelated pre-existing test flake investigated in isolation, confirmed unrelated. Process change approved: full suite reserved for one pass before committing, targeted runs during iteration — PT-2571

- Store's header property and real border art both close, built together as ruled. Use-pattern data honestly derived from a defensible existing signal, with genuine restraint (left null rather than guessed for categories that couldn't be confirmed). Real pre-existing gap caught incidentally — PT-2556's Sith schedule had never reached the shelf repo, since the checker only compares disk to the extractor, never git — folded into this commit. Border art's fill inset derived from the source's own authored ratio, not eyeballed; Terminal's already-ported textures reused exactly as instructed. Implementation deliberately kept private to Store rather than generalized with Terminal, avoiding any risk to an already-shipped screen — PT-2570

- TEST 132 confirms the whole prior round of Terminal/Store fixes: the 2×2 grid (measured pixel-smooth, no step artifacts), free options, the third texture (confirmed via a genuine mutation test on the real asset file after recognizing a plain circle's shape can't carry provenance information via similarity score alone), and all three small closures (In Stock, In Inventory, display name, Notes autofocus). Panel overflow re-confirmed fixed at both anticipated window sizes. Phantom credits row confirmed gone — PT-2566

- All four items from PT-2560's priority ruling closed. Display-name mismatch correctly diagnosed as fixture-authoring error, not app code — the join was always right; fixture renamed, on-hand text and In Stock count hardened as additional real improvements. Notes' autofocus traced to a real race condition (persistent, never-unmounting parent stack) invisible to isolated widget tests — fixed with explicit postFrameCallback focus request. Self-caught vacuous first regression test (removed the sibling widget instead of keeping it mounted, would have passed regardless of the fix) rebuilt correctly. Sith Battlemaster feat schedule authored — a tempting lineage hypothesis ruled out by actual K2 stat measurement before the true mechanical match (Jedi Weaponmaster/Sith Marauder) was found and mirrored; independent audit guard extended, mutation-confirmed it can actually fail — PT-2561

- Terminal's two severe live-comparison defects and the full asset-parity sweep all close — Terminal thread now genuinely complete: structurally correct, functionally complete, visually matched to the real source. 2×2 grid rebuilt from already-extracted data; a real secondary geometry conflict (stat boxes vs. border art's native texel size) solved elegantly by generalizing the border renderer to a configurable corner size, which naturally produces the correct rounded pill shape. Free/costed content-modeling gap fixed; caught a real secondary bug in the row-width validator along the way (padding free replies with costed overhead before measuring). Third texture ported (uibit_fill_circ). Genuinely excellent judgment: a fourth referenced control deliberately NOT ported after confirming against the live screenshot it never actually renders on this platform — correctly recognizing porting it would move away from exact parity, not toward it — PT-2559

- All five TEST 131 severe defects closed, exactly addressing both anticipated failure modes. Terminal's panel-scale fix inverted an existing test that had been asserting the bug as correct behavior. Genuinely valuable self-caught false negative on the credits-row fix — first test exercised the wrong transaction direction, would have passed clean against unfixed code, caught before being reported done. Store's In Inventory fix found a second root cause (forSaleBack's own deduplication had already discarded the count). Sith feat schedule (Inquisitor/Warrior/Assassin) closed, calibrated against already-ruled totals; a fourth zero-schedule class found and correctly held rather than silently fixed; a tautological audit clause caught and replaced with a real independent guard — PT-2556

- Terminal border art ported and wired, first real game texture this product has ever embedded. Both textures confirmed as pure alpha masks before wiring, matching the existing tint-at-runtime pattern. Deep rendering bug found and fixed: standard Image widgets never painted within a test's pump cycle, traced precisely to real asset I/O never resolving outside runAsync — fixed with a CustomPainter approach that also enables per-tile rotation the original approach couldn't support. Mutation-tested by removing a texture file (build itself refuses to bundle). Backdrop question correctly not decided unilaterally, ruled at PT-2552: PT-1150 stands unchanged, the console backdrop stays a synthesized original (K2's structure, K1's warmth), not a direct port — genuinely different in kind from the two generic alpha-mask textures just ported — PT-2552

- Terminal's real border art ported and wired — `PT-2551`. CORRECTION to `PT-2545`/`PT-2550` confirmed first: the Terminal/Computer Use screen was never genuinely unbuilt — a real, working, tested build (`TerminalPanel`, wired end-to-end through the conversation system, real cost/supply logic, a substantial passing test suite) has been on main since before either ruling. The one genuine remaining gap — frame art still a generic placeholder rather than the source's real `uibit_brdr_16we`/`16wc` — is closed: a new stdlib-only extractor (`extract_terminal_textures.py`) pulls both from K2's own `swpc_tex_gui.erf`, decoded through the project's existing verified TPC reader. First real game texture this product has ever embedded — everything else has drawn from the theme's own palette. A real rendering-layer defect was caught and fixed along the way: `Image.asset`/`DecorationImage` never painted within a widget test's pump cycle (confirmed by isolating the exact cause — real asset I/O started outside `tester.runAsync` never resolves in this test binding, and a `setState` from a `Future` callback schedules nothing `pumpAndSettle` will wait for), so the frame is drawn with a `CustomPainter` over a pre-decoded `ui.Image` instead, which also gives correct per-tile rotation a `DecorationImage` could never do. Tracked in `ASSET-REPLACEMENT-01` as `extracted`, AI-recreation owed.

- Store/Merchant built and closed, engine and app both. Minimal blueprint reader with merchant economy honestly flagged as unruled rather than assumed. New `store.opened` effect correctly matched to the older advisory architecture family. Real dead-field defect caught only by end-to-end testing (a title set correctly but never displayed — removed rather than wired to behavior the source doesn't have). Real self-caught regression in Loom's own authoring tool from a routine repin — same silent-no-op shape `PT-1516` already fixed once for a different field, fixed in all three affected places with mutation confirmation each. Full end-to-end fixture built and verified (walk to merchant, talk, open store, buy/sell writes real events) — PT-2550

- Companion Character Sheet routing gap closed. Investigated the real rules before coding — companions genuinely don't level independently under this ruleset (PARTY-01/LOOT-01), so the fix changes nothing observable today, honestly reported as such rather than overstated. What changed: the mechanism is now general rather than hardcoded, correctly future-proofed for any future source of a companion's own record. Dedicated mutation test targets the more dangerous failure direction (removing the identity check would leak one character's data under a different portrait, worse than the original defect). Closed a real gap where an existing test file's own header comment claimed uniform behavior neither of its tests had ever actually exercised — PT-2547

- ⚠⚠⚠ THE ENTIRE SEVEN-SCREEN SHELL THREAD IS FULLY COMPLETE. Keyboard shortcuts under Options built (censused off the real key router, 25 keys). PT-1249's footer portrait built (no single source directly answered it — synthesized from the ruling's own words plus the closest real precedent, three points of real disagreement identified and correctly resolved in the ruling's favor; integration testing caught a real 0.09px overflow invisible to widget tests using synthetic data). PT-1132's player-vs-companion portrait sizing built, keyed on isPlayer/self rather than identity so it generalizes correctly to multiplayer — two real side findings caught (sidebar's hard-coded wing height, an untested footer chip size caught only by mutation) — PT-2546

- Four of five smaller TEST 129 findings closed: both stale comments (the "three of seven" one, and the portrait-as-circle one, corrected in place with the model-viewport confusion explained rather than just deleted), the stuck level-up label, the DIALOG→Dialogue pane label, and the misplaced note-privacy gate — generalized to close the same hole in both readers, error message improved to name where a gate actually belongs — PT-2544

- All five severe TEST 129 defects closed. Condition classifier: a real defect caught in Coder's own already-written first attempt (a positive "bad" list — structurally the exact shape ruled against, wearing a different disguise) before shipping, honestly disclosed and rebuilt correctly — default is now bad, good requires its own positive source, currently empty. Notes' focus, the dismissed-companion name, the phantom re-add state, and the four-screen escape gap all fixed with precise root causes. Two more self-caught vacuous guards (a resolver bypassed by a pre-computed test value; a phantom-fix with zero coverage until mutation proved it) — both attributed directly to disciplined mutation testing rather than suspicion, second slice running with this exact shape. Phantom fix reproduces Tester's exact reported symptom through the real board — PT-2542

- Sidebar portrait fix closes the whole seven-screen shell thread completely. Verified independently against the sidebar's own real source (mipc8x6_p) rather than inherited on trust — confirms the same square-shape conclusion. Incidentally verified a separate, previously-unconfirmed design claim (PT-1133's flanking vitality/force bars) as a byproduct of the same check. Guard built to name both screens together, so either one changing can't silently reopen the split PT-1132 forbids — PT-2537

- ⚠⚠⚠ THE SEVEN-SCREEN SHELL IS COMPLETE. Character Sheet, Equip, Inventory, Abilities, Options/Settings, Journal/Messages/Notes, and Party Selection all built and closed. Party Selection's own close corrected two real errors in the portrait ruling (size was 3x too small; shape was wrong — circle was always only a documented placeholder, square is the true intended shape in both source games), caught three more self-found defects (a dead layout constraint whose test was too weak to catch it, an invisible scrollbar thumb painted in the wrong token, a proportional drift between text sizes), and honestly disclosed a false citation in a comment/test even though the underlying fix had already been correct — the standard this whole thread has held throughout. Sidebar portrait ruled to match the same correction — PT-2536

- Journal/Messages/Notes fully closed, both TEST 128 defects fixed — sixth of seven shell-locked screens complete. Privacy guarantee found stronger than originally reported (text never enters the log at all, not just "never reaches a gate") — storage interface rebuilt to accept only a length, structurally preventing the weaker version from recurring. Quest format read directly from K2's real global.jrl (117 categories, 728 entries); Priority confirmed a genuine first-class field, independently reconfirming the four-key ruling for a completely different reason than either the owner's or Coder's original reasoning. Message history built as a fold over the real event log (never existed before). Nav bar's silent tap and Options' misattributed reason text both fixed, with the nav-bar fix's own guard honestly found to have checked only half the claim (blocked navigation, never confirmed the reason displayed) — same shape as an earlier arc guard, callback made required rather than optional to close it structurally. Precise correction accepted: the original Whisper report was never a claim about inspecting a save, only about the mechanism — nothing to reconcile — PT-2532

- Whisper's "all ?" mystery resolved — was correct behavior, three-tile sample happened to hit three of 102 unlearned tiles by chance. Every held save decoded directly confirms real powerTaken events; both a priced and unpriced known power confirmed rendering correctly with a real control. Fully owned, including the real cost (three of Coder's guards spent verifying a wrong report). One small detail left honestly unreconciled (Coder's inspected record showed no events, matching none of Tester's saves) — most plausible explanation offered (regenerated fixture), not forced to a conclusion — PT-2530

- Shared nav bar built and the portrait default-screen fix unblocked — whole thread closed. One shared implementation on the shell, not five per-screen copies, specifically because the ruling requires identical positioning everywhere. Escape-to-Options split confirmed matching K2's own two separate controls. Three self-caught issues in one slice: a status line asserting a repealed rule (guard now holds wording and binding together); a visual clipping bug invisible to every automated check, caught only by the mandatory render; a vacuous first guard for that same bug correctly diagnosed and rebuilt against Flutter's own real geometry rather than a re-derived formula. All six affected tests rebuilt to walk the real player path — PT-2526

- Pirate's save table and the third Gunslinger route both closed — whole Pirate/prestige thread now complete. Merge mechanism's real asymmetry diagnosed (adds fields to existing rows, never adds rows) — Pirate alone kept a gapped PHB shape since it's the only prestige class still carrying a printed-source table. Fix correctly scoped narrow: no printed source of its own → full authored range; the twelve real base classes correctly untouched, keeping their gaps per the already-standing `PT-2163`. Third route verified with real, complete data at level 6 specifically, not just Pirate's bare presence in the roster — PT-2519

- Hit-die extraction fix, Pirate shipping, and the Gunslinger entry-route questions all closed. Hit die was authored in the source the whole time, matched by a recognition pattern that threw the value away — set only where nothing else already answers. Pirate's orphaned row (missing header broke the extractor's row-shape scan) fixed; 39 classes, 20 prestige. Pirate keeps its already-authored skill base of 4 rather than the hybrid's computed 5, correctly respecting existing content over overwriting it with a formula — independently cross-verified against class-skills.json. An existing audit had been silently reporting "20 classes" against a 19-class roster the whole time; Pirate was the missing one. Prestige-to-prestige entry ruled intended, not a gap — PT-2518

- Prestige class skill points closed for real, corrected values (3/5/7 baseline, not the stale 2/6/8) with the approved hybrid spread — five distinct values across 19 classes. `PT-2490`'s audit safeguard re-confirmed passing — PT-2517

- Level-up Powers offer filter and the droid hardcoding both closed — the whole Powers-chaining thread now complete. Offer fix matches the ruling exactly (shown-and-refused, reason always visible, unpriced routed through chargen's own mechanism). Self-caught design flaw: a hardcoded "known non-power prerequisite" set would have silently refused any future unanticipated gate type — caught by a test using a deliberately invented value, fixed to derive from the roster's own real names instead. Droid defect found three times worse than named (Abilities and Skills also hardcoded wrong, not just Feats) — fixed completely, not just the narrow scope originally flagged. Verification includes a positive control (a genuinely open tier can still be spent), applying `PT-2507`'s standing practice immediately — PT-2513

- All four TEST 127 defects closed, each with genuinely honest self-correction along the way. Double-border mechanism owned as never actually built despite being reported done at `PT-2498` ("a marker alone marks nothing") — real scope corrected to all four panels, not the three visible to Tester. Empty-bag fix's own first guard checked text that didn't match what actually rendered, honestly disclosed. Vacuous-pass power fix separated "not authored" from "authored but withheld" after a first attempt left a real mutation surviving. Boots join fixed with a general fallback rule (category names a lattice slot → item goes there), guarded against overreach. — PT-2508

- Equip lattice now guarded as 12, not just arranged as 12 — the arrangement never needed changing; the missing piece was a count guard, since the existing row-by-row test could never have settled the eleven-vs-twelve question on its own. Precise, targeted mutation (removing Boots while silently updating the arrangement expectation to match) proves the new guard catches real drift rather than duplicating existing coverage — PT-2505

- Equip's slot-count discrepancy closed — 12 slots, matching the diagram exactly as originally drawn. No new slot needed inventing; Boots (the genuinely new feet slot, no KOTOR source precedent) was already one of the diagram's 12 filled cells. The "eleven" in the surrounding prose was simply stale, never updated after Boots was added — corrected directly in `design/APP-UI-VISION-01.md` in both places it appeared — PT-2503

- Shell wired into play — the whole shell-layout thread closed. Root cause: source canvas and design dimensions were coincidentally exactly 4:3, so a frame preserving that ratio always cost a 16:9 play screen a quarter of its working measure regardless of viewport size. Resolved non-destructively — chrome correctly identified as blameless (always built for 16:9), the frame itself yielded instead; page panels keep 4:3 (real `.gui`-authored coordinates), board fills freely (no such constraint). Second self-caught bug: stretching scaled the frame's own rails proportionally, paradoxically shrinking usable area on wider viewports — fixed to use the smaller scaling factor, final result genuinely better than even the unframed baseline. Self-introduced crash from an unsafe widget-lookup pattern found and fixed, honestly disclosed. Double-border render prevented (inner panel frame yields to the outer shell frame), explicitly connected to the already-rejected option C from the original layout decision — PT-2500

- Frame scale/fit closed. Real offender identified as Abilities (510px overhang), not the three screens originally named — measured individually against the bare frame rather than assuming the ruling's own scope. Earlier deliberate decision to permit the overhang correctly overturned ("it is the source's doing, and it still reads as a defect"). Separate double-scaling defect diagnosed and fixed (full layout pass against the wrong viewport, then bitmap-squeezed into the frame) — scale parameter removed entirely, not just redefaulted. Genuinely sharp self-caught measurement error: an identical number across three different layouts correctly read as a systematic bug in the measurement tool, not three coincidental real defects — traced to a sub-pixel boundary question, confirmed zero practical consequence. TPC/ERF decoders verified against real known content, promoted to `MAIN_WORK/scripts` — PT-2498

- Abilities built — fourth of seven shell-locked screens. `skillinfo.gui` confirmed unfinished developer scratch (keyboard-mashed placeholder, message copied from a container panel), correctly disregarded. Feats mechanism confirmed as a genuine two-pane stack-and-swap, matching the Store's already-proven pattern. Rebuilt once after correctly recognizing the first pass had accidentally used Inventory's column idiom instead of the real layout. Render-and-look found genuinely severe defects: two different feats rendering identically as a single letter, and a null cost printing as a stated price of zero, directly contradicting the cast path's own refusal to state one. Import prefixed rather than renamed, citing `PT-2432`'s own precedent. Five mutants killed, each a real gap — PT-2497
- Shell layout decided this session (owner + Coder direct work): two panes not three, party rail held at its real current width, log deliberately left undrawn rather than mocked prematurely (a drawn log would itself read as a decision not yet made). Two real mock errors caught directly by the owner (party rail's side, its real proportion against the frame) — PT-2497

- Inventory built — third of seven shell-locked screens. Six real corrections found by checking the actual K2 source files (two rules not one, a filter label the text never mentioned for this screen, precise layout details, the seventh category confirmed PC-only validating the locked design's choice, K1's structurally different screen confirmed to leak nothing in). Category classifier correctly split across two data sources per existing `PT-1957` ruling, sixty categories correctly not hand-mapped (real authoring work, out of scope). Render-and-look found three defects — two owned directly, one honestly credited to the test suite rather than claimed personally. Old status-line's "nothing picked up" vs. "no inventory concept" distinction correctly preserved and sharpened with a third case (filter hiding everything). Use Item correctly kept report-only, not a second spend path — PT-2494

- Multiclass display fixed across all three surfaces (party card, sheet header, Character Info) — reads "Soldier 3 / Jedi Consular 1", reusing the same difference logic an existing function already uses. Self-caught mutation gap fixed (first guard only forbade the separator, would have passed a wrong build that reformatted every single-class character). Fixture blindspot fixed (persistent header let an assertion pass before real page content existed — "the fixture could not tell the page from the chrome above it"). Two small items closed properly (null-string interpolation, credits traced to the real record); one genuinely careful edge case handled — the old placeholder value scoped and renamed to survive only in the one no-character test configuration, after confirming a naive default would have silently changed which dialogue options exist. Third item confirmed already closed, not re-fixed — PT-2492

- Fourth Force pool cause properly closed. New fixture actually fights, kills, and levels — genuine content in the session log. Guard demonstrably fails when reverted (refills to full maximum from a log that never heard of the cast). Closes exactly what was honestly left open at `PT-2489`. Full 38-class, full-level-range grant-schedule audit built and confirmed sensitive (feeding it a known-wrong schedule produced hundreds of disagreements). One self-caught wrong expectation pinned correctly (level 1 grants two powers, matching creation's own number). Two real data gaps found and ruled/held separately — PT-2490

- Vitality and Force pool refresh both closed, both paths. Root cause precisely refined Tester's own accurate symptom-level observation: not a literal hardcoded constant, but a real recompute running against a class list frozen at board-open, so Force's die term structurally couldn't move. Vitality confirmed as a genuinely skipped recompute (guarded to run once ever), tracing to PT-2474 having repaired only the Force half of this mechanism. Both now share one `_refreshForLevel`, called by both paths. Ruled: current Force does not automatically heal alongside the rising maximum at level-up — mirrors the pool's own already-ruled treatment rather than inventing new healing behavior — PT-2489

- Level-up reaches play, end to end — the whole thread closed for this slice. All five commit events reuse existing chargen kinds, correctly anticipated by the data model's own comment. Honest near-miss: suspected a real gap (an unread field), verified before acting, found the multiclass upsert logic was already correctly built — "I nearly 'fixed' it," reported rather than quietly moved past. Two guards self-caught: the end-to-end test had bypassed real choice-making entirely (proved only the level was written, never the choices), rebuilt to drive through the real screen; a test that wrote nothing was an incomplete fixture, not a broken commit, verified directly and turned into a standing precondition — PT-2485

- Recreate-vs-extraction policy question closed — owner reviewed the rebuilt alignment slider directly, confirmed it looks good. `ASSET-REPLACEMENT-01`'s recreate-close standing rule stays governing; "extract the assets perfectly" resolved as describing that standard already met, not a request to switch to bit-exact extraction — PT-2480

- Alignment marker fixed — two independent defects, either alone sufficient (a measurement divided by an unrelated dimension; fixed-pixel hairlines coincidentally matching a derived gap, closing over the whole band). Fixed by re-deriving all thicknesses from the same texture's own row count, so the gap and outlines can't drift apart again — structurally the same unit. Fill changed from opaque to a measured wash, letting the gradient read through. Three assertions, one per cause, deliberately separate. Standing lesson recorded: a number measured off the right object and divided by the wrong one — confirmed twice in two slices (the same shape as the grey-waist colour error) — sourced, which is exactly what makes it look trustworthy while being wrong — PT-2479

- Fight-outcome fix landed, all four fixtures properly repaired. Fix confirmed as one line, matching the strike path's existing pattern exactly — wrong first attempt (copying the strike path's board-sweep call too) correctly diagnosed and discarded, since a cast can legitimately need creatures a sweep would remove. "Four guards were asserting against corpses and none of them knew." Repairs use existing copied-shelf infrastructure, boost vitality directly (not a second creature, avoiding a confound), leave the caster's level untouched (keeps the save exactly as unreachable as before — deterministic, not a coin toss). Both repairs loudly fail if the survival guarantee ever moves. Honest self-caught git mistake: destroyed own uncommitted repair via the exact behavior a prior standing note already named — caught immediately, named plainly — PT-2477

- Auto Level Up's stale-pools defect closed, confirmed wider than originally understood. Not just a display gap — the handed-in record was frozen at board-open time, so ~10 real derivations (base attack, saves, DCs, the sheet, Force pool's true maximum) all silently computed against the pre-level-up character. Fixed by folding record and log together, reusing an existing pairing rather than inventing a parallel mechanism. Force pool correctly re-derives its true maximum from the now-current class while preserving the log's own real projection for current/ceiling — a level-up raises the ceiling, doesn't silently refill what's spent — PT-2476

- Package-fault sentence plumbing closed — the whole silent-failure investigation now fully closed. Summary tile correctly kept unchanged (still just the count, no invented category, citing the standing ruling that already bars exactly that). Real sentences surfaced below the row, scrolling, one per fault. The real substance correctly identified: showing every fault rather than the first — the obvious "copy the nearby pattern" move would have reproduced the same original defect in a new shape. Proven against the real bench's own actual fault order (four benign, then one real). Honestly stopped at the real limit: nothing in the data distinguishes benign from severe, and building an automatic split would itself be the barred mechanical category. Methodological lesson merged into Tester's own existing note rather than duplicated, with a real complementary addition (the false cause and the true lead had to be separated before either could be discarded or kept) — PT-2469

- Async race in the menu status line closed — three real states now, where there was one permanent blank. Found and fixed a genuine second defect while building the first: the obvious timeout utility left an uncancellable background timer leaking on every screen closed mid-listing, caught via an unrelated test failing for what looked like an unrelated reason. Guard built against the real shipped duration specifically so it can't pass against the leak it exists to catch. Correctly recognized a genuinely in-flight read can't be cancelled — the timeout controls only the message's silence, and a late real result correctly replaces "timed out" rather than being discarded beneath it. No retry button built — the natural leave-and-re-enter flow already provides one, a dedicated button correctly left for its own ruling — PT-2468
- Two original silent-failure claims (empty-party load, tag/handle mismatch) confirmed to not reproduce at all — `TEST 122`, both directly re-tested against the current build. Traced to a real methodological gap: routed items received matched pairs and controls throughout this whole session; these three asides each got one reading and a plausible cause. Both `TEST 120` and `TEST 121` amended in place with retracted notes; everything properly controlled in both reports (the five-round durations, all ten droid refusals, the whole Force Confusion battery, the `Present.placed` classifier finding) remains fully valid and unaffected — PT-2468

- Escape/overlay router gap closed. The character sheet, bag, and map now consume escape by closing themselves. Correctly scoped narrowly (escape only, not the full keyboard) since these three stay actionable underneath, unlike the fully modal screens that correctly swallow every key — flagged explicitly as an interpretive choice rather than decided silently. One press closes exactly one overlay. Original leave-when-nothing-is-open behavior confirmed untouched — PT-2466

- Duration change and droid gate both confirmed decisively in real play — TEST 121. Duration confirmed on both reachable powers via two independent methods converging on the same number; beast variants' duration correctly reported as inherited, not observed (nothing can currently be classified as beast). Droid gate re-confirmed through real casting with the Force pool itself as an independent witness the refusals cost nothing — PT-2464
- Continue/New Game timing trap and the mis-worded outside-fight refusal both closed. Pointer system didn't respect visual disabling at all — fixed to genuinely ignore pointers on disabled rows, keyboard guard preserved. Refusal fix found the codebase's own pre-existing comment already naming the exact defect before anyone set out to fix it — PT-2465

- Duration balance ruling fully closed — all four mind-affecting powers (Force Distraction, Force Confusion, Beast Control, Beast Confusion) now state five rounds. One-slice asymmetry correctly reported rather than unilaterally tidied before the ruling landed — PT-2463

- Force Distraction/Confusion's duration change, the third droid classifier, and the chargen validation gate all closed. Duration change caught a silent duplicate `?? 10` default in the app itself, removed — an absent field now fails loudly instead of silently handing out the old number. Third classifier unified with a default (not required) parameter, correctly chosen for this validator family's own "empty means disabled" convention rather than copying the prior fix's exact shape; a real forwarding gap caught by mutation (one validation path reached, the other silently left on the old rule). Chargen sweep's first fixture under-populated relative to the real app's own construction, caught and fixed. Sweep honestly proven to cover only the minimum legal output, not every full character — a full combinatorial sweep held as its own future slice. Gate approved regardless: it calls the identical validateRecord call the load path already makes, so it structurally cannot refuse anything that wouldn't already be refused later — only moves when an inevitable refusal becomes visible — PT-2462

- Droid classifier defect closed properly — the duplicate `Present.placed` copy deleted entirely rather than repaired, so there's structurally one answer to "what kind is this" now, not two that could drift apart again. Correctly proceeded without waiting for a fresh ruling, same severe class already established. New guard sweeps placements specifically (the half the original guard couldn't see), with a second self-caught gap closed (a chassis-only droid case, matching `PT-1490`'s documented blueprint shape). Honest self-diagnosis recorded as a standing lesson: count what does the job, not what calls the function — the fourth confirmed instance of one answer kept in two places this session, explicitly connected to `PT-2451`'s writer-not-field lesson as the same principle from a different angle — PT-2457

- Force Confusion fully confirmed in real play — TEST 120, every routed clause. Both fighting-for-you directions confirmed with careful staging to overcome the AI's own targeting-preference bias. One-of-each-kind confirmed as the active instance (a second cast succeeds once the first genuinely expires), no stand-at-1 (confirmed going negative), returns to its side, and the win-condition test solved with genuinely inventive terrain engineering (an impassable-but-sight-permeable water pocket isolating a confused creature so the real party-wipe condition could be cleanly tested) — PT-2456

- Facing/hear-turn observability fully closed — hearing gained its own range field (absent means as-far-as-sight, never exceeding it), the Sith Trooper gained the project's first shipped `blind_spot` (120°). Three real tests broke on the new content, all correctly diagnosed: a duplicate TOML key silently failing character load (the same trap Tester hit, caught in Coder's own new content before shipping), an existing control that had only ever been true by accident (no blind spots existed anywhere to be absent from), and a baked-in "adjacent therefore seen" assumption that stops holding once a real blind spot can put an adjacent player behind a creature — PT-2455

- All three severe items from `PT-2450` closed, plus `container.opened`'s missing guard. Confusion's early-return gate fixed (missing `!p.confuses`), the confusion-tracking write closes with the same fix (same root cause, one gate firing first blocked both). Direct, honest ownership of the mistake — the exact twin-list shape the nearby comment already warned against. Severity-compounding explicitly validated as `PT-2240`'s pattern arriving from a new direction. Droid classifier fixed to require the real `is_droid` set rather than default to it (a default of `{'droid'}` would have been the bug itself) — confirmed to have been breaking two powers in opposite directions at once (Stun Droid wrongly refusing, Confusion wrongly accepting the same four species). Guards assert three independent signals (spend, tracking write, targeting change), not an absent message. A genuine made-save scenario honestly reported as currently untestable given the fixture population, rather than faked with a guard that couldn't fail — PT-2453

- `blinded` fully closed — all four ruled clauses now built (attack penalty, square-naming cursor, Dexterity loss, sight-based auto-fail). Dexterity threaded through one shared function both the attack roll and the sheet read, avoiding `PT-1468`'s exact prior defect shape. Togruta's exception rolled once per encounter and remembered, not per-frame (would have flickered). Hide-contest needed no new rule — the existing "better of Awareness or Alertness" mechanism already falls back correctly. Honest equivalent-mutant disclosure, correctly reported rather than forced or hidden. Third recorded instance of the same guard mistake (testing the ingredient, not the site) — recorded as a standing check: confirm a new guard calls the real production entry point, not the helper underneath it — PT-2447

- Faction-change mechanism fully closed — `force_confusion`, `beast_confusion` both built, `Role` never touched, all 25 call sites individually classified. Genuine correction to Coder's own already-approved proposal found by re-reading: refusal is per caster AND per kind (one sentient plus one beast simultaneously), not one instance total — the row's own text already said so, and K2 really does carry three separate globals. Wipe check confirmed the two-predicate design's real value by forcing the override into it and watching it correctly fail. Another self-caught degenerate guard, explicitly recognized as the same shape as an earlier one this session (tested a reimplementation rather than the real production function). One small, genuinely unreachable edge case (a hypothetical confused companion) correctly reported rather than decided — held until something actually makes it reachable — PT-2443

- Rest/meditation's two real play-confirmed gaps both closed. Reload asymmetry traced to its root: only `encounter.ended` had ever carried Force state, so a cast, either meditation tier, or a rest had nowhere to persist outside a fight — all four events now carry it. A subtle, easy-to-get-backward bug caught and explained precisely: recording what an action left (`after`) rather than what it found, since a naive `before` read would have silently made every power look free. Absent field correctly means "leave the pool alone," specifically protecting every pre-existing save from a catastrophic empty-pool misread. Alignment readout added (band and score together, since the hysteresis rule means the same score can sit in two different bands — neither number alone is checkable). A guard caught a real oversight (four new event kinds not registered in a related declaration set) — honestly credited to the guard, not claimed as a personal catch — PT-2441

- Force Distraction (Combat Mind Trick) closed — all five slices, zero new consumer code needed, confirming the consumer-first check was worth doing. Two more genuinely valuable self-caught test failures: a fixture that never exercised a genuine made save, and a case where "nothing was distracted" was equally true of a refused cast, a made save, and a broken build. One claim honestly reported as untestable given the fixture's own limitations, with real indirect evidence offered rather than overclaiming or dropping it. `targets: sentient` closed on both rows per PT-2436's ruling. A new guard's own quality catch: it called the underlying helper directly rather than the actual reader meant to use it — fixed to call the real reader — PT-2437

- `REST-AND-MEDITATION-01` fully closed — engine and app, both blockers resolved, real play reachable. Five event kinds declared as a whole (alignment is a fold over the save; one undeclared kind means a silent, permanent flat default forever). Chosen-side field added, correctly absent rather than a fake neutral default. Reload test itself was initially broken (a Flutter widget-reuse pitfall left it reading stale in-memory state) — caught by mutation, fixed, all 15 real mutations now die against a genuine reload. Two of my own prior rulings (`PT-1605`, `PT-2430`) found to directly contradict each other under repeated load-and-quit — reconciled using `PT-2432`'s own already-approved "an empty window is not a session that was played" rather than needing a fresh ruling: the marker rides to the save only alongside the first real event, so an empty sitting leaves no trace. A long-standing gap in `_rest`'s own day-tracking (since `PT-2181`) closed incidentally — PT-2434

- Deflection resource fix closed — Force only, confirmed decisively. More than three deflections across a 40-round fight, with two mutants confirming both directions (old reaction cap no longer binds; new Force cost genuinely charges). The free-deflection control specifically caught that "more deflections than before" alone could be satisfied by a broken, unpriced build recreating the exact passive shape `§4.1` originally refused. Genuinely honest self-caught mistake: Coder's own first assertion assumed every attempt succeeds, walking directly into the blind spot `TEST 117` had already named — corrected, with the mistake recorded rather than silently fixed — PT-2424

- Force Body's latent replaces/stacks divergence closed with a tripwire, not a fix (nothing was broken). A real throw rather than `assert`, since the defect it guards against is silent and correct-looking — a strippable guard would protect nothing where it mattered. Counts what's standing, not the raw list, since a mid-turn list of two expired-plus-active entries is normal and correct. Consequence honestly reported and ruled: the fold's own comparison logic is now structurally unreachable (at most one entry can ever legitimately stand) — removed rather than left as dead scaffolding — PT-2422

- Deflection-alone confirmed in real play — TEST 117. Redirection genuinely removed from the power list (not just unused), so the engine had no stronger tier to reach for. Term-by-term match against TEST 115 confirms Redirection's own `+3` correctly absent and the outcome correctly stopping at "deflected" with nothing sent back — PT-2421

- Eight-item unsavable-penalty defect closed (5 Sonic Mines, 3 grenades/detonators). Naive fix correctly avoided: the same save clause means two different things to its two sibling effects (damage halves, penalty negates) — adding the category alone would have shipped a "halved penalty" no row states and the rules have no meaning for. Outcome read from each item's own prose instead. Second gap surfaced (unspecified save type) resolved by borrowing from a sibling family whose own text states it outright, honestly graded as stronger evidence than the reasoning used for the mines themselves. Guard correctly prioritized: confirming damage stays halved, since a shallow fix could have set every save to `none` and passed every penalty-only assertion while silently making the damage un-halvable — PT-2420

- Rocket/launcher delivery genuinely closes this time — confirmed end to end through the real throw path, not just a corpus diff. Rocket schema was missing both `deploy` and (found while fixing it) the `damage` verb — the second gap would have silently blocked `PT-2409`'s own burst ruling for ten of eleven rockets. Modelled on Charge's proven schema, deliberately excluding two fields after checking no rocket row states them. Launcher gate fixed to match by catalogue id, not filename. Confirmed with three real cases through the throw bed — critically, both refusal cases would have passed on the broken build; only the positive case (a real successful throw) could have caught the original defect, the same shape that felt sufficient at the original wrong closure. Building the positive case found two more real bugs: a package-relative path making every rocket invisible, and an unhandled read exception capable of silently emptying a whole bag over one unreadable file — PT-2419

- Cast menu, aim-verb naming, and Force Mire's `targets` field all closed. The cast-menu gate exposed a genuinely important self-caught false guard: `PT-2360`'s droid push-not-stun test had its own fixture spend the Action before the cast ever ran, so all three assertions were trivially true of a build that cast nothing at all — the exact "absent refusal is not a positive signal" shape, found in an already-approved guard. Fixed to advance a round first and assert the pool genuinely moved. Aim verb correctly only lists while blind (a verb that always shows would offer a key that only ever refuses). Force Mire's `targets` borrowed faithfully from Force Slow ("As Force Slow"). Four paired census counts moving together confirmed as the correct signal for a single row crossing one boundary — PT-2418

- `a_blast_stops_at_a_wall_test`'s hang closed — it was never a hang. Three tests each paying a genuine 60-second timeout, repeatedly, while an already-known keystroke-eating defect swallowed the per-square arrow presses a movement helper depends on. Measured at every step: a deliberately broken single wait timed at 3 minutes, extrapolating cleanly to the original ~9-10 minute report at three stalled waits per test. Confirmed gone at HEAD by actually re-running the original trigger (pool genuinely moved, picker genuinely opened, same bed now fails cleanly in 9 seconds) rather than assuming an already-closed fix (`PT-2377`) must have covered it. Amplifier closed: numbered timeout messages, not a timing change — a real flakiness risk on the timing option correctly declined, especially given this exact mechanism was already miscalibrated once before. Building the fix surfaced a second, genuinely more dangerous mechanism: outside `runAsync`, `pumpUntil` doesn't timeout at all — the fake test clock never advances and the wall-clock check is never reached, producing a true, silent hang. Own prior "it was never a hang" claim correctly narrowed — true of the file measured, too strong as a general claim. Confirmed latent, not live, across all 26 real callers. Closed with a synchronous check (real timer type, empirically confirmed rather than trusted from docs) rather than the declined timing change — solved a genuine design paradox first (any await-based detector would inherit the exact bug it existed to catch). Verified both directions in one run: the wrong case fails instantly with a reason, the correct case behaves exactly as before — PT-2414, PT-2415, PT-2416

- The quotation trap closed in all four remaining note-readers, and the rocket burst reading closed all eleven rockets. Both threads carried a genuinely serious, self-caught near-miss: the quotation fix's own first attempt (blanking quoted spans to whitespace) silently deleted a real sentence from a real power's note by opening an unintended gap in an unrelated field's own pattern — the verification built to catch exactly this reported "0 changed," and only a coincidental, unrelated gate block led to the manual diff that actually found it. Redesigned to never alter the text at all (match against the original, skip anything overlapping a quotation), confirmed byte-identical output. The rocket burst's own first attempt had a variable referenced before its real assignment, silently resolving to a stale leftover value rather than crashing — "a name that exists and means something else is worse than one that does not." Both closed properly, both correctly scoped (rockets only for the burst reading, not the broader ~30-item population) — PT-2410

- Rocket/launcher delivery's design work at PT-2408 (the `deploy=thrown` discovery, the launcher gate concept, the 6-of-11 scoping decision) — real and correct in shape, confirmed genuinely working end to end at `PT-2419` after PT-2417 found it had never actually been verified through the real throw path — PT-2408, corrected PT-2417, closed PT-2419

- Seven-item multi-word/multi-condition catalogue gap closed. Concussion Rocket's real root cause wasn't a spelling fault — a correct, deliberate refusal (knockdown's unruled duration) was silencing an unrelated, perfectly-valid sibling condition (Stun) in the same clause, because the reader only captured the first match. Refusal correctly rescoped per condition, not per item; the durationless-knockdown refusal itself correctly preserved, not papered over by inventing a duration. Verified by full corpus diff (exactly seven changed) plus a durable sweep guard for future wording mismatches. Same vocabulary found hand-duplicated in two more places, both correctly widened independently rather than importing the extractor's own map (which would agree by construction and catch nothing) — PT-2407

- `_reach` reads a range as a radius — closed. Root cause: the note *warning* about the defect ("a RANGE, not a radius") contained both the quoted phrase and the negating word close enough to trigger the same detection pattern it was written to warn against. Fixed by stripping quoted spans before matching, verified against all 106 rows (exactly one changed). Shutdown's own reach (10m) stays correctly unmodelled — no power in the corpus carries a range concept at all — PT-2404

- Condition enforcement thread confirmed genuinely complete for current content, honestly re-verified. Twelve of the thirteen remaining conditions carry zero applications anywhere in the shipped corpus; the thirteenth (`entangled`) is already tracked under the grenade thread. Coder's own prior recommendation to resume this thread withdrawn once measured against real data — building the other twelve would be exactly the no-caller defect class this session has spent all week hunting. Correct trigger for the future is authoring, not a scheduled slice; the tracking set already holds what's needed — PT-2404
- Casting-costs-the-Action, Overload, Blinded, Deflection, and Force Camouflage all confirmed in real play — TEST 115. Overload's weapon-disable correctly follows whichever creature is aimed at, not a fixed target. Camouflage's matched-pair design isolated the exact `+4` and expiration timing in one clean setup — PT-2405

- Always-on power mechanism, Deflection-as-reaction, Force Camouflage's duration, the Force Body window contradiction, and its pricing re-examination all closed. Always-on correctly refused — an existing ruling (`POWER-COSTS-01 §4`) had already resolved all eight relevant powers three ways; nothing wants a passive flag. Deflection built as a callback after the roll, before damage (an alternative to being hit, not to being aimed at), paying for the attempt not the success — the detail that keeps it from becoming the refused passive in disguise. Camouflage's two numbered tiers get their ten-round clock; base tier correctly stays inert (its permission is gated on an item category nothing in the catalogue provides yet). Force Body's window corrected to 10 rounds (`§4.3`'s 5 used the wrong, items-specific divisor). Pricing re-examined and found sound: break-even (measured in casts) never moved; what changed is the ceiling, and the ceiling is actually tighter overall because a separately-closed fix (casting costing the Action) bounds it harder than the doubled window loosens it. 15/20/25 confirmed to hold — PT-2402

- Casting costs the Action, the duration field, and the better-power refusal all closed. Action-cost refusal correctly asks `canAct` (one shared question, not a duplicate) — stunned/prone/paralysed all correctly refused by the same line. Four broken tests correctly fixed as fixtures, not the rule; two needing a narrow, visibly-marked test-only exception (`giveBackActionForTest`, zero production callers, guarded by its own test). Duration field's guard nearly gave a false negative twice — the inverse of "a mutation that kills nothing may not have landed," here a mutation with no observable effect may never have applied at all; caught by verifying against git before trusting either result. Better-power refusal reuses `forceBodyPercent` as the single shared definition of "better," pool correctly still spent on a refused cast (matching the source's own removal-before-check sequencing) — PT-2400

- Chain B (overload → shutdown) closed, both pieces. `save_also` authored on Overload's Will save (arrives only as "As Shutdown" in prose, not readable, so the document states it); refuses a duplicate save-type or a save with no primary. Weapon-silencing outcome correctly scoped to only the creature aimed at, not everyone in the broader area sweep. `WeaponInert` built as the first clock whose subject isn't the creature storing it — matches by weapon name so a second weapon keeps working, destroyed is the same record with no clock. Both real strike-callers routed through one shared check, avoiding this corpus's most-counted defect (a fix applied to one call path, not its structural sibling). Two honest self-catches: a second, parallel allow-list (`PowerRecord.appliesNothing`) independently missing the same field `PT-2394` had only fixed once — lesson recorded ("grep for the other field names, not the new one"); the real consumer had no guard at all, replacing its whole body killed nothing across 1,209 tests — the exact `PT-2240` paid no-op shape, caught before shipping — PT-2397

- Force Body confirmed decisively in real play — TEST 114, all three items. Both purses moving matched `ceil(cost × pct/100)` per purse exactly across all three tiers. Replaces-not-stacks confirmed, with a genuinely valuable proactive find: the currently-correct behavior is produced by two mechanisms (app's single-element assignment, Lodestar's smallest-wins fold) that only agree because the list structurally can't hold more than one entry — tracked as its own held item, not urgent. Refusal confirmed with the boundary case named (`<=`, so exactly-sufficient vitality also refuses). Tester honestly corrected their own prior credit, recognizing TEST 113 had only found one of the two real gates — PT-2396

- Paralyze closed — the first condition in this whole thread never inert for even a moment, ruled and enforced the same day. `paralysed` built as the genuine top rung of the ladder (cannot act AND cannot move, both mutants of "give it only one" dying), correctly adding no number for "treated as helpless," correctly refused the stand-up mechanism it structurally can't pay for. Two real blocking checks fired and caught genuine new problems (a duration-scoping bug, an unnamed save type resolved by triangulating from a sibling item plus the source's own Stasis power). `ACTION-ECONOMY-01`'s internal contradiction corrected exactly to scope, the separate item/power pipeline divergence correctly left untouched. A test written long ago specifically to fail on this exact day did precisely that — PT-2393

- `Force Body` ×3 — now fully closed, both purses genuinely move in real play. Engine half at PT-2391; TEST 113 found the app-side wiring never actually connected (reopened, PT-2394); root cause corrected as two independent dead gates, not one — an early-return in `_applyPower` that bailed before reaching Force Body at all, plus the `modifiers`-gated assignment originally named. Seam test casts through the real key path and reads the actual purse; old and new readings disagree in opposite directions on both numbers (20/0 vs 10/10). Split computed before the affordability check. Self-caught contradiction: the fatigue toll would have quietly shrunk alongside the Force cost too, an unruled second benefit — `cast` now takes `spend` separately from `cost`. Refuses rather than kills the caster on an unaffordable vitality half. Clears sibling tiers on cast so a power can't get stronger as an earlier tier's clock expires — PT-2391, PT-2394, PT-2395

- Condition enforcement confirmed decisively in real play — TEST 112, all six enforced conditions (prone, slowed, shaken, held, cowering, plus the fear residual). Stand-up mechanic confirmed working for player and three enemy instances. Matched-pair Force Slow/Mire comparison and a single Horror cast proving both halves of the fear residual are among the most efficient test designs this session has produced. Real finding correctly attributed as not a gap in the condition work: the "Bonus" mechanic is granted by nothing anywhere in the product, making `slowed`'s "Bonus lost" and `cowering`'s "Bonus kept" currently unfalsifiable — held as its own small tracked item, not the condition work's fault — PT-2385
- Area-power anchoring thread fully closed. `anchor` field built (`caster`/`target`), derived from the document's own already-correct prose rather than re-transcribed — 43 powers now carry it, 12 of 13 script-checkable cases independently confirm it. Nine corrections applied, Force Scream inversion resolved exactly as predicted. One real judgment call approved: a SPELLCONE running caster-to-target is the same volume whether described as "in front of the caster" or "toward the chosen point" — kept as caster-anchored for Force Scream's lower tiers and Force Lightning. Another self-caught degenerate guard (a fixture where both old and new centre computations coincidentally agreed) found and fixed — PT-2386

- Target-selection thread fully closed. Single-target powers now aim at an explicitly chosen target (matching the games); area/radius powers correctly unaffected; the why-column shows refusal reasons per candidate, reusing the real `mayTarget`; `Revitalize` correctly selects the nearest fallen ally automatically (no menu — its own prose names the target, keyed on `only_if_fallen` + single-target so it stays correct if a second power shares the shape). Along the way: an invisible defect where the target menu rendered correctly while every keystroke silently re-triggered the same cast; five separate self-caught degenerate guards across this whole thread, none catchable by reading, all found by mutation; a test bed's "Probe Droid" found to be `species: human` the whole time. An unexplained hang in `a_blast_stops_at_a_wall_test` honestly flagged, held as its own future slice — PT-2377, PT-2378, PT-2379, PT-2380

- HANDOFF documents fully reconciled — all 17. `AGENDA-CURRENT`'s last line (a status claim, not content) ruled resolved and synced — a Sep 7 backlog claim, unmentioned through this many subsequent slices, treated as stale rather than left permanently forking the document — PT-2350

- **Condition enforcement thread closed.** Opened as a severe, live defect (1 of 20 conditions enforced, 53 real applications lying to the player) and closed with a structurally-verified honest tally: 6 enforced (`stunned`, `held`, `cowering`, `slowed`, `prone`, `shaken`), 2 comparisons never applicable (`flat-footed`, `helpless`), 12 named-but-awaiting of which 10 are applied by nothing shipped at all. The only two reachable-and-unenforced conditions (`blinded`, `entangled`) are both already correctly held by existing rulings. No unbuilt, reachable, unheld condition remains. Tally derived (not listed) with a test asserting the three sets partition all twenty names. Along the way: caught two of my own contradictory rulings before they shipped a permanent player soft-lock (`prone`'s end-condition mechanism needing the Action that `prone` itself forbade); found and fixed a live "fear that misses still lands" defect against the document's own categorical rule; found the shelf's own data was wrong about `helpless` (a comparison, not a state, correctly remapped to `stunned` via the power's own comparison to Force Stasis); found and resolved a major live collision where `Force Slow` would have started halving movement today, something its prose never states — PT-2367, PT-2368, PT-2369, PT-2370

- The "paid no-op" defect closed on `force_confusion`/`beast_confusion` and eight siblings (10 total, not the 2 originally named). Checking each power's prose first confirmed both are faction changes, already held as separate future work — no condition invented to paper over an out-of-scope effect. Fixed the real problem instead: a player was charged real Force points and told the opposite of the truth for a cast that does nothing regardless of the save's outcome, `PT-2240`'s already-named anti-pattern. Refusal predicate structurally tied to the same carrier list `_applyPower` reads. Separately found 35 of 106 powers apply nothing at all and correctly not refused — a power that never asked for a save and does nothing is a known silent state, not a lie; the 35 asserted in the guard so it can't drift unnoticed — PT-2364

- Force Push's real, ruled pieces all closed. Made-save contradiction fixed (source's own reason on the row). Droid stun suppression built as a new `condition_excludes` mechanism — the existing `excludes` would have deleted the whole power for a droid, the opposite of the source's behavior; correctly evaluated before the save so the exclusion doesn't depend on a die. Forced movement built, deliberately not wired to opportunity attacks (that system has no concept of involuntary movement). Blocked-path outcome distinguishable from never-pushed. Diagonal direction: real precedent found (`bearingBetween`) argues against snapping to eight-way facing at all — corrects the direction originally suggested. Tie-break exhaustively checked across all 360 integer bearings, confirmed mathematically unreachable rather than merely untested. Size gate stays held — PT-2362

- Item gates and BonusFeats both confirmed in real play — TEST 111. Constitution gate and feat-chain gate (all three tiers as OR) both confirmed cleanly. Circular-dependency negative confirmed decisively via one call to production `wornBy` printing both the grant set and refusal list simultaneously, on real shipped data (the exact droid-tool/41-device case) deliberately placed first in the map. Own note: the original routing's "read the sheet" instruction was wrong — the sheet structurally can't show either bonus (ability display reads the raw record, Reflex calculation takes no save-bonus parameter). Tester built a separate bench with identical dice across runs to confirm both bonuses land, recovering from the routing gap rather than just reporting it — PT-2361

- BonusFeats thread fully closed. Grant channel accounts for every real property exactly (73 + 29 + 9 = 111, zero silently dropped). Permanent/granted split proven load-bearing by mutation (merging the sets fails the suite) — `_featsInPlay` derives from the already-recomputing `_armour` pattern, no new `Combatant` field, unequip-doesn't-survive holds by construction. Genuinely valuable self-caught false positive: the first circular-case guard passed for the wrong reason (the gate call had no vocabulary, so every gate was declared rather than evaluated) — caught only by a separate "the granter really does grant it" instrumentation line Coder almost didn't write. Same shape as the ledger-refresh and slot-check catches — the instrument is the difference, not the mutations, recurring as a standing pattern in this codebase's testing culture. Correct and fully wired end to end, but currently unexercised by any real shipped content — same honest framing as the implant gates finding — PT-2356

- Uncanny Dodge's grenade-DC reduction confirmed in real play — TEST 110 confirms decisively. Own note: the original routing mis-stated the numbers (said both tiers state "-2"; the real shipped data is -2 and -4) — Tester caught it by measuring against the real data rather than trusting the brief, correctly distinguished a mis-stated routing from an actual defect, and confirmed the build is correct throughout. Combined-tiers case reads identically to tier-2-alone, demonstrating replacement rather than merely inferring it from a matching number. Character blueprints confirmed to parse no `feats` key at all — the player-only scope is correct, not a gap — PT-2351
- HANDOFF docs mirror fully closed — the check that has warned since it was written passes for the first time, gate dropped from 8 to 7 warnings. `RUNNING-ON-THIS-MACHINE.md` (no corpus source) correctly reported as a fact, not a fault, left untouched — PT-2351

- HANDOFF documents mostly reconciled — 15 of 17 closed at the time. Major find: the existing classifier itself was wrong, not the content — its heuristic assumed all mirror/source divergence is additive, misreading 5 of 6 real "forks" as such when they were ordinary, unsynced edits (one document's own text literally said "THIS SECTION WAS STALE" while held back as a supposed fork). Rebuilt to ask the structural question (a block with no counterpart at all, via real diff-based deletion detection) — immediately found a genuine fork a careful manual read had missed. Two real forks confirmed: `PREGENS-01` (2 lines, safe to sync — a warning about something already removed) and `AGENDA-CURRENT` (8 lines, 7 already incorporated elsewhere in the corpus under different names, the 8th a status claim resolved and synced at `PT-2350`) — PT-2349
- Uncanny Dodge's grenade-DC reduction closed. Tiers correctly replace, not stack — not an edge case, every Scout past level 7 holds both by the feat's own progression, so summing would have shipped `-6` as the default late-game experience rather than `-4`. Eight sentences found printing the stated DC while rolling against the lowered one, `PT-1326`'s own principle — fixed to print what was actually rolled against — PT-2349
- Mine-tag-as-log-index closed. Traced to `§2`'s own already-stated principle — the count of folded rows and a log index are both forbidden ordinals regardless of which was in use. Tag now derives from the board square, unique by construction since the set-mine path already refuses an occupied one — PT-2349

- Archives/history-rewrite question closed — no rewrite. The genuinely safe cleanup already happened (91 duplicates at `PT-2326`); the remaining 151 are the only tracked copy of themselves, contributing real, necessary size rather than clutter, and the rewrite cost (dozens of dangling commit-SHA citations, every agent needing a full re-clone) was already measured disproportionate. Not revisited again absent a real, new reason — PT-2348

- Item gates thread's slot-vocabulary and check closed. Real authority found (`PT-1252`'s locked slot lattice) rather than guessed from visible keys — confirmed 4 of 5 rows already right, only the forearm row wrong. A deeper problem surfaced and split rather than patched: one column had been serving as both a real key and a display label, agreeing only by coincidence while every slot happened to be one word — `Worn at` split out as its own column, display tree confirmed byte-identical afterward. Third independent surfacing of "a display name is not a key" this thread (after `replaces`, `requires_feat`). Check rebuilt as membership against the real list; census (not mutations) is what proved it correct this time too — 7 correctly-slotted items confirmed clean, 2 genuine faults found in Tester's own packages, correctly untouched. A prior slice's own overstated claim (the "mask" evidence) found wrong and honestly corrected — PT-2347

- Two-handed Strength multiplier fix confirmed in real play — TEST 109 confirms decisively. Correctly identified the mechanically two-handed weapon first (single-attack, empty off-hand — the vibrosword, not the double-blade its name suggests). Key control: recognized the negative-modifier reading alone would pass identically even with the entire multiplier mechanism deleted (`lifts` requires positive modifier too), and closed that gap with a second run on the identical setup using a positive Strength score, confirming the same weapon does correctly lift when given a positive input. Confirmed on both the player's own attack and a placed creature's — PT-2345

- Stacking-penalty fix confirmed in real play — TEST 108 confirms decisively across two carriers (`abilityPenalties`: three Master Force Screams, pool draining three times, Strength term never moving off single-cast value; `modifiers`: Force Slow twice). Additional proof: different powers (Scream + Choke) still correctly stack, on genuinely independent clocks — confirms the fix scopes to same-power recast specifically, not any two simultaneous penalties. Dark Healing's ruled behavior change (replaces, not compounds) confirmed. Lower-tier-as-no-op, Crush Opposition, and Battle Meditation remain untested, honestly disclosed rather than assumed — PT-2343

- Two-handed Strength multiplier closed — bonus only, not penalty, matching `§7.4`. Original `−1` pin survived three rulings because truncation, round-down-with-multiplication, and bonus-only all independently produce `−1` at that specific modifier — third instance this session of a fixture sitting at a value where the fault it's meant to catch can't occur. Moved to `−3`, where all three readings diverge — PT-2339
- `sync_docs.py` deleted — a real, live hazard (could destroy 937 lines of forked content on one accidental run) removed, with the docstring that had falsely claimed it was already gone corrected alongside it — PT-2339

- The stacking-penalty fix (`PT-2336`) closed. `abilityPenalties` reuses the buff side's own `superseded`/`outranks` rather than a second implementation. Document expanded from 4 to 20 powers authoring `replaces`; the extractor now refuses any future power that stays silent while granting a standing row over a lower tier that also grants one — closes the class, not just the sixteen instances found. Confirmed correct and intentional: Dark Healing's self-cost now replaces rather than compounds on recast, the one case in the affected population where the principle favours the caster — followed exactly as ruled, not carved out — PT-2337

- Grenade `damage` schema, `takes` optional-key mechanism, PT-2322's mine regression, `on_save` guard, `ability_penalty` validation, and the "eighteen"→relation fix all closed. Most severe: `PT-2322`'s own earlier deploy-required fix had broken every existing mine in the product, undetected by 1,128 green tests because the one test built specifically to catch this had frozen around a hard-coded schema snapshot the day it was written, under a comment claiming it read "the real charge row." Fixed at both levels — the regression (hazards supply their own known deployment rather than being asked to author it) and the reason it went undetected (the check now reads the live installed row). `on_save`'s `==` chain had no final else — an unrecognized value like "halved" would silently deal full damage; refused now, and moved to check before the save rolls rather than nested inside `if (made)` — PT-2336
- Stale HANDOFF rulings document corrected — 785 rulings, 14,331 lines behind, confirmed stale not forked before copying. A written-once, already-false count in the README replaced with a live relation to the actual check — PT-2336

- Species adjustments (Int/Cha for placed creatures) closed. Guard asserts "whatever the parser finds arrives," not a fixed list of six — forward-compatible by construction, confirmed it would have caught `PT-2163`'s original Wisdom gap had it existed then — PT-2335

- Both severe PT-2330 defects closed. Evasion now correctly applies to Force powers (shared decision logic `evasionApplies`, arithmetic left duplicated on purpose — the risk was never in the trivial halving, only in which save-kinds the feat answers to). Massive Critical damage now correctly matches displayed and dealt — the `tdd` skill's own principle (test at the observable public boundary, not a re-derived arithmetic) moved the guard from the engine to the app's `strike`, which is what exposed that the original diagnosis was wrong in one detail: the old guard wasn't inert, it correctly reached the branch and asserted a true total, it simply never checked that the parts summed to it. `DamageOutcome`'s own documented invariant had never actually been checked by anything — PT-2334

- `abilityModifier` fix confirmed in real play — TEST 107 confirms Force pool, the sheet-vs-fight agreement, and skill budget cleanly. Reaction pool correctly reported as unobservable at this specific site (every bug-affected value lands below the first banding threshold regardless of derivation) rather than falsely ticked off as a pass — PT-2333

- Codebase cleanup — Finding 2 (`Timed<T>` interface + the `standing` rename) closed. Six lists (`AbilityPenalty`, `AbilityBonus`, `Condition`, `Immunity`, `SkillBonus`, `CombatModifier`) consolidated to one `endRound` case. Genuine coverage gap found and closed along the way: deleting the immunities tick entirely left all 1,373 tests passing — nothing anywhere verified any of the six lists ever ran down. `ShieldUp.standing`→`holding`, `Encounter.standing`→`upright`; the compiler caught three callers a text-based sweep missed. `PT-2325` fully closed except the held archives question (151 loose files, only tracked copy of themselves — not urgent) — PT-2332

- Grenades — fully closed, whole thread. Found and fixed the missing instantaneous `damage` EffectKind (blocked 35 items across mines/grenades/droid devices, not just grenades). Mines built first as the urgent, already-reachable half — activated `Evasion` and `Uncanny Dodge`'s grenade-DC reduction for the first time ever via the existing pipeline needing zero new branches. `§53` corrected against the real source (no attack roll exists anywhere in either script; area + independent saves only), Action-cost economy correctly left alone as the one part that was right. Shield per-part resolution fixed along the way (mixed disruptor blows). `entangle` built, `deploy` field built closing an already-live defect ("Set mine" accepting a grenade), with a self-caught 105-vs-11 overcount corrected to the precise signal. Throw verb (`o`) built — routes through `effectFromShelf`, refuses the other three charge-verbs out loud. Friendly fire confirmed to include the thrower themselves, correctly recognized as faithful behavior and deliberately asserted with a purpose-built low-damage fixture, not patched around. Three separate unfalsifiable-guard self-catches across the thread, the last one from a test whose assertion target never actually depended on what it claimed to verify. Ion's flat +30, Plasma's fear effect, and the Thermal Detonator's push all built; its knockdown correctly stays refused (no duration to invent). Shield Disrupter resolved for free. A genuine source bug (Poison Grenade's early-loop-termination) ruled not to port. A latent naming split corrected proactively — PT-2317–2324

- Shields — fully closed, mechanism and activation, one of this session's largest threads. Real mechanism: full absorption of flagged damage kinds until a depleting pool empties (not flat resistance), built from `forceshields.2da` as a real extract joined by the game's own exact chain (item→spell→if-chain→row, 31 of 31 items). Owner's own opening two-type framing corrected (droid/organic share one mechanism). K1's entire 15-item half recovered from a single label mismatch. Ion built as a per-shield hole overriding the general stop-list (resolves the table's own internal self-contradiction), applied against the summed total so mixed damage passes through whole rather than half-stopping. K1/K2 divergences (pools, kinds, a Repair-skill-scaling duration term K1 lacks entirely) built faithfully as real, separate facts about each game. Activation built as a proper Gear verb (`g`), gated correctly against `§3`'s own stated design intent, `shield-generator` populated via the same join after an incorrect first ruling collided with an existing check and was caught by actually building it. One true exception (the Baragwin Droid Shield, the only item whose description states nothing at all) correctly distinguished from "unparseable" and ruled narrowly, with a new permanent gate check holding both the general rule and the exception together. Real remaining gaps stayed correctly deferred: Shield Disrupter, `PT-2106`'s revisit (blocked on nothing to test against until now-closed), whether pool or duration clock binds first in practice — PT-2307–2316

- Mind Trick/Breath Control duration-conversion contradiction — closed, was never real. `PT-24` (powers, ÷3) and `PT-1859` (items, 6s/round) are correctly scoped and don't conflict — a stale, never-built-from table in `PT-255` predates both and was mistaken for a live source. Verified 8 of 8 comparable powers agree with what actually ships, not the old table. `PT-255` withdrawn in place. Separately: `PT-24`'s own citation had gone missing from `FORCE-POWERS-01` (pre-dating this session, confirmed via git), restored — PT-2306

- Force Camouflage/Force Valor stacking defect — closed, both chains. Camouflage ruled to replace (evidence from a shared `forcepriority` column identical across both a chain that states replacement and one that doesn't). Checking the fix surfaced Valor had the identical defect against its own explicit prose since the chain was written — a real, live balance defect (Strength 10→12→15, measured), fixed as implementing an already-stated rule rather than new scope. Mechanism: a `replaces:` Notes-cell aside, id-matched, refuses loudly on an unrecognized id, one shared predicate across all three timed-carrier lists, matched bidirectionally so the bug can't return via reversed cast order. Beast Confusion/Beast Trick authored, mirroring their generics exactly. `Force Confusion`'s "one at a time" corrected to per-kind. `Mind Trick`'s paired beast-and-droid exclusion corrected, ordered ahead of the narrower droid-only pattern. Costs left exactly as ruled, no prior context found. 104→106 powers, five censuses moved with reasons recorded — PT-2305

- ~380-cell (212 real) item-description truncation sweep — closed. Every verification clean: zero mid-phrase endings, zero unbalanced markup, zero unintended non-text changes across 1,424 items, idempotent. Two real detection-rule refinements caught by the dry run before writing (lowercase-continuation signature instead of "no terminal punctuation"; source newlines respected as sentence boundaries). Third instance in one slice of a hard-coded scan-limit producing a confident wrong conclusion (`range(0, 80000)` against a 136,551-entry table) — named as its own standing lesson given the count. 13 genuinely-shorter-but-cleanly-ended cells correctly left alone (editorial trim, not truncation), kept visible every run — PT-2303

- Mid-word `Charges:` truncations — closed, confirmed already fixed by an earlier repair pass. All 88 cells complete. A near-false-finding caught before filing: first pass reported all 88 still truncated, traced to the investigator's own 95-character display window rather than the actual data — PT-2301

- Revitalize's standing-ally feedback — closed. Built with the ruled sentence, "nobody here has fallen." Fixture `PT-2290` called unbuildable now exists, built directly from `PT-2297`'s explanation — closes the last open piece of the healing-family thread — PT-2299

- Force damage ability-modifier question — closed, no. No Force damage power in either game carries an ability-modifier term; `Heal`'s formula is the only instance, not a general pattern. Build already agrees row for row. Near-miss correctly avoided — an expression that looked like a dropped multiplier term traced to its assignments and confirmed always statically 1 (same shape as the Revitalize percentage mechanic) — PT-2296

- Healing-family defect fully verified in play — TEST 101 closes both remaining gaps. Revitalize confirmed reviving a genuinely fallen ally (achieved through real combat, all three tiers) with the exact pre-fix failure scenario re-run and confirmed inert — caster inside their own blast radius untouched, standing ally shows no damage/heal, and an independent pre-cast frame confirms the heal is a real clean upward move with no intermediate dip. Battle Meditation's caster-centring confirmed with a single geometric setup distinguishing all three possible centrings at once — PT-2293

- ⚠⚠⚠ Healing family net-harmful defect — closed, two-line fix. Damage-nulling widened to cover `ally` alongside `self`; the separate `!healsSelf` early-return guard also updated (the naive one-line fix alone would have caused a second, different regression — all six ally-healing powers returning before their heal ever ran). `heals: caster` correctly left untouched (Drain Life/Death Field's damage-then-caster-heal is its whole meaning). Reproduced Tester's exact live finding as a mutation, then killed it. Third verification clause (a genuine revive) honestly reported as unbuildable in the current fixture system — asking Tester to close it using their existing live scenario — PT-2291

- Force Scream family's flat-dice fix — confirmed in real play by TEST 100. All three tiers genuinely distinct, matching documented figures; ability-penalty figures independently confirmed as a second discriminator. Droid exclusion still holds through all three casts — PT-2290
- Two pre-existing test-infrastructure defects (`acceptance_test`, `whole_loop_test`) — closed. Both were reading a result after a fixed settle/poll rather than waiting for the actual write, the "read side" of duration-waiting that an earlier pass had converted the tap side of but not this. First fix attempt (poll for quiescence) shared the absence-versus-error shape already named this session and was corrected — waits for a change against a self-captured length instead. 0/3 → 3/3 — PT-2290

- Force Scream family's flat-dice defect — closed, urgent. All three tiers were rolling 20d6 instead of 3d6/5d6/7d6 — `count` never read on any path, invisible because it only differs from the level-derived fallback on these three rows in the whole corpus. Verified on a real board (80 vs 10 damage against a Soldier 3), fixed, guarded at Force levels 1/20/30 with a control confirming genuinely level-scaling powers still scale. `PT-2257`'s ruling confirmed unaffected — it was always about the document's stated figures, which the engine now actually delivers. Any existing play impression of this family predates the fix and should be treated as untested — PT-2282

- `Force Valor` chain — closed, functional end to end. `AbilityBonus` built as a genuine parallel type to `ability_penalties` (not a signed reuse), the fifth clock ticking on the same round-boundary walk as `Condition`/`AbilityPenalty`/`AbilityDamage`/`CombatModifier`. Saving-throw half via existing `Modifies.saves`. Four mutations killed, including one that caught the first guard testing only the type's static shape and not its actual round-tick — PT-2281

- Cast target selection, ally-targeting piece — closed. `_cast` gains a real third arm, ally powers route correctly out of combat and in. 16 area/1 single geometry work closed alongside (preposition fix, 13-power authored default, base `Heal` singled out and marked authored). All verified in code with a positive-signal guard — PT-2277–2278

- Shared-shelf race with Tester — closed. `sandboxed()` copies the shelf once per isolate rather than symlinking the live one, removing the race's timing window entirely rather than trying to catch it. Own guard caught a real bug in the fix itself (the snapshot was being swept by the same cleanup as disposable temp dirs). Three full runs since, zero failures; `/tmp` leak improved further as a side effect (28→6 per run) — PT-2273

- `Force Incinerate` — closed, maps to `held`, built (4 rounds, Fortitude) — PT-2272
- `/tmp` leak — closed. Static analysis (17 suspects) trusted less than real measurement (28 directories, 8 prefixes) — ten cleanups across five files, whole run leak 28→2. Remaining 2 confirmed as the documented, self-limiting setUpAll fallback, not a new defect — PT-2272

- 11 K2-only droid-exclusion gaps — closed (Force Scream ×3, Drain Force ×3, Crush Opposition ×5), extending `PT-2184`'s standing precedent. Corpus-wide exclusions 30→41 — PT-2269
- Area-targeting kind-exclusion defect (pre-existing, self-found while building the above) — closed. `mayTarget` ran once, on the primary target only; everyone else caught in a blast was filtered on membership/distance/sight but never on kind. Death Field had been draining nearby droids since the area path was built. Fixed at the root — `_reachedBy` now asks the same `mayTarget` gate for every creature in an area, with `relation: null` deliberately preserved so ally/enemy status (an already-answered question) doesn't get silently re-asked here — PT-2269

- Force Root's swap — closed, confirmed in play by TEST 098. `held` built as the twentieth condition, Root's row read directly, the underlying inheritance defect fixed at its root (row-says-nothing vs row-says-something-unparseable now correctly distinguished, stops the build loudly instead of silently inheriting). Confirmed in play: cast on a target already carrying Mire's own attack penalty, nothing added — replace, not extend, proven rather than assumed. `−6` Defence/Reflex confirmed unchanged — PT-2264–2267

(Struck items move here with the PT number that closed them, for reference — not a full history, just enough to answer "wasn't that already done?")

- Weapon's-own-stats reaching a strike, all six properties (damage, keen, attack, massive, enhancement, armour-scoped-out) — PT-2194–2209
- Upgrade-fitting / worn-ability mechanism, full thread — PT-2201–2205
- `_cast` dispatch (conditions, timed penalties, area, damage) — PT-2196, 2212, 2218–2219
- DC fix (Force levels not character levels) — PT-2216–2217
- Force pool `forceLevels` stub fix — PT-2215
- Character leveling spine (`character.levelled`) — PT-2226
- `walk_into_it` real cause and fix — PT-2226
- `save_kind` gap (14→51 powers) and `heals: caster` — PT-2226
- Combat modifier table + `affects` fill (99/104) — PT-2229–2230
- Custom weapon shape (parallel path, not a fourth rung) — PT-2210, 2211, 2225
- §7.4 two-handed Strength wielding choice — PT-2221–2223
- Long Sword base die (1d6→1d8) — PT-2198
- Battleaxe base type (renamed, die settled at 1d12) — PT-2200, 2227–2228
- Force Pike (item-level override, both items kept) — PT-2200, 2206
- Force Kill / Force Push on-save fixes — PT-2219–2220
- Stunned-refusal wording + round-count guard — PT-2219–2220
- `DecreaseAbilityScore` — PT-2229 area
- Force Plague's `save_kind` — a ruling (PT-2189, via Force Affliction one tier down) that never reached the prose it governed; the reader was correct, the document was a sentence short. Written and verified, 51→53 — PT-2232
- Force Affliction / Contagion / Plague — the disease tree, closed. Magnitude built as its own table (7/4/12 off each physical attribute), kept separate from the timed-penalty table since the two mechanisms genuinely differ — one ticks down and expires, one accumulates and heals a point per day of rest, and a null roundsLeft on the wrong table would have silently carried a third, different meaning under PT-2170 — PT-2253
- `audit_rulings.py` coverage gap — closed. Now scans all repos (Lodestar, app, Loom, Lens) alongside MAIN_WORK, distinguishing a ghost citation in a document from one in a comment. This is the check that would have caught the earlier PT-2245 mint — PT-2256
- Load Game dialog overflow — closed. Save list is Flexible + scrollable under a ceiling; Back stays outside the scrolling region on purpose (a dialog you can't leave is worse than one you can't read). Self-caught an unfalsifiable line in the first version of the fix, found only by mutating it — PT-2256
- Force Scream family's balance — ruled, no reduction. All three tiers negate the ability penalty on a made save (not just halve it — an earlier report had this wrong, corrected). Found along the way: the engine's hard-coded negate-always rule was correct for every row in the corpus purely by coincidence, since the document had no way to represent a contradicting case — real column built to fix that, behaviour unchanged. Screams stay at source figures; Force Choke's own reduction was narrowly anchored to its specific stun mechanic, not a general precedent — PT-2257
- CryoBan Grenade and five siblings — extractor defect closed. A line-wise reader dropped everything after any label whose value sat on the line below it rather than the same line; 21 fields restored across six items. First explanatory theory (renderer dropped unmappable effects) was wrong and honestly corrected — the real discriminator was layout, not content. Item-effect refusals went from six unnamed shapes to eleven precisely named ones — PT-2259
- Six miscategorised upgrade items — closed. Three independent signals confirmed the fix, including a document header that had been wrong by exactly six since PT-781. Orphaned components 27→21, the remaining 21 exactly matching PT-2184's two held groups — PT-2259
- Force Affliction's save-outcome — closed, ruled. Contagion and Plague ruled: a made Fortitude save stops the ability damage entirely, confirming existing shipped behavior as deliberate rather than changing it — matches Affliction's own explicit precedent and standard genre convention for poison-flavored effects. `bool? negatedBySave` holds "the document declines to say" as its own real state. Cross-table absence-semantics divergence (penalty table: absence lands; disease table: absence negates) confirmed as correct and deliberate, to be documented loudly in both places — PT-2259
- Force Mire / Force Root (Chain A) — built. Replace, not extend; resolved transitively at extract time, ordered by chain depth, inheriting per-row-by-quantity rather than wholesale — PT-2254–2255
- "Armour blocks Force powers" — built. Engine-side, like BlindSpot; only its exception is written down, on six items sitting on one base type (zeison-sha) that EQUIPMENT-01 §5.1 already names as an armour anomaly — the anomaly and the restriction turned out to be the same fact. Applies to `armortype = armor`. A real collision caught during the build: the item catalogue's own separate light/medium/heavy grading would have made Zeison Sha "medium," inverting the exact armour the source exempts into the one that blocks — resolved by grading the base type instead, on its own real baseac. Field is `restricts_force`, absence is the safe default, gate fires before target-eligibility, cost, and pool — PT-2254–2255
- XP-award event kind (`character.xp-awarded`, permanent, wired to `awardsFor` at fight end) — EXPERIENCE-01 §2's formula confirmed built on the wrong table, superseded in favor of K2's real `xptable.2da`, confirmed via Neverwinter Nights cross-check; `.crtr` gained an authored CR field — PT-2233–2234
- Targeting gate, friend/enemy half — closed set (sentient/beast/droid) declared and validated at the document; gate verdicts 16→44 of 104. Dark Healing can be cast for the first time — its `targets: self` was an aim sitting in the kind column, the exact cause of TEST 095's original refusal — PT-2235, 2237
- TEST 096's three severe XP-system defects — weapon kills paying zero XP (fixed by recording a creature's worth once at board-join, not derived from board state that changes on death), a level not landing until reload (level and XP now come from one place, tested against `replay`), and real save corruption (confirmed by byte-level arithmetic — the readable half was the older half, silent loss of the most recent progress; fixed with a per-path write queue plus rename-onto-save) — PT-2240
- Dark Healing's aiming half, and the much larger finding underneath it: the aim-checking layer had never once run for any placement-targeted power in this product's history, because `kindOf` reads a species field no blueprint carries. Dark Healing is the first power whose aim has ever been real; the other 25 `affects: self` powers confirmed genuinely unaffected — PT-2241
- PT-1518's save-listing gap — resolved with `format = 3`, a payload length recorded in the header at write time, catching corruption in both directions for one free integer comparison at read time. No listing-code change needed; formats 1 and 2 keep loading — PT-2241
- Out-of-combat casting — self-aimed casts land on `_me`, never entering the fight-shaped half of `_applyPower`; `PT-1845` unchanged — PT-2244
- Penalty table (Dark Healing's two-magnitude row) — and a major discovery underneath it: Force Choke and all three Force Screams had been completely inert since PT-2196, the reader looking one structural level above where the data actually lived — PT-2244
- Battle Meditation two-sided modifier — `affects` now per-row within the modifier table, resolving Improved/Master Battle Meditation's two-sided shape — PT-2244
- Save queue key-collision (self-found) and `/tmp` disk-leak (self-found, serious — three days, 3.2GB, disk down to 45MB free, both write-side and crash-recovery cleanup added) — PT-2244
- `_myDefence` missing the armour term — both bonus and cap now read from one source; two fixture faults found along the way, both caught by a control asserting a term's presence rather than a number — PT-2251
- Massive Criticals / item-count / base-type-count document errors — the `2d10` contradiction resolved: the cap is a live conversion implementing "2d8→2d6" specifically, not a general ceiling; real, retail `2d10` items pass through untouched, as they always correctly have — PT-2252
- EQUIPMENT-01 §16, "Creating Your Own Items" — the custom weapon shape from PT-2210/2211 given its own real chapter — PT-2250
- A real perception system, fully closed, all three pieces — two senses, four transitions (including `Inaudible`), the agitated bit, pursue-to-last-known-square, and facing/BlindSpot as a computed bearing rather than compass-based. Headline fix: `_closeOn` was reading a target's true board position regardless of perception — an enemy that couldn't see the player still walked to their exact square, fixed so destination now reads the observer's own knowledge — PT-2246–2248
