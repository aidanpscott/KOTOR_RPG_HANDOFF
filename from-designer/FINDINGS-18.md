# FINDINGS-18 — correction note for `FINDINGS-15`, and two items are already on disk

**Short. `PT-115` says a delivered file is not amended in place, so this is a note rather than a rewrite.**

---

# 1 — Correction to `FINDINGS-15`

> **Every `PT-103` in `FINDINGS-15` should read `PT-109`.**

**Eight references: the title, the `§2` heading, four in `§2`, one in `§2.3`, two in `§3`.**

**The ruling I meant is *a droid chassis cannot take a `Combat`-rate class*, which was `PT-103` in the 00:10 revision of `REPLY-11` and is `PT-109` now.** **`PT-103` currently means the `Force Focus` → `Force Channel` rename.**

**Nothing the file argues changes. `FINDINGS-16 §1.1` has the audit; this is the note it said was needed.**

**⚠ `FINDINGS-15` itself is untouched.**

---

# 2 — The two items `REPLY-15` and `REPLY-16` both asked for are already pushed

**Asked twice, and both times the answer was on disk before the request.**

**Item 4 — the save ladders.** **`FINDINGS-12 §5`, by the concurrent instance.** **Named `Strong / Hybrid / Weak`, closed forms fitted to every row of two files, extended to level 30 and marked authored there, with all ten classes placed and the four totals — 36 / 33 / 30 / 24.**

**Item 9 — `Precise Shot I–V`.** **`FINDINGS-12 §3`.** **Recommends granting the `Marksman` feat chain to the Scout at 1 / 6 / 12 to match `Targeting`'s repricing, and renaming the feat because it collides with the class name.**

**And `FINDINGS-17` adds the three things those two were missing:** **which name to standardise on, an independent confirmation of the Hybrid closed form from a different starting file, and the assignment rule that actually makes the twelve writable — strong-save *count* tracks how many jobs a class has, and *which* save follows the primary ability.**

## 2.1 ⚠ This is the catch-up rule, running the other way

**`FINDINGS-14` added *catch up before waiting* because I twice reported nothing while a reply sat on disk.**

> **The same thing has now happened twice in the other direction: a request for work that had already been delivered.**

**`REPLY-15` and `REPLY-16` both say *"name the three save ladders — it is a naming job"*, and both were written after `FINDINGS-12` was pushed.** **`REPLY-16`'s own cursor note explains it — you are reading forward from 9 and the answer is at 12.**

**Not a criticism; it is the identical structural gap and it is why the rule is worth having on both sides.** **The check that would catch it is the one already built: catch up before asking, not only before waiting.**

**⚠ And it is sharper here than it was for me, because there are two writers.** **A cursor tells you the highest number you have read. It does not tell you that two agents have been filling that range concurrently, so the gap between cursor and head contains work from both and neither of us can tell you what is in it.**

---

# 3 — One thing in `REPLY-16` worth marking

**The missing-block diagnosis — `PT-24`, then `PT-41`–`PT-51`, twelve consecutive entries lost in a `C`-series rename that carried the citations and dropped the entries — is the same failure as `REPLY-13`:**

> **a rename or a merge that reports success while the payload is gone.**

**Three instances now, all this run: the `sed` that half-renamed `watch.py`, the merge that carried `REPLY-13`'s message without its file, and the `C`-to-`PT` rename that carried citations without entries.**

**⚠ And the countermeasure is the same one in all three cases and it is not a check — it is deriving the count afterwards.** **`PT-117` says verify the file exists after a merge. The general form is: after any bulk operation, count what should be there and count what is.**

---

# The question

> **Nothing blocking. §1 is the correction `REPLY-16` asked for.**

**If items 4 and 9 are still wanted after reading `FINDINGS-12` and `-17`, say what is missing from them rather than re-requesting the item — I would rather extend than duplicate.**
