# FINDINGS-50 — the collision resolved, and `FORMS-01` had already ruled it against itself

**Owner: the **form** is `Force Focus`. The **feat chain** is `Force Channel`.**

---

# 1 — ⚠ `FORMS-01` states the governing rule and then breaks it one line later

**Derived, `FORMS-01 §6.2`, two lines eight rows apart:**

    line 189   | FORM_FORCE_I_FOCUS | Force Channel — the docx renames it Force
               Channel; THE 2DA LABEL GOVERNS HERE |

    line 196   | Force Channel | FP regeneration +50% out of combat; Force power
               damage +3; Saves vs Force +2 |

> **⚠ It declares that the 2DA label governs, and then prints the docx name in the naming table and again in the effects table.**

**The 2DA label is `FORM_FORCE_I_FOCUS`. Under `FORMS-01`'s own stated rule the form is **`Force Focus`**.**

**The owner's ruling is not an override. It is `FORMS-01 §6.2` enforced against itself.**

---

# 2 — And the chain has the better claim to the other name

**⚠ Both objects carry `FOCUS` in their source labels, so *the 2DA label governs* cannot separate them on its own.**

    the form         FORM_FORCE_I_FOCUS
    the class chain  FORCE_FOCUS · FORCE_FOCUS_ADVANCED · FORCE_FOCUS_MASTERY
                     jcn_granted at 1 / 6 / 12

**What separates them is that the chain already has a `Force Channel` family and the form does not.**

**`FEATS-LIBRARY-01` carries `Force Channel (Alter)` and `Force Channel (Control)` — the cut rows `XXXX_FORCE_FOCUS_ALTER` and `XXXX_FORCE_FOCUS_CONTROL`, filed under *Restricted — owner unassigned*.**

> **⚠ Two cut siblings of the Consular's chain were already named `Force Channel` before `PT-103` renamed the chain itself.** **The name belongs to that family and `PT-103` was completing it rather than inventing it.**

**So: the form takes the source label it was told to take, and the chain keeps the name three of its five rows already carry.**

---

# 3 — What needs changing, and where

| Document | Change |
|---|---|
| **`FORMS-01 §6.2`** | `Force Channel` → **`Force Focus`**, twice — the naming table at line 189 and the effects table at line 196 |
| **`FORCE-POOL-01-v3`** | ⚠ cites `FORMS-01 §7.1` three times. **Check whether any citation names the form** |
| **`FEATS-LIBRARY-01`** | no change. `Force Channel`, `(Alter)` and `(Control)` all stay |
| **`CHAPTER-CLASSES` / Consular record** | no change |

**⚠ `§7.2` of `FORMS-01` is the one to check rather than assume** — it is titled *"Two forms reduce degradation"* and if `Force Focus` is one of the two it is named there as well.

---

# 4 — ⚠ Still outstanding from `FINDINGS-49 §1`

**`FINDINGS-32`'s acquisition rules remain wrong against `FORMS-01` and this ruling does not touch them:**

**No feat cost** — `FORMS-01 §4` settles *"no cost, no duration"*, and I invented a one-feat price.
**Two groups, not one** — every Force character holds a lightsaber form **and** a Force form simultaneously. My grants gave one.
**The Battlemaster's tier 1** is worth an action once per encounter, not the free switch I priced.

**And `FINDINGS-49 §3`'s open question stands: `Jedi Sage` and `Sith Sorcerer` were not named in the prestige-entry ruling. They are the caster pair, so a **Force** form by symmetry — but that is my inference, not the owner's words.**

---

# The question

> **⚠ `§4` — the acquisition rework. Mine, or does it belong with whoever holds `FORMS-01`?**

**The rename itself is settled and `§3` lists every place it touches.**
