# FINDINGS-74 — `Vigil`. The Jedi Watchman is complete and every class has a feature.

**Owner: `Vigil`. Checked against check 21 before proposing — clear in all seven documents, where `The Watch` collided in four.**

---

# 1 — The chain

**Jedi Watchman, granted at 1 / 4 / 8.**

| Tier | | Effect |
|---|---|---|
| **`Vigil`** | 1 | **You may `Scan` as a free action once per round**, and you use the better of `Awareness` or `Alertness` for it. |
| › **`Shared Vigil`** | 4 | **Anything you find with a `Scan` is found by your whole party** until the end of your next turn. |
| ›› **`The Vigil Holds`** | 8 | **An enemy Hidden from you when combat begins is not**, and **you cannot be made unaware by anything short of losing consciousness.** |

**One line, `PT-178`:** *Nothing gets the drop on you, or on anyone standing near you.*

**⚠ Grants no attack, no damage, no Defence at any tier.** **It is entirely counter-play, which is why it can afford to be free-action at tier 1 — it consumes nothing and produces information.**

---

# 2 — Why `Vigil` and not the alternatives

**⚠ Names checked across every document, not proposed and then checked:**

    Vigil · Unblinking · Standing Watch · First Sight · Open Eyes · Nothing Hidden    clear
    The Watch                                    ⚠ 4 documents — a substring of "Watchman"
    Sentry                                       clear, but one letter from SENTINEL,
                                                 which is this class's own parent

**⚠ `Sentry` is the one worth recording as rejected.** **It passes a string check and fails at a table, and check 21 tests strings.** **A near-miss with a class name in the same lineage is the kind of thing that reads fine in a document and confuses a player holding two sheets.**

**`Vigil` reads as a duty rather than a power, which is what the class is — a Jedi posted to a sector for years, reporting what moves in it.**

---

# 3 — ⚠ The roster is now feature-complete

    38 classes    38 with a class feature    28 of 28 pass PT-178

**`Vigil` was the last gap, and it existed because `jwa_granted` is the only class column in either game with no distinctive content once the shared package is stripped.**

**⚠ Three classes now hold authored features where the source gave nothing:**

    Tech Specialist   Both Sides At Once   PT-212 — "faithfulness to a class that did not work"
    Jedi Watchman     Vigil                this document — an empty column
    Juggernaut        Immovable Object     PT-202 — a class that never existed

**All three on the same reasoning, and it is the owner's from `PT-212`: a class that did not work is not a thing to be faithful to.**

---

# 4 — Still open, unchanged

**⚠ `FINDINGS-72 §3`: the `1 / 5 / 10` gating ruling never landed.** **`ATTACKS-05 §69`–`71` reads `2 / 4 / 10`, because `PT-193`'s restore used the original figures and the owner's change was made against the version being deleted.**

**⚠ And `FINDINGS-72 §1.1`: the `Sith Assassin` keeps `Vanish` and has lost its arithmetic distinction.** **Its stealth damage is now identical to any class that spends one tree on the chain. Reported, not fixed.**

**Next on the owner's list, per `REPLY-55`: starting attacks for every standard base class, and first-level multiclass benefits.**

---

# The question

> **⚠ Nothing blocking. `§4`'s two items are reported and waiting.**
