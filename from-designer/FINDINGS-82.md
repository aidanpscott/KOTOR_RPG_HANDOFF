# FINDINGS-82 — ⚠ Answering `REPLY-63`'s question. Four things are open, and I derived them rather than recalled them.

**`REPLY-63`: *"Say what you think is still open. If we agree it is nothing, the workstream closes."***

**⚠ It is not nothing. Four, and two have been reported twice without moving.**

---

# 1 — ⚠ The `1 / 5 / 10` gating never landed. Third report.

**Owner ruled it at `PT-...` after `FINDINGS-64`. `PT-193`'s restore used the original figures because the change was made against the version being deleted.**

**Current, derived:**

    ATTACKS-05 §69–71    Sneak Attack     2 / 4 / 10
    ATTACKS-04 §146      Stealthy Shot    2 / 4 / 10

**⚠ Reported in `FINDINGS-72 §3` and `FINDINGS-74 §4`. Neither moved it.**

**It is one edit and it is not mine to make.**

---

# 2 — ⚠ The stale Scoundrel prose. Second report, and it is now three lines.

**`FEATS-LIBRARY-01`, all still live:**

    §738   "Scoundrel (prestige) — the Smuggler's ladder, continued past its cap"
    §764   "The Scoundrel is what a Smuggler becomes when Sneak Attack is the only
            thing left."
    §766   "It does not get a fourth speed — three is the source's shape and a
            fourth would flatten the distinction between the other three."

**⚠ `PT-193` deleted the ladder. `§766` argues about a fourth speed on a thing that does not exist.**

**And `§764` is not merely stale — it is **false about the current class**. The Scoundrel has no dice advantage at all; its case is `Nowhere To Stand`, which buys openings.**

**⚠ The table above them carries a supersession note. The prose beneath it does not.** **Same shape as `PT-240`: the correction reached the structure and stopped at the text.**

---

# 3 — ⚠ The Smuggler and Sith Assassin are byte-identical, and the corpus says so

**`FEATS-LIBRARY-01 §722`–`725`, which I had not read:**

> *"The Smuggler and the Sith Assassin are granted the same mechanic on the same schedule, **byte for byte**."*

**⚠ That is the corpus stating my `FINDINGS-72 §1.1` finding more strongly than I did, in a document I have cited eleven times.**

**Reported twice as *the Assassin lost its arithmetic distinction*. It is worse: two classes now hold one mechanic identically, and `PT-83` split a pair at 89% overlap for less.**

**What separates them is `Vanish` against `Quickdraw`. That is one axis, and `FINDINGS-34` is on record that one axis is what this project has had to fix three times.**

---

# 4 — `check_landed` is at twelve, up from ten

**Two of the new four are from this workstream:**

    PT-197   two clauses on the stealth chains, both owner-ruled
    PT-198   ⚠ the stealth chains ARE declarations. PT-195 reversed.

**⚠ `PT-198` is a reversal that reached no document.** **A reader consulting the rules gets `PT-195`'s answer, and `PT-195` was reversed.**

**`PT-236` and `PT-238` are also on the list — the workstream-closing ruling and the `Treasure Hunter` rename, neither of which left a trace where the rules are stated.**

---

# 5 — What is genuinely closed

**⚠ To be clear about the balance: thirty-six classes each have a rate, stat line, entry, feature, starting attacks and an unlock level. The design work is done.**

**All four items above are **transcription**. None needs a decision, none blocks a class, and none is mine to fix.**

> **⚠ Which is why they have survived three reports. Nothing owns them.**

**`check_landed.py` exists for exactly this and has been at ten or more since it was written. It is run and read, and nothing is scheduled off the back of it.**

---

# The question

> **⚠ `§3` is the one that is a design problem rather than a transcription one.** **The corpus states the Smuggler/Assassin identity in its own words and no ruling has answered it.**

**`§1`, `§2` and `§4` are edits. Say who makes them and they are done.**
