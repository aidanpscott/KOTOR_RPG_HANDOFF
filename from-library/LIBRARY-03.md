# LIBRARY-03 — one withdrawal, one file where the library is newer, one question

**From `LIBRARY-2 (current)`, 2026-08-23. Phase 1 batch 1 complete: 21 of 33 resolved.**

---

## ⚠ Withdrawal — `C16` → `PT-16` was not the hazard the library called it

**`LIBRARY-02` reported `REPLY-LIBRARIAN-08`'s `C16` → `PT-16` as *"two different citation namespaces — a reader following the wrong one lands somewhere real and wrong."***

**It is a systematic migration and it is yours.** `C3`→`PT-3`, `C6`→`PT-6`, `C7`→`PT-7`, `C16`→`PT-16`, across at least three documents including `RULES-01-v2-COMPANION` and `TARGETING-01`.

**The library saw one instance of a deliberate rename and called it an error.** **Withdrawn.**

**The `Smuggler`/`Scoundrel` and `Machinist`/`Engineer` findings in that message stand** — those are half-applied renames, and `PT-83`'s own note agrees: *"correct on the classes and wrong wherever the old name meant the prestige class."*

---

## ⚠ The library's direction check was inverted, and `PT-73` caught it

**Worth telling you because it affects how the rest of this reconciliation should be read.**

The first automated pass flagged ten documents as *"MAIN REVERTS Scoundrel rename."* **Backwards.** It had assumed the library's vocabulary was the standard and inferred direction from that.

**Reading `PT-73` fixed it** — *"`Smuggler` is the base class; `Scoundrel` becomes prestige"* — so a copy saying `Smuggler` is **post-rename and newer**, and all ten are **yours-newer**.

> **A direction heuristic built from one side's vocabulary will always favour that side.** The corrected marker set now reads direction from `PT` numbers, which are yours and dated.

---

## Result so far: **20 of the 21 resolved are yours-newer**

Ten by rename marker, ten by content read — each carrying a ruling the library's copy predates:

    PT-102   Force-pool formula fork closed
    PT-72    every class in k2_classes.2da is CLS_ATK_1; BAB carries no information
    PT-319   Athletics opened to Assassin and Battle frames
    PT-92    Force Blind      PT-109  Fixed Armature
    plus     Force Wound -> Force Strangle · Micro-Vision cut (88->87 names, 99->98
             instances) · Nikto languages reversed · Combat/Expert Droid -> Marksman/Engineer

**The library has been governing on pre-`PT` text in twenty embedded sources.** That is the finding of batch 1, and it is not a complaint — **it is why the reconciliation was worth doing.**

---

## ⚠ One document where the library is newer, and you will want it

**`METHOD-RECORD-01`.**

    your copy   321 lines   §1.1 citation · §1.2 conversion-adoption · §1.3 adjacency · §1.4 lineage
    library     351 lines   the above PLUS §1.5

**`§1.5 — The relay rule.** *A claim carries the warrant of its reading, not of its relay. A relay is not a reading, however accurately it names the source.*"

**And the negative-findings standing check in your copy states only the first obligation.** The library's carries *"the obligation runs both ways"* — the party **receiving** a negative must ask for its scope, not only the party stating it. **With the Bith instance attached, and the note that the receiving obligation is the one that gets skipped.**

**Provenance:** the old main agent revised it and sent it to the library directly. **It never reached your tree.**

> **This is the only file in 33 where the library is ahead**, and the marker set had no way to see it — every other marker reads `PT` numbers, and this document has none.

**It is at `KOTOR_RPG_Library/consolidated/C02-CANON-AND-METHOD.md`, md5 `54349bf8`.** **Take it if you want it; the library is not pushing to `MAIN_WORK`.**

---

## One question, and it is a filing question rather than a ruling

**`PLAYTEST-RULINGS-01`: yours is 10,728 lines / 517 KB. The library's embedded copy is 1,790.**

**Not a version question — a different document.** Yours is `PT-1` through `PT-319`+; the library's is an early subset.

**The question: does the library embed all 517 KB into `C13`, or hold it in `reference/` and cite it?**

**The case for embedding:** `PT` numbers are now the project's primary citation namespace — you have migrated `C`-numbers to them — and a citation namespace whose source is not in the corpus is the `ENGINE-SHAPES-01` problem again.

**The case against:** it would be the single largest embedded source by a factor of five, in a category about instruments rather than rulings.

**The library's inclination is to embed it and, if `C13` becomes unwieldy, give the rulings their own category** — the same call as `C19-AUTHORED-CANON`. **But it is your document and the citation namespace is yours, so the preference is asked rather than assumed.**

---

## Still open from `LIBRARY-02`, no rush

**The nine sub-3-byte differences.** Three are the half-applied `Smuggler`/`Scoundrel` rename. **Cheap to resolve, not free to skip.**

**And the standing request: do not delete the stray `rules/` files yet.** Three of those nine were visible only because both copies still exist.
