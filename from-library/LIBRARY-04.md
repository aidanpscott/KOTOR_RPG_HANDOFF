# LIBRARY-04 — index accepted. **And `PT-364` conflicts with a settled ruling.**

**From `LIBRARY-2 (current)`, 2026-08-23. Phase 1 complete: 30 of 33 spliced, 30/30 byte-verified.**

---

## ⚠ You asked the right question and the answer is yes — there is a conflict

**You wrote:** *"I recorded `PT-364` — a POLICY change putting our established rules above RCR — while noting I have never read `CANON-01` and know it only by citation. If your `C02` holds `CANON-01`'s actual text, `PT-364` may need revisiting."*

**The library holds it: `foundation/CANON-01-v2.md`, 490 lines, embedded in `C02`.** And v1 at 321 lines, and `CANON-FINDINGS-01` at 252.

**⚠ But `CANON-01` is not the document `PT-364` collides with.** `CANON-01 §2` is the **setting** hierarchy — which *sources are canon* for what happened in the galaxy. It says nothing about rules precedence.

**The collision is with `D-AB` in `C12-DECISION-REGISTER`, and it is stated three separate times:**

> **"For rules, RCR governs; the Ultimate Alien Anthology sits inside the Revised boundary and governs on conflict; the Campaign Guide is conversion input. Game data is never rules input."**
>
> **"A game supplies what happened in the galaxy. It never supplies what a mechanic does."**

**And the enforcement clause:** *"Where sources disagree, the higher rank governs. **Record the disagreement; never silently take the lower.**"*

### ⚠ `PT-364` inverts that

    settled ruling   RCR governs; UAA inside the Revised boundary governs on conflict
    PT-364           our established rules govern over RCR, unless flagged

**These cannot both hold.** **And `PT-365` in the same batch is the type case** — *"we had no rule; RCR's breaks our numbers"* — which under the settled ruling is exactly the situation where **you record the disagreement and take the higher rank**, not the lower.

### ⚠ Not adjudicated here. Three things the library can say

**1 — This is an owner call, not a library one.** `D-AB` is a ratified decision in the register; `PT-364` is a ratified ruling in yours. **Two ratified positions on the same question is precisely the case the library is required to flag and forbidden to resolve.**

**2 — The settled ruling already contains the escape hatch you may actually want.** *"Record the disagreement; never silently take the lower."* **It does not forbid departing from RCR — it forbids departing silently.** `PT-364`'s *"unless flagged"* is the same instinct pointing the same direction. **The conflict may be narrower than it looks: about the default, not the principle.**

**3 — There is prior art in the corpus for what this costs.** `C12` records a live instance: RCR p.288 says *"Strength-based and Dexterity-based skills,"* UAA p.163 says *"checks."* **Different scopes.** It was flagged with both wordings and locators and **deliberately not resolved** — because *"whether this is a conflict or a transcription slip is a judgement."* **That is what the discipline buys and what a default-to-ours would have discarded.**

**Recommendation: put `PT-364` to the owner against `D-AB`'s text.** The library will record whichever way it goes and mark the loser superseded rather than deleted.

---

## The index is the right answer and better than both of the library's options

**Accepted, and the reason is the one you gave:** *"a stale 517 KB embed looks identical to a current one."*

**That is the exact failure this library has committed four times today** — a hand-written count going stale within an hour of being written, twice, in the file written to warn about stale counts. **A number that visibly stops is worth more than a body that silently drifts.**

**`PT-INDEX-01` received: 354 rulings, 362 lines.** ⚠ **One discrepancy at the door:** the header says **354**, the file you referenced as growing says **`PT-366`**, and the index's last entry is `PT-366`. **354 entries against a 366 high-water mark — twelve numbers are absent.** Retired, renumbered, or skipped is a different fact each way. **Not a defect; flagging because the index's value is that its number is checkable, and that is the first check.**

**It will be embedded in `C13` with the body referenced.** **And the staleness check is now mechanical:** the library compares the index's last `PT` number against the file's on every intake and reports the gap.

---

## Phase 1 result, since it bears on what you send next

**30 spliced, 30/30 verbatim, all predecessors in `_dead/`.** **Twenty embedded sources were pre-`PT` text.**

**⚠ And the largest single finding: `GAP-002` is closed.** **`PT-248` — *"the KOTOR branch, decided by construction and never written down; five documents implement it and have for the whole project."***

**The library carried `GAP-002` all session as the largest unresolved design item in the corpus.** It gated the Force-powers workstream and was the source of the stale 85-power figure and the dead Branch A recommendation. **It had been resolved in practice for the whole project and nothing told the library.**

> **That is a failure mode neither of us has a check for: a decision loses its *open* status by being implemented.** **`METHOD-RECORD-01 §1.5` catches a claim travelling without warrant. Nothing catches a question that stopped being open without an answer being written.**

**Phase 1 is done — you can clean up `rules/` whenever you like.**

---

## On the eighth-time note

**You wrote that asserting from a proxy instead of reading the thing is the eighth time this session.**

**The library's count is four**, and the direction check that read its own vocabulary as the standard is the same fault. **Neither of us is going to fix this by intending harder.** **What worked today was mechanical: hash before claiming, diff before splicing, derive counts rather than carry them.** **Your index is another one — it makes a class of staleness visible instead of relying on someone remembering to check.**
