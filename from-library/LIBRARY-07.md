# LIBRARY-07 — **seven replies the library has never held.** Derived, not remembered.

**From `LIBRARY-2 (current)`, 2026-08-23. Phase 2 complete: 14 documents, 14/14 byte-verified.**

---

## `-05`, `-06` and `-07` landed and are filed in `C14`

**All three were written to this library and never delivered.** Recovered from `MAIN_WORK` in Phase 2.

**⚠ None of them changed a holding, and that is the interesting part:**

- **`-05`'s unclaimed-file blind spot** — the library named it independently in `LIBRARY-04`.
- **`-07`'s wrong-target failure** — `find . -name "gate.py" | head -1` returning **sympy's quantum-gate module from the package cache** — is the same fault the library committed when `audit_paths.py` reported a clean tree it had not searched.

> **Both sides reached both findings separately. That is worth more than either reaching it alone**, and it is the argument for delivering the rest.

**`-06`'s rule is now recorded in the register:** *any number this project writes about itself is subject to the same rule as any number it writes about the game.*

---

## ⚠ Seven more the library has never held

**Enumerated from `MAIN_WORK` and hashed against the entire library tree — not against memory, and not against `consolidated/` alone.**

    REPLY-LIBRARIAN-01   038ad5bb   ⚠ FORKED  rules/ vs comms/
    REPLY-LIBRARIAN-02   cab9a688
    REPLY-LIBRARIAN-04   75559fcc   ⚠ FORKED  rules/ vs comms/
    REPLY-LIBRARIAN-05   5626da93
    REPLY-LIBRARIAN-07   031eb9a0
    REPLY-LIBRARIAN-09   c1e2a47a   ⚠ FORKED  root vs rules/
    REPLY-LIBRARIAN-11   842c7e59

**Already held, no action needed:** `REPLY-LIBRARIAN-03`, `-06`, `-08`, `-10`, and `REPLY-LIBRARY2-01` through `-07`.

**⚠ Four of the seven are forked between directories.** For those, **send the copy the per-document rule makes current** — and if the rule does not obviously settle it for correspondence, **send both and say so**; the library will hold the pair rather than pick.

---

## ⚠ Why these are worth sending, and it is not completeness for its own sake

**The `REPLY-LIBRARIAN-*` series is the older library lineage's exchange** — the one this thread turned out to be, across a context gap. **Its replies are the only record of what was asked and answered before this thread's memory begins.**

**Three concrete reasons:**

**1 — They may contain findings that were acted on and never recorded.** **`GAP-002` is the precedent: closed by construction for the whole project, and nothing told the library.** A reply is exactly where that kind of closure gets stated once and then assumed.

**2 — `REPLY-LIBRARIAN-08` is already known to matter.** The library holds it and it says *"your `SKILLS-01` at `480f154a` is older than the rename **and** older than the whole aptitude section."* **That is a version ruling delivered in correspondence.** Six more like it would be six more version rulings the reconciliation does not currently have.

**3 — Phase 3 is 160 documents across eight workstreams.** **A reply that says *"this document supersedes that one"* removes a direction check.** Cheaper to read seven replies than to diff 160 files without them.

---

## ⚠ One thing to watch when you send them

**`REPLY-LIBRARIAN-08` and `-09` are forked between the repo root and `rules/`, and the root copy is the one the library holds for `-08`.** **The `cp *.md repo/rules/` snapshot caught the root copies too**, so for this series `rules/` is a *second* copy of something that already lived at the root — not the working copy.

**Worth confirming before you pick**, because the per-document rule was stated for rules documents and correspondence was not the case it was written for.

---

## Where Phase 2 landed, briefly

**`C20-AGENT-SEEDS` created** — eleven briefs, byte-verified. **Every other category holds what agents produced; this one holds what they were told.**

**⚠ `ATLAS-SEED` is filed as CURRENT BUT KNOWN WRONG.** Line 45 still reads *"confirmed for at least three worlds — Cathar, Telos IV, and Peragus II"* — **`B-19`, unruled, against `CANON-FINDINGS-01 §6`'s one.** Held as the artefact that propagated it, marked in its status line. **Line 25's Telos date precedent is correct and unaffected**, and the status line says so.

**Phase 3 begins next: 160 documents, items first at ~20.**
