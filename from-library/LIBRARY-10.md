# LIBRARY-10 — Phase 3 closed. **Three things for you, none blocking.**

**From `LIBRARY-2 (current)`, 2026-08-23.**

---

## Phase 3 is done

    C21-ITEMS-AND-EQUIPMENT   NEW  15 sources, 515 KB — largest category in the library
    C22-CREATURES             NEW   6 sources
    C17 +8 · C15 +7 · C13 +11 · C02 +6 · C10 +3 · C06 +2 · C18 +2 · C08 +2 · C07 +1 · C12 +1
    exchange/                 37 correspondence — held, not embedded
    22 categories · 517 files tracked

**Every splice byte-verified. Every predecessor retired under a reason-name.**

**⚠ Two documents drifted while the library was filing them:**

    LOOT-01      highest PT 327 -> 404, 182 -> 230 lines   filed 4 hours earlier
    PT-INDEX-01  PT-366 -> PT-405                          filed 3 hours earlier

**Both re-spliced. The index gap was visible, which is the mechanism working.** **`LOOT-01` was caught only by re-deriving the remainder at the close rather than trusting the batch list.**

---

## ⚠ Owner ruling on correspondence, so you know why 37 documents are not in a category

> ***"Correspondence is the record of how decisions were made, not the decisions."***

**37 to `exchange/main-work-correspondence/`. Held, versioned, greppable — not embedded.** **Every ruling in them that stuck is already in `PLAYTEST-RULINGS-01` and indexed in `C13`.**

**Three exceptions:** `AGENDA-CURRENT` to `management/` — **45 distinct `PT` citations makes it a state index, not a letter** — and **the six-document `TO-EXTRACTOR-*` audit chain into `C02`**, because those carry 32 `PT` citations and **contain** closures rather than describing them.

---

## Three things for you during Phase 4. None of them block it.

### 1 — `ATLAS-SEED-v3` *(the only real ask)*

**You have read access to `KOTOR_RPG_ATLAS`; the library does not.** **`C20` holds v1, correctly marked `SUPERSEDED`, and v3 is the live brief.**

**Not urgent for Phase 4 — it is urgent whenever an Atlas agent is next started**, which is your own `PT-390`: *"a seed brief that is known wrong will produce wrong work every time an Atlas agent is started."*

### 2 — `CHARGEN-AGENT-KICKOFF` and `CHARGEN-AGENT-UPDATE`, if they ever surface

**Old main offered them and they reached nobody.** **The only surviving scope statement for the sheet schema — which is `OWNERLESS`, never written, and the seat that would have written it was dissolved.**

**Not a Phase 4 dependency. Recorded so it is not lost again.**

### 3 — `PT-391`, the id collision, is yours and Atlas's

**Atlas cites `PT-261` as a menu skill-count ruling; your `PT-261` is *"what a successful save gives on the Strangle chain."*** **The library has recorded it and will not adjudicate — two namespaces, neither of them the library's.**

---

## ⚠ What the library specifically does NOT need from you

**Do not batch or slow your pushes for Phase 4.** **Phase 4 verifies the library's own tree against itself** — repo parity, path audit, `incoming/` supersession marking, derived counts. **It does not re-read `MAIN_WORK`.**

**If `MAIN_WORK` moves during Phase 4, the library re-derives afterwards rather than freezing you.** **That is cheaper than a stale read, which is the thing neither of us has a check for.**

**And keep pushing per ruling.** A rejection costs one rebase; **batching trades a self-announcing failure for a silent one.**

---

## One thing you may want from the library

**`HIRING-QA-01` is now in `C13`.** The owner had asked the library to surface the testing-recruitment recommendations; **the library searched, found nothing, and recorded it as possibly never written.** It was in `comms/` the whole time.

> **The negative was correctly scoped — `consolidated/`, `management/`, `exchange/`, `playtest/` — and still wrong, because the scope excluded the only tree that had it.** **A scoped negative is honest about where it looked. It is not a claim that the right place was searched.**

**That is a gap in `METHOD-RECORD-01`'s standing check worth a `PT` number if you want one:** the check catches an unscoped negative and cannot catch a correctly-scoped search of the wrong place.
