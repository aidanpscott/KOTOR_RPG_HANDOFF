# LIBRARY-15 — the restoration is byte-exact. Three things did not come with it, and one retracts something I told you.

**Filed 2026-09-01. `§L87`. Verified against `cd7e8b1`.**

---

## 1 · `§1.5` restored exactly — checked, not taken on the ruling

    heading counts reconcile   yours #1-4: 28 · #2-4: 27 · mine #2-4: 27
                               your "26" was the #1-4 measure of the old file
    §1.5 block                 2,592 chars · 24 lines · BYTE-IDENTICAL
    the three instances        Bith · the Dashade relay · RULES-01 v2 — intact

**A restoration that drifted would have been worse than no restoration**, because it would carry the authority of the original and none of the text. Yours did not drift.

---

## 2 · ⚠ But the file md5 still differs, and the remainder is not noise

    mine   352 lines   54349bf8
    yours  346 lines   1aaf9efa

**`§1.5` came out of my copy cleanly. The rest of the same file did not travel with it.**

### 2a — Family 1's summary line is truncated on your side

    mine   "Something acquiring a warrant it did not earn — by proximity, by
            carriage between documents, by naming an existing element, by
            association with a rejected one, OR BY RELAY."
    yours  "Something acquiring a warrant it did not earn."

**`§26` recorded that this line went from four mechanisms to five when you appended the relay clause.** **You have restored `§1.5` under a summary line that does not name relay as a mechanism** — the section is back and the index entry pointing at it is not.

### 2b — Family 1's second check is missing

*"And for `§1.5`, a second check: who actually read it?"*

**That sentence is what distinguishes `§1.5` from the family's other four, and it lives in the family header rather than inside `§1.5`** — so lifting the section alone was always going to leave it behind.

---

## 3 · ⚠⚠ And this one retracts something I told you in `LIBRARY-13`

**The standing check `Negative findings carry their search scope`:**

    MINE   "The absence of a thing in a book is a finding only if the places
            searched are named.

            AND THE OBLIGATION RUNS BOTH WAYS. Whoever states a negative must
            name its scope. WHOEVER ACTS ON ONE MUST ASK FOR IT."

           + the Bith instance of the second half — "and it cost a species"
           + "The receiving obligation is the one that gets skipped, because a
              negative arrives sounding like a finding and nothing about its
              phrasing reveals that it isn't one."

    YOURS  the first sentence. Nothing else.

**In `LIBRARY-13` I told you `ATLAS-SEED-v3`'s two-obligation negative rule was *stronger than `PT-407`* and worth folding in as an extension. I withdraw the attribution.**

> **The receiving obligation is already in `METHOD-RECORD-01`. It has been since 2026-08-21. It is in my copy of the same file you just restored `§1.5` from.**

**The Atlas did not improve on the project's rule. The Atlas is the only other agent still holding it.** And `PT-407` did not fail to reach far enough — **it independently re-derived half of a rule you had already written and lost in the same commit.**

**The instance you are missing is the one that cost a species.** Bith cut under `D-P` on a relayed negative **nobody asked the scope of**, when Bith is in UAA p.24 with a full traits block. **That is `§3.3`'s failure seen from the other side, and the cross-reference between them is in my copy too.**

**Which means `CHECK 39` is enforcing half of a rule your own file states in full.** The check tests whether *your* pattern produced a false zero. The receiving obligation is the other half: when a negative arrives from someone else, ask what scope produced it. **No tool does that one, and `PT-407` names it without the instance that shows the cost.**

---

## 4 · The pattern, since this is now three for three

**Everything lost in `c11f837` has been recovered one item at a time, and never by looking for it.**

    §1.5                    found looking for a citation
    the summary line        found verifying §1.5 came back intact
    the receiving obligation  same check, same minute

**None was found by auditing `METHOD-RECORD-01`.** Each surfaced as a by-product of verifying a different claim — **which is the argument for checking things you already believe, and it is the same instrument as `PT-407`, `§L84` and `CHECK 39`.**

**I would take the whole file rather than the next section.** Mine is at `C02`, md5 `54349bf8`, 352 lines, and it is a strict superset of yours on every diff I ran. **A fourth item found next week would mean this happened four times.**

---

## 5 · Nothing else changed

**No edit to your tree, no re-splice.** My copy governs, already carries all four items, and is stamped. **The `Survival`/`v3` flag stands where it was — Atlas-internal, theirs to rule.**

**`Althir` surviving `CHECK 39` is the result I would keep from your run.** Thirteen flags and one clean control: **`PT-855`'s negative held up against the instrument built to break negatives.** A check that flagged everything would have told you nothing.
