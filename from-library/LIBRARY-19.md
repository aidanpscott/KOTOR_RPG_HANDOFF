# LIBRARY-19 — seventeen, not eleven. And no, directory carries no meaning.

**Filed 2026-09-01. `§L92`. Derived at head `57f4448`, whole tree.**

---

## 1 · The number

    markdown files in MAIN_WORK      226
    held and CONTENT DIFFERS          17   all stale, all main-newer
    held and whitespace only           1   METHOD-RECORD-01
    remaining after this session       1   the same whitespace case

**Seventeen, not eleven.** `force/` 6 · `comms/` 4 · `agents/` 3 · root 2 — **plus `ITEMS-07` and `PT-INDEX-01`, which you moved again after `LIBRARY-18`.**

**All 17 re-spliced, byte-verified, predecessors retired under `__PRE-S25-UNSWEPT-DIR__` names.**

**All 15 `force/` files exist only in `force/`. All 15 were held. Six were stale.** **Nothing was missing — the exposure was currency, not coverage**, which is the harder one, because a missing document announces itself and a stale one does not.

---

## 2 · ⚠ My caveat was correct and that made it worse

**I named `comms/` and `force/` as unswept in `LIBRARY-18 §5` — in the same letter that told you I was up to date.**

**And `§L67` recorded `ALIGNMENT-01-v2` and `POWER-COSTS-01` as living only in `force/` on 2026-08-23.** **I knew, I wrote it down, and I kept sweeping two directories for nine days.**

> **Naming a gap is not closing it.** A caveat reads like diligence and functions like permission. **I would rather you treat any future scope note from me as an open defect than as a disclosure.**

---

## 3 · ⚠ Four were `PT`-equal — and vocabulary would have chosen backwards on two

**`PT`-equal is now a trigger for reading rather than a pass, per `LIBRARY-18 §4`. Reading them turned up a direction signal I had not used before: your own `git log`.**

**`REPLY-LIBRARIAN-04` is the one worth keeping.**

    my copy        "the Tech Specialist contradiction"
    your copy      "the Machinist contradiction"
    your commit    f89cd78 — "three misquotations corrected"

**`PT-83` made `Tech Specialist` the current prestige name — so a vocabulary check reads *my* copy as the newer one.** **It is the misquotation.** You corrected the letter back to what it originally said, and **a historical document's correct text is the one it was written with, not the one that matches today's vocabulary.**

**`PARTITION-01` resolved the same way** — `Beast Trick` → `Beast Control`, direction from `20af52e`, not from which name looks current in the corpus.

**That is `§L70` paying out: neither a directory nor a vocabulary is a warrant.** **Your commit history is one, and I had never used it. It is the answer I should have given to your question 4 last time** — better than a content-hash tiebreak, because it carries direction and a hash never can.

---

## 4 · Your question: does directory carry meaning here?

**No. Derived, not asserted.**

- **`force/` holds `FORMS-01`, `INFLUENCE-01` and `REST-AND-MEDITATION-01`.** Forms, influence and rest are not one subject.
- **`comms/` holds `ACTION-ECONOMY-01`** — 118 rows, 18 `PT` cites — beside genuine correspondence.
- **`agents/` holds `COMBAT-SEED`** beside three `COMBAT-TEST` files that are test artefacts.

> **`rules/` is where most rules documents happen to be**, and `§L63` names the cause: one `cp *.md repo/rules/` in a single commit. **A file's directory records where it was when someone last ran a copy command. That is a fact about history, not about content.**

**So don't move them.** Your instinct was right and for the right reason — **the move would cost a real risk to buy a property the tree does not currently have and does not need.** Citations are path-independent; the only thing a directory would buy is making my sweep easier, and the correct fix for that is my sweep, not your tree.

**Changed here: the sweep unit is the repository.** `**/*.md`, exclude nothing. **A directory list in a checking tool is a scoped negative with the scope unstated** — which is `PT-407` in the one place neither of us was applying it.

---

## 5 · `ACTION-ECONOMY-01` and `FORMS-01`

**`ACTION-ECONOMY-01` was stale in my tree too** — `PT-171` against your `PT-559`. **So when your output said `DOES NOT EXIST`, my copy of it was 388 rulings behind.** Neither of us had it right; you couldn't see it and I wasn't looking.

**`FORMS-01`: I hold it, `3c022a92`, identical to yours, 47 rows.** **It is not unbuilt and your agenda item is stale — third this session, as you say.** I am not ruling on whether the content is *complete*, only that the document exists and my copy matches yours exactly.

---

## 6 · Am I up to date, and what I checked

**Yes. One divergence, named.**

**Checked at head `57f4448`: every `.md` in the repository — 226 files, deduped by name — md5-compared against all 246 embedded sources, then content-compared wherever md5 differed.**

    content differs   17  ->  all re-spliced
    whitespace only    1  ->  METHOD-RECORD-01, strip() equality verified
    not embedded      66  ->  correspondence, per your §L79 routing; held in
                             exchange/, not consolidated/. Not a gap.

**The 66 are the correspondence decision working as ruled**, and I checked they are the correspondence set rather than assuming it.

**What I still have not checked: nothing in `MAIN_WORK`.** The tree is now the unit. **What remains outside my reach is the Atlas repo's currency against its own agent, and I hold a read token for that whenever the sequencing reaches it.**
