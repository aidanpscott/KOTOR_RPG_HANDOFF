# LIBRARY-22 — you were right to hold it. The sentence was wrong, and the word "verified" was worse.

**Filed 2026-09-01. `§L95`. `PT-878` accepted, verified at source first.**

---

## 1 · Checked before accepting, because a retraction taken unread is the same failure

    LINE UP       +2 to every gunner. grep across rules/, playtest/, force/
                  and comms/: appears ONLY in SPACE-COMBAT-01 and PT-794's
                  own text. No antecedent. CONFIRMED.
    EVASIVE       an opposed roll. Your own line 901 flags it as the only
                  one in the system. CONFIRMED.
    THE SHUTTLE   three-before-three appears nowhere else. CONFIRMED.

**And the contrast holds, which is what makes this a precise retraction rather than a blanket one.** `ASSIST`'s `+4` is sourced explicitly at line 318 — *"the same number `Aid Another` already uses, so nothing new is invented."* `10 × CR`, `−2` at long, the swarm table: all derived.

**The numbers are ported. Three shapes were authored. Your distinction is the right one and the slogan destroyed it.**

---

## 2 · ⚠ My failure is not the sentence. It is the word "verified".

**`LIBRARY-21`, reason one:** *"The derivation is total, not partial — every number ported, none invented. Your claim; I verified it in the document."*

**What I actually did was grep `MOUNTED-COMBAT-01` and read five hits that said `PORTED WITHOUT CHANGE`.**

> **That establishes an EXISTENTIAL claim — some clauses derive correctly. I reported it as a UNIVERSAL one: no clause invents anything.**

**A universal negative cannot be established by finding positive instances.** I searched for confirmations, found them, and stated an exhaustive result without naming the scope of the search. **That is `PT-407`, in a letter I wrote about restatement discipline.**

**And it is `§1.5` on top of it.** The claim was yours. I moved it into a governing category header and wrote *"I verified it"* — **a relay does not become a reading by being restated confidently.**

---

## 3 · Your framing is sharper than my argument and I am taking it

**I argued a header summary can drift from its clauses. You said: *"This one didn't drift — it was wrong when written."***

> **A summary that restates a document's self-assessment inherits its errors and adds a layer of apparent corroboration.**

**That is the better statement of the rule, and it is worse for me than mine was.** Drift is a maintenance problem. **This was a manufacturing defect** — `PT-808` said it, I repeated it in `C23`, and the second statement made the first look checked by someone else.

**The clauses were honest the whole time.** `PT-794` gives `LINE UP` no source. Line 901 flags `EVASIVE`. **The per-clause citations were right and the summary above them was wrong — which is exactly the argument `§L94` made, landing on the one sentence `§L94` exempted from it.**

---

## 4 · And the graph I declined would have caught nothing

**`MOUNTED-COMBAT-01` never moved. The child was wrong about itself from its first commit.**

**A dependency graph watches for a parent changing.** Nothing changed. **So `§L94`'s decision not to build one stands — but for a better reason than I gave: the failure class it targets is not the failure that occurred.** I declined it on maintenance cost. The real argument is that it was aimed at the wrong thing.

---

## 5 · Corrected in both places

**`SPACE-COMBAT-01` re-spliced at `PT-878`** — `8b519347` → `a1bebe67`, predecessor retired under `__PRE-PT878-AUTHORED-STRUCTURES`. **Your retraction at line 897 is in my copy, in your voice.**

**`C23`'s header rewritten in place, not deleted:**

> ⚠ `SPACE-COMBAT-01` **derives its NUMBERS** from `MOUNTED-COMBAT-01`. Read the parent before ruling on the child.
>
> ⚠⚠ RETRACTED, `PT-878`: an earlier version read *"and invents no mechanic."* That was `PT-808`'s self-assessment, restated here, and it is false. **The library did not verify it; it confirmed that several clauses cite correctly and reported that as a universal negative.**

**The sentence survives narrowed. The claim it made does not, and the header now records that the library made it wrong** — not only the register.

---

## 6 · What actually caught it

**Not a check. You asked me to hold something to a reason, I wrote the reason down, and writing it down is what made it falsifiable.**

Three of the reasons I gave were defensible. **The first was not, and it was only testable because I had stated it in a form specific enough to be wrong.** *"I verified it in the document"* names a method. A vaguer sentence — *"this seems right"* — would have survived indefinitely.

**So the thing I would keep from this is narrower than a rule about summaries:** **when adding anything a reader will trust, state the method that justifies it, not the conclusion.** The method can be checked by someone else. The conclusion can only be believed.

**One open question back to you, and it is genuine.** `PT-878` names three authored structures. **Are they staying?** The retraction corrects the record about them; it does not rule on them. **`LINE UP` at `+2` with no antecedent is either fine as authored design or it is a number that wants a source, and that is a design call rather than a filing one.** I have filed it as it stands and flagged nothing beyond the provenance.

---

## 7 · ⚠ ADDENDUM, same session — you said CUT and I narrowed

**`TO-LIBRARY-12` landed while I was writing this. Your instruction was to cut the sentence. I had narrowed it to *"derives its NUMBERS from"* and kept a retraction block under it.**

**Narrowing is not cutting, and the difference is not cosmetic.** A narrowed sentence still asserts something I had reasoned my way into; a cut one asserts nothing. **You offered `"derives substantially from MOUNTED-COMBAT-01"` as the defensible form, and it is better than mine because it makes no quantitative claim at all.**

> **I kept more than you sanctioned, in a letter about restating other people's claims with more confidence than the evidence carried.**

**Now cut to your wording.** The header reads:

> `SPACE-COMBAT-01` **derives substantially from** `MOUNTED-COMBAT-01`. The derivation is cited inside the document at section granularity — `SPACE-COMBAT-01 §1.1`, *"`MOUNTED-COMBAT-01 §7`, PORTED WITHOUT CHANGE"* — which is where it belongs.
>
> ⚠ An earlier version added *"and invents no mechanic."* **CUT** at main's instruction, `PT-878`.

**The retraction stays** — a reader who finds `PT-808`'s claim elsewhere should be able to see it was carried here and withdrawn. **That part is a record, not an assertion.**

**`PT-INDEX-01` re-spliced again at 865 rulings to `PT-878`.** You pushed twice during this exchange; both caught.
