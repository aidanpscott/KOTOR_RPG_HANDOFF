# LIBRARY-29 — `decisions/` first pass: two conflicts dissolved, one real gap, and one agreement neither tree could have copied

**Filed 2026-09-01. `§L102`. Boundary crossing: a pointer added to `C02`.**

---

## 1 · Two apparent conflicts, both checked before reporting, both dissolved

**These are the rulings only I can check, because `RULING-SWTOR-DEEP-HISTORY` and `AMENDMENT-DEEP-HISTORY-BOUND` are in `C02` and absent from the Atlas tree.**

### ① `D-SWTOR-01` versus the library's SWTOR rule

    library    "a SWTOR-attested deep-history fact may be PROPOSED for
                admission, ONE ITEM AT A TIME... never admitted by default
                and NEVER ADMITTED AS A CLASS"
    D-SWTOR-01 "Places — Admitted. Hutts — Admitted, aged back 313 years"

**Two classes against a rule saying never as a class. It reads as a direct hit.**

**It is not. The objects differ in time direction.** The library's rule governs SWTOR attesting the **deep past** — Khem Val at ~5000 BBY, before the campaign. **`D-SWTOR-01` governs SWTOR at 3643 BBY, 313 years *after*, reasoned backward.** A building standing in 3643 is evidence it stood in 3956. **That is not a deep-history attestation and the per-item rule does not reach it.**

### ② `D-EXCEPT-01` versus `D-EXCEPT-05`

**`D-EXCEPT-01` admits a 2015 FFG book. `D-EXCEPT-05`'s table says *"post-2014 canon — no exception reaches it."*** Read as written, one Atlas ruling falsifies another.

**It does not.** `D-EXCEPT-05` line 48 lists `D-EXCEPT-01` in its own standing set — *"`01` post-2014 book, `Nar Kreeta` terrain."* **The table row is scoped to the wiki exceptions, and line 42 says so explicitly about `Dorin`.**

> **Two near-misses in one pass, both would have been findings published against another agent's tree, and both dissolved on reading one more line of the document that appeared to contradict.**

---

## 2 · ⚠⚠ The real finding, and it is mine

**`AMENDMENT-DEEP-HISTORY-BOUND`, `C02`, CURRENT, md5 `f35ca632`:**

> ***"Nothing published after the April 2014 reboot is admitted at any depth."***

**Unqualified, and now false.** `D-EXCEPT-01` — owner ruling, 2026-08-25 — admits `Lords of Nal Hutta` (FFG, 2015) for `Nar Kreeta`, **terrain only**.

**Not a conflict.** A later owner ruling carving a named exception to an earlier one is normal. **The defect is that a reader consulting `C02` alone refuses `Nar Kreeta`'s terrain and is wrong, and nothing in `C02` points at the exception.**

**`§L94`'s shape again: correct in their tree, invisible in mine, unlinked.**

**Fixed as a pointer in the status line. The verbatim body is untouched** — the ruling said what it said on the day it was written, and `§L67` governs. **This is inside the write-freely tier as I scoped it to you: a pointer, not a record, and it does not touch `C03`.**

---

## 3 · And one agreement neither tree could have copied

    library      "Nothing published after the April 2014 reboot"
    D-EXCEPT-05  "post-2014 canon — no exception reaches it"

**Same bound, same date, two trees, no shared document.** The Atlas does not hold `AMENDMENT-DEEP-HISTORY-BOUND` and derived the boundary independently — **including the reasoning that it is a continuity rather than a year, which is the exact point my copy makes about why the 2010 bound was replaced.**

**That is the duplicate read producing corroboration instead of a correction.** **First time this week, and worth recording, because a mechanism that only ever finds errors starts to look like an error-finding mechanism rather than a truth-finding one.**

---

## 4 · Position

**Eight source-admission rulings read. 24 remain, and `D-REVAN-01`–`04` plus `D-VIT-01` are next** — they are 470 lines about Revan, Vitiate and the fall, and they will touch `C03`'s dated records rather than `C02`'s method.

**That is where I expect the first genuine collision**, because it is the first place their rulings and my machine-checked records describe the same events.

**Still not writing into `C03`.** `§1.5` unconfirmed at Atlas head `3175d89` — `METHOD-RECORD-01` absent, zero hits. **Checked this pass, not carried from the last.**
