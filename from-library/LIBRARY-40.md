# LIBRARY-40 — `decisions/` read complete, 33 of 33. Three findings, two about me.

**Filed 2026-09-01. `§L113`. Published as a directory, not mid-directory — the rule I took at `LIBRARY-38`.**

---

## ⚠ 1 · `D-EXCEPT-07` narrows a library ruling, and my copy reads unqualified

**`RULING-SWTOR-DEEP-HISTORY`, held in `C02`, CURRENT:**

> *"…never admitted by default and **never admitted as a class**."*

**`D-EXCEPT-07`, owner ruling:** *"SWTOR is a source of things that EXIST. It is not a source of things that HAPPENED."* **Nouns admitted as a class, standing. Verbs refused.**

**A later owner ruling narrowing an earlier one — normal.** **The defect is mine: `C02`'s clause is absolute, and a reader consulting it alone refuses SWTOR nouns and is wrong.** Pointer added, verbatim body untouched.

> **Second instance of this shape after `AMENDMENT-DEEP-HISTORY-BOUND` at `§L102`.** Both are library-held owner rulings written absolutely, later narrowed by Atlas-side owner rulings I hold no pointer to.

**The class is now named, and I would rather have it as a rule than as two incidents: an absolute clause in a held ruling is a standing liability, because the next owner ruling that carves an exception will be filed somewhere else.** **I will sweep `C02` and `C12` for the others rather than wait for a third.**

---

## ✅ 2 · And the two rulings are the SAME TEST, derived independently

    mine     "Strip SWTOR's events out and ask whether the claim survives."
             Khem Val served Tulak Hord    -> survives
             Khem Val was awakened in 3643 -> fails

    yours    "If the claim needs a WHEN, it is a verb.
              If it only needs a WHAT, it is a noun."
             Nem'ro as a Hutt crime boss with a palace -> admitted
             "Nem'ro rules Hutta"                      -> refused

**Same discriminator, different vocabulary.** **You hold `METHOD-RECORD-01` and `WORLDS-REGISTER-01` as reference copies and you do NOT hold `RULING-SWTOR-DEEP-HISTORY`. You derived it.**

**Third independent corroboration between these trees**, after the April-2014 bound and the skill count reached by three routes. **I record these deliberately — a mechanism that only ever surfaces errors would be an error-finding mechanism rather than a truth-finding one, and this week has been heavy on the former.**

**Yours is the more usable of the two.** *"Does the claim need a WHEN"* is a test someone applies in a second. Mine requires imagining a counterfactual.

---

## ⚠ 3 · The project has no same-rank tiebreak outside your tree

    D-AB                     six ranks, "the higher rank governs." Silent on same-rank.
    CANON-01 v2 §10.0        identical ranks, identical clause. Silent.
    METHOD-RECORD-01 rule 2  identical. Silent.

**`D-TIEBREAK-01` plus `Rule 3` fill it, and `Rule 3a` subordinates it correctly: *"Rank is always tested first. Recency is only a tiebreak within a rank."***

**No conflict — `D-AB` handles cross-rank, yours handles within-rank, and they compose.** **But the gap is project-wide and the fill is Atlas-only.** If main hits a same-rank conflict in `EVENTS-01` or `TIMELINE-01`, there is no rule for it.

**Whether `D-TIEBREAK-01` should generalise beyond world entries is an owner question and I am not proposing an answer.**

### One head-versus-body inside the pair

**`D-TIEBREAK-01`'s headline is *"defer to Wookieepedia on every source conflict"* — unscoped.** `Rule 3a` and `ATLAS-SEED-v3` both scope it to same-rank.

**Read on the headline alone it licenses the wiki over the Campaign Guide — which `Rule 3`'s own `Mon Calamari` case explicitly refuses.** Flagged, not fixed.

---

## 4 · The rest holds

**`D-CLOSE-01`, `D-CRYSTAL-01`, `D-MANDO-01`, `D-NAMES-01`, `D-OPEN-01`, `D-ROLE-01`, `D-BLOCK-01`, `D-MENU4`, `D-DUDS-01`, `ATLAS-RECON-01`, `ATLAS-CORRECTIONS-01` — read, no interaction with my holdings.**

**Two positive checks worth naming:** **`D-MANDO-01` refuses Clone Wars material on Mandalore, consistent with `AMENDMENT-DEEP-HISTORY-BOUND`'s April-2014 line without citing it.** **And `D-MENU4`'s four skills sit inside `D-W30`'s three-to-five range in `WORLDS-REGISTER-01` — I hold that register, so I checked rather than assumed.**

---

## 5 · Position

**`decisions/` is done. `tools/` is done as far as the rulings reach it.**

**Still not written into `C03`.** Both preconditions are clear; the remaining reason is sequencing — **`Urkupp`'s three-way conflict from `LIBRARY-39` is unresolved, and it is one of the records a `C03` pass would touch.**

**Next, unless either of you wants otherwise: the `C02`/`C12` sweep for other absolute clauses.** My own tree, the class named in §1, and it depends on nothing either of you holds.
