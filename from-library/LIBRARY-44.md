# LIBRARY-44 — Pass A run. 39 restatements listed, nothing edited.

**Filed 2026-09-01. `§L117`. `management/RESTATEMENT-REGISTER-01.md` is the output.**

---

## 1 · The run

    embedded sources          247
    comparable sentences   29,647
    rule-shaped restatements   39      2 at three sources · 37 at two

**`tools/survey_restatement.py` states its own 57% recall in its docstring and again at the head of every run. The register states it before the list.** **Nobody can reach the 39 without passing the caveat twice.**

**No pointers added. No status lines touched. Nothing edited outside `management/` and `tools/`.** Verified after: 247 sources, 0 unstamped, 0 duplicate basenames — unchanged.

> **`§L110`'s five duplicate embeds came from an append made while doing something else. This pass deliberately did nothing else.**

---

## 2 · Triage

    2   already pointered   SWTOR rule (§L113/§L115) · April-2014 bound (§L102)
    3   hierarchy family    "where sources disagree, the higher rank governs"
                            "for rules the games rank nowhere"
                            "the dependency test was always the rule"
    34  untriaged           need a read

### The one new three-source case, and I cleared it

**`universal powers always cost 1.0x regardless of alignment` — `ALIGNMENT-01`, `DRIFT-RATE-TEST`, `DRIFT-RATE-TEST-v2`.**

**All three identical apart from bold placement. No drift, no narrowing ruling against it.** **Benign, and recorded as benign** — a candidate cleared is worth as much as one raised and costs a minute.

---

## 3 · ⚠ The hierarchy family is the one that matters, and it is yours as much as mine

**Three of the 39 are the setting/rules hierarchy restated across sources.**

**`§L109` added a rank ABOVE rank 1 by owner ruling ten hours ago. `§L115` established that the SWTOR rule existed in seven copies with one pointer.**

> **The hierarchy is the most-cited rule in this project and it has just been amended.** **If it is restated the way the SWTOR rule was, the `D-REVAN` amendment reaches only the copies someone remembers.**

**`§L109` recorded that amendment as LATENT — it has not yet displaced a rank-1 or rank-2 statement.** **That makes a missed copy cheap today and expensive the first time it fires**, which is the worst shape for a defect: no symptom now, and a wrong answer at the exact moment someone finally relies on it.

**Not chased. Pass A produces a list; chasing is Pass B and it was not authorised.**

---

## 4 · What I would do next, and what I want ruled

**Pass B on the hierarchy family only** — three candidates, plus the paraphrase search that Pass A cannot do. **Bounded, and aimed at the rule most recently amended rather than at the largest pile.**

**The 34 untriaged can wait.** They are 2-source restatements with no known narrowing ruling against any of them, and reading 34 to find the two that matter is the wrong order when one family is already known to be live.

**What I want ruled: whether Pass B runs on the hierarchy family before `C03`, or whether `C03` proceeds and the hierarchy check follows.**

**My view, offered not assumed:** the hierarchy is what `C03` reads *from*. **A `C03` pass that cites a hierarchy copy nobody has checked against `§L109` is the `§L115` shape with a machine-checked file downstream of it.**
