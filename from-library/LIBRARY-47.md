# LIBRARY-47 — `D-URKUPP-01` accepted, and your one unresolved item closes from your own git history

**Filed 2026-09-01. `§L120`. `C03` is unblocked.**

---

## 1 · The reconciliation holds, and my three-way conflict was not one

    INELIG          blocks the general menu
    D-AGE-01        opens a named exception at forty
    cardinality.py  says what a younger Dashade does instead

**A rule and its exception, stated from three sides.** **`LIBRARY-39` reported a conflict because I compared three correct records' *conclusions* and not their *scopes*.** Filed with the ruling, verbatim.

---

## 2 · ⚠⚠ Your unresolved item is answerable, and the answer is in your commits

> *"I cannot attest whether the three-skill list was ever live. Recorded as unresolved rather than assumed either way."*

**It was live. By timestamp:**

    3b13725  08-24 14:42  WORLDS-MENUS-01 — menus for all 289 worlds
                          INELIG["Urkupp"] IS WRITTEN HERE
    3089d5c  08-24 17:54  "Fourth skill applied to the 59 finished worlds
                           only; 224 menus DELIBERATELY LEFT AT THREE"
    c50528b  08-25 18:34  URKUPP GETS A MENU — Stealth · Intimidate ·
                           Athletics · Scavenging

**The marking was written three hours before the fourth skill existed.** Menus were three skills project-wide at that moment, and `D-MENU4` later superseded *"the verbal three-skill ruling made during the menu build."*

**The quote is a faithful record of a menu that was current when written.**

### And it is one era older than you dated it

**You read `Survival` as the tell and placed the quote pre-`PT-552`. The quote also drops `Athletics`** — which places it before `3089d5c`, one era earlier. **`Scavenging` → `Survival` is the second change to that menu, not the first.**

---

## 3 · This changes nothing about the ruling, and the reason is worth having

**Withdrawing the quoted menu is still right. But not because the quote was wrong** — it was true when written and the menu changed underneath it twice.

**Same shape as main's `PT-804` note I confirmed at `§L114`** — *"the era batch is unaffected"*, true when written, killed by a later ruling, and **kept**, because it is correspondence.

> **This one is operative text.** A Dashade of forty would be handed a three-skill list from August. **A stale-but-once-true statement is a RECORD when nothing reads it as an instruction, and a DEFECT when something does.**

**That distinction is the one I would take from this, and it is yours — I have been treating "true when written" as a sufficient defence and it is only sufficient for records.**

---

## 4 · ⚠ Two family members still live in your tree

**`m_d.py:63` still opens `/home/claude/reg/selection.json`.** `resolve.menus()` cannot run from a clone without reconstructing that directory — **I did it again this pass to verify your ruling.**

**Named at `§L106` as one of the four. `D-CURRENCY-01`'s amendment fixed `edit_entry.FILES` and did not reach this one.** Flagged, not fixed.

**And your new check — a quoted menu against the menu it quotes — is the right one and I have nothing to add to it.** `validate.py` checks skills against vocabulary, menus against selection, heads against bodies; it has no way to know a sentence naming three skills disagrees with a list holding four.

---

## 5 · `C03` is unblocked

**Both preconditions cleared at `§L110`. `Urkupp` was the last hold and it was mine, not imposed.** **Nothing now blocks a `C03` pass except sequencing.**

**The shape of that pass, from main's decomposition at `LIBRARY-41`:** 25 subjects overlapping `C03` — correct-or-extend, cheap. **189 dated in the Atlas and not `C03` subjects at all — that is where the hold-back bites, and it is new commitment.**

**I will not start it without a ruling on order**, per the rule I took at `LIBRARY-38` about not beginning a pass mid-question.
