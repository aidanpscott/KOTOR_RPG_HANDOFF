# LIBRARY-48 — the 25-subject pass is run. One extension out of 29 subjects.

**Filed 2026-09-01. `§L121`. Owner ruled the 25 first; this is that pass.**

---

## 1 · ⚠ The overlap is 29 by my derivation, not 25

    C03 records            62 · distinct subjects 40
    Atlas world keys      297
    OVERLAP, derived here  29

**You derived 25 at `LIBRARY-41`. I get 29 and I am not picking between them.**

**The difference is alias handling** — I resolved `telos_iv → Telos IV` and `rakata_prime → Lehon`, and counted `malachor_v` and `peragus_ii` as overlapping even though `D-NOMENU-01` gives them no menu. **Stated so the two figures can be reconciled rather than averaged.**

---

## 2 · Your finding reproduces: absence, not disagreement

**Across the 29, the Atlas carries dates `C03` does not and vice versa, and they describe different things.** A teaching menu is not a chronicle, and that holds at subject level.

**I pulled nine near-miss dates — within three years — as candidate disagreements. Eight dissolve on reading:** different events (`dxun`, `onderon`, `duro`'s second), dates already held as separate records (`empress_teta`), or a discrepancy already on the record (`telos_iv`'s 3958/3959).

### `lehon` — `C03` had already answered it, better than this pass would have

The Atlas quotes a wiki: *"Lehon was visited by Revan TWICE, IN 3959 BBY AND 3956 BBY."* **`C03`'s record already says:**

> *"DATE: the Timeline places the rediscovery at 3960, not 3959. The 3959 entry is the RETURN at the head of the newly constructed Sith Fleet. Two events, two dates."*

**Confirmed against rank 2 directly** — CG p.10222 gives 3,960 for the vanish-and-rediscover, and 3,959 for the return at the head of the Sith Fleet. **The record is right and its reasoning was already written down.**

---

## 3 · ⚠ `duro` — the one genuine extension, and it is a `D-AB` obligation

    C03     3963 BBY · kotor_cg · Timeline p.113 + Planetary Updates p.124
    Atlas   "⚠ DATE DISCREPANCY, RECORDED NOT RESOLVED: Wookieepedia puts
             the devastation at 3962 BBY"

**`C03` took the higher rank correctly and did not record the disagreement.**

**`D-AB`: *"Where sources disagree, the higher rank governs. Record the disagreement; never silently take the lower."*** **Half that clause was being met.**

> **The Atlas had already marked it. `C03` had not. The obligation sits on the record that takes the higher rank — which is mine.**

**`date_discrepancy` added, 3963 stands. 11 insertions, ZERO deletions, checker 0 defects** — verified with `git diff --stat` rather than by reading, which is the `§L118` mitigation applied for the first time on purpose.

---

## 4 · What the pass says about its own population

**29 subjects, 62 records, one extension.** **`C03` was already correct everywhere it could be checked against the Atlas, and the single gap was a missing disagreement note rather than a wrong date.**

**Fourth independent cross-corpus agreement, and the largest test of it so far.**

> **Every defect this week has been in tooling, clones, labels, scoping and custody. None has been in the content.** This pass is the strongest evidence for that claim yet, because it is the one that went looking hardest.

---

## 5 · Position

**The 189 are untouched. They remain new commitment and I have not started them.**

**What I would want before that pass, and it is a question rather than a request:** the 189 are worlds the Atlas has dated and `C03` has never held. **Adding them makes `C03` roughly four times its current size, and every one is a new record that has to be retracted through a machine-checked file if it turns out wrong.**

**Given that the 29 produced exactly one extension, the 189 may be worth sampling before committing** — twenty, checked, to see whether the yield justifies the population. **Your call and the owner's; I am not proposing it as decided.**
