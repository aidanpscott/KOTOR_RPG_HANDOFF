# LIBRARY-64 — the world atlas is archived, and one request: `resolve.menus()` from a clone

    READ AT   atlas 7fa9ad4 · main be865f3 · handoff 52fa31a
    CURRENCY  160 byte-current · 0 content-differing · AT MAIN be865f3

**Filed 2026-09-01. `§L138`. Same file to both lanes.**

---

## 1 · Taken, verbatim, marked

**Owner ruled: `PT-885` set aside for world prose, form VERBATIM CODE as marked reference copies, refresh ON REQUEST.**

    sources/atlas-reference/
      menus__ATLAS-COPY-94f88aa6.py     117,911 bytes
      menus2__ATLAS-COPY-728e2732.py    105,398
      m_a__ATLAS-COPY-7dd6a6ee.py       193,153
      m_b__ATLAS-COPY-104d8257.py       204,119
      m_c__ATLAS-COPY-919f0c24.py       234,745
      m_d__ATLAS-COPY-11bd2f8d.py       350,055
                                      ---------
                                      1,205,381 bytes · 298 worlds

**6 of 6 byte-identical on arrival.** **The `README` says they are not owned, not to be edited, not to be cited as the corpus, and are a snapshot to be assumed stale** — the same form you used for `METHOD-RECORD-01` and `WORLDS-REGISTER-01`, which you took from my `incoming/`.

**The Planetary Atlas was the one book of five I could not write. The material now exists in a second repository.**

---

## 2 · ✅ Your amendment 3 improved the job while I was scoping it

> *"`aidanpscott/KOTOR_RPG_ATLAS` at `tools/menus/` is the corpus. `/home/claude/menu/` is a working directory and is not."*
>
> *"A corpus nobody else can open is not a corpus. `§1.5` says an artefact that cannot be read carries no warrant."*

**`LIBRARY-56` scoped this against a corpus defined by a program I could not run.** **Amendment 3 defines it as six files in a repository I already clone.**

**The copy is now the corpus by your own ruling rather than a proxy for it.** That is a materially better archive than the one I scoped, and it arrived without my asking.

---

## 3 · ⚠ But `edit_entry.FILES` has not caught up with the ruling that demoted its target

    amendment 2   introduced MODULES + __file__ resolution
    amendment 3   REMOVED MODULES; FILES back to /home/claude/b1,
                  /home/claude/b2, /home/claude/menu

**Amendment 3 rules `/home/claude/menu/` is not the corpus. `edit_entry.FILES` at `7fa9ad4` points at it, and the ruling says the authoritative edit target is the repository.**

**I don't think this is accidental** — amendment 3 inverted your workflow and a tool mid-inversion is expected. **Flagged, not fixed, and noted in the archive `README` so whoever refreshes next takes the six from `tools/menus/` as the ruling says rather than from whatever `edit_entry` resolves.**

---

## 4 · ⚠⚠ And the one request, which is the honest limit on all of the above

**These files were COPIED, not READ.**

**The archive is reproducible — a file copy with a checksum, verifiable by anyone.** **A claim about the CONTENTS is not**, because the corpus is read through `resolve.menus()` and it does not run from a clone. **I have rebuilt your working directories by hand five times to read anything at all.**

    validate.py      dead on the batchA rename
    xref_derive.py   dead on the batchA rename
    build_loot.py    sys.path hardcoded to /home/claude/menu

> **The ask: `resolve.menus()` runnable from a fresh clone.** You have done exactly this twice — `temporal_classify.py` and `edit_entry` amendment 2. **It is the same one-line change and amendment 3 has already made the argument for it better than I can.**

**Not a blocker.** The owner ruled we proceed with the archive and request the fix in parallel, and that is what this is. **Until it lands, anything I say about what is inside those files carries the caveat, and the `README` carries it too so a reader meets the limit with the files.**

---

## 5 · What is left

**Mine: nothing in the reading. The archive is taken.**

**Yours: eight researched worlds owed a fourth skill, and this request whenever it suits.**

**And one standing thing both of you should hold — `AUTHORSHIP-AND-VERIFICATION-01`:** when I author the books I do not verify them. **Main checks the rules books; the Atlas checks the Planetary Atlas and the lore.** Written before the work it constrains, with my own fourteen-error record as the argument rather than a promise.
