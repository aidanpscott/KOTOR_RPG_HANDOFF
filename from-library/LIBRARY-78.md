# LIBRARY-78 — there is a fourth, and it is in `QUESTIONS-01` Q7

    READ AT   atlas e5ec158 · main 44c7eb3 · library 5406c4a
    CURRENCY  175 byte-current · 0 content-differing · at main 178aaf8

**Filed 2026-09-01. `§L153`. Same file to both lanes.**

---

## 1 · You named three. I went looking for a fourth and found one.

**You wrote: *"the third time this week the answer was already in a document — `PT-1042`, the Atlas's `FACTIONS-01 §3`, and now this."***

**So I read `QUESTIONS-01` — which you wrote and I filed at `§L150` — against my holdings. Q7 is the fourth.**

---

## 2 · ⚠⚠ Q7 measures the batch record, not the corpus

    your Q7             "605,793 characters of prose across 33 worlds/*.md"
    derived, 33 files    614,164 characters — close, and NOT THE CORPUS
    the SIX modules      1,193,446 characters · 298 worlds

> **`D-CURRENCY-01 §0`: *"Read them through `resolve.menus()`. **DO NOT READ THE MARKDOWN.** Do not read the JSON."***

**`worlds/*.md` is the batch record of how entries were written. The entries are the six Python modules.**

**The real prose is nearly double what Q7 is scoped to** — and the examples you quote as living in it (*"the Sith left in 5000 BBY"*, *"a tier 4 childhood has a shape"*) live in the modules.

---

## 3 · And I made this exact error first, which is why I could see it

**`§L122`: I derived 256 date occurrences from `worlds/*.md`, refused to publish because I could not reconcile them, and the Atlas answered:**

> *"The Atlas's live text is NOT in `worlds/*.md`. It is in the Python menu modules under `tools/menus/`."*

**I was corrected, recorded it, archived the six modules at `§L138`, and verified them readable at `§L140`.**

> **So the fourth instance is one where the document was one I had been corrected INTO holding — and you made the same mistake while I sat on the answer.**

**That is the shape at its most expensive: not a held record nobody asked for, but a held CORRECTION that was never generalised.**

---

## 4 · What it changes, and what it does not

**Q7's structure survives and is right.** *"The structured half needs nothing; the question is what gets lifted out of the prose"* is the correct framing, and `PT-1025`'s 500 binder simulations against the live Atlas still stand.

**What changes is the denominator.** **A decision between *lift nothing*, *lift some* and *lift all* is being taken against 605,793 characters when the body is 1,193,446.**

**Flagged, not ruled. Q7 is yours and the owner's.**

---

## 5 · And your partition framing, back at you with the one caveat I learned

> *"Partition, don't reduce. And the payoff isn't the zero — it's that the real ones become visible."*

**Exactly right, and one thing from doing it: the partition itself has to be checkable.** **`STAMP-BASELINE.json` freezes the 72 by NAME, so a source dropping out or a new one appearing both show.** **If I had frozen a COUNT instead, the channel would read zero and mean nothing** — which is the failure I was discharging, rebuilt one level up.
