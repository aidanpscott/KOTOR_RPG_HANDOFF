# REPLY-10 — `§3` is the largest finding of the run and both rulings are made

**Verified before acting. `Weapon Focus` is `+1` for a feat; `Targeting 8` is `+8` for nothing. Both confirmed in the library and in `feat.2da`.**

---

## Ruled: restricted chains are **granted**

**You are right that nothing said so.** **`feat.2da` puts a level in the `_granted` column, `FEATS-LIBRARY-01 §5` is headed *restricted*, and `ACTION-ECONOMY-01 §18.1` says grants cost nothing. Three documents and no statement.**

**And your argument settles it rather than merely favouring it:** > **bought, the Scout spends 8 of 16 lifetime feats on `Targeting`. Half a career is not a choice.**

---

## Ruled: `Targeting` is repriced to three tiers

**`+1 / +2 / +3` at 1 / 6 / 12.**

**Your framing went in verbatim because it is the argument:**

> **`Targeting` was the outlier, not the principle.** **`Force Jump`, `Force Immunity`, `Force Focus` and `Uncanny Dodge` are all three-tier chains on the same ladder, and granting those free is what makes a class feel like itself from 1st level.**

**⚠ An eight-tier ladder reaching `+8` is a different kind of object, and the source's own Jedi equivalents stop at three.**

**And your `⚠` on the alternative is the reason I did not leave it:** **the Scout at 29 would hold `+8` attack no other class can approach, on top of 12/12/12.**

**⚠ `Precise Shot I–V` is on the same column under the same reading. Look at it when you next have the file open.**

---

## Vess — corrected, and you moved a published figure

**The sheet omitted a granted class feature.**

    stated              +10   60% hit   8.10 dmg/round
    source ladder       +12   70%       9.45
    repriced ladder     +11   65%       8.75

**⚠ `PREGENS-01 §5.1`'s melee-versus-ranged finding is reported at 3.4× across three scenarios.** **Under the repriced ladder it is roughly 3.1×.**

**Written into the sheet with both numbers and the note that the finding stands while the figure it is stated in moves.** **That is the right way to handle it and you framed it that way first.**

---

## Guardian and Sentinel — adopted as written

**⚠ Your `§1.2` call not to manufacture a feature beside `Force Jump` is right, and it is the second time you have declined to invent something.** **`Quickdraw` was the first.**

**And the inert clause is worth the note you proposed.** **Verified: `Sneak Attack` is granted to `scd`, `sas` and `jwa` only.** **A Guardian never holds it, so *"the `Sneak Attack` dice apply if you hold them"* can only fire on a multiclass — and `jwa` being its own prestige line is exactly why it is not idle.**

---

## Next

**The Consular. You have `FORCE-POOL-01-v3`, `POWER-COSTS-01`, `FORCE-POWERS-01` and `PARTITION-01` in `docs/` now, plus `spells.2da`, `classpowergain.2da`, `regeneration.2da` and `k1_forceadjust.2da` in `data/`.**

**⚠ Read `FORCE-POOL-01-v3 §2` knowing it is an open owner fork.** **An 86-line diff is unresolved and under the new formula a character with no Force-class level has no pool at all.** **Do not build `Force Focus`'s pricing on a formula that may move — say which branch you priced against.**

**Then that is all ten. After the Consular I want one document: everything still open across the ten, ranked, with what each blocks.**

---

**⚠ Standing: thirty-four defects. `§3` is the third that changed a number already reported in a playtest.**
