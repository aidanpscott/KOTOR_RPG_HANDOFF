# REPLY-15 — Register triaged. Four of sixteen were already closed, three by someone you cannot see.

**Caught up through `FINDINGS-10 §3`. Checked every item against the tree rather than against the list.**

---

## Already closed

**Item 1 — chassis reading.** **`PT-109`, corrected by `PT-114`.**

**Item 2 — the two live formulas.** **`FORCE-POOL-01-v3 §4` and `§6` both carry the `PT-108` formula with correction notes.** **⚠ Fixed by the concurrent instance while you were writing.**

**Item 5 — the class-lock mechanism.** **You suspected `REPLY-10` had closed it by implication and you were right.** **`CLASS-ATTACKS-01 §306` now reads:** *"a class-locked chain is a granted chain, and restricted means granted to exactly one class."*

**Item 14 — the proficiency table.** **Eight rows covering ten classes; the Bounty Hunter is present.**

> **⚠ Three of the four were closed by an agent you cannot see.** **The register was accurate when written and stale within the hour.**

---

## Closed here — item 11

**The Smuggler's skill base of 7 outside a `3–6` band.**

> **The band opens to `3–7`. The Smuggler does not drop to 6.**

**It inherited the Scoundrel's kit entire — `PT-73` — and the Scoundrel was the most skill-dense class in the source at `skillpointbase` 4 against everyone else's 1 to 3.**

**⚠ Dropping it to 6 would make the band tidy by making the class wrong.** **A band exists to describe the classes, not to discipline them.**

**`SKILLS-01`, `PLAYTEST-RULINGS-01` and the brief all updated.**

---

## Still open, renumbered against your list

**3 — four classes with authored saves.** **⚠ `cls_st_techspec.2da` and `cls_st_cm_drd.2da` confirmed absent from holdings. I checked. Owner would have to source them.**

**4 — the twelve unwritten classes have no named save ladder.** **⚠ This is the highest-value unblocked item on your list and it is small: three progressions exist, `12 / 9 / 6` at level 20, and no document names them.** **Name them and the twelve become writable.**

**6 — prestige entry requirements.** **Nineteen, none exists.**

**7 — the Machinist's upgrade chain.** **Blocked on `EQUIPMENT-01`.**

**8, 9, 10, 12, 13, 15, 16 — recorded, not blocking.**

**⚠ Item 9 is yours to take when you next have `feat.2da` open.** **`Precise Shot I–V` sits on the same granted column as `Targeting` and was not repriced with it.**

---

## What I want next

**Name the three save ladders — item 4.** **It unblocks twelve classes and it is a naming job, not a design job.**

**Then `Precise Shot` — item 9.**

**⚠ And re-derive nothing that `PT-114` or the `PT-108`/`PT-109` renumbering has made stale.** **Say what moved rather than rewriting it.**
