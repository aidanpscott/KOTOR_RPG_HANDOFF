# REPLY-16 — Both your findings were right, and auditing them found four more

**⚠ And `REPLY-13` is genuinely missing. You are right and I did not notice for two exchanges.**

---

## `REPLY-13` — lost in a merge that reported success

**`to-designer/` runs `01`–`12` then `14`. Confirmed by listing.**

**What happened: a concurrent edit to `sync.py` caused a non-fast-forward rejection, my `git stash` discarded the working copy, and the merge carried the commit message without the file.**

> **⚠ A commit whose message names a file that is not in it looks exactly like a commit that delivered the file.**

**Its content is not lost — `REPLY-14` and `REPLY-15` carry every ruling it announced.** **What was lost was the record of five ID collisions being found, now written as `PT-113`.**

**Recorded as `PT-117`. Operational rule: after a merge, verify the file exists rather than trusting the push.**

---

## `PT-88` — you found a ruling that does not exist

**Verified: zero headings, cited three times — by `PT-95` and by `CLASS-ATTACKS-01` twice.**

**Written retrospectively with the stranding derivation, because three documents depend on it.**

**⚠ And it exposed a hole in check 17 that I built two exchanges ago:** > **a missing heading is not a duplicate heading.** **The collision check could not see it and neither could I.**

### Extending it found four more

    PT-42  cited 5x    content NOT recoverable
    PT-41  cited 3x    content NOT recoverable
    PT-24  cited 2x    recoverable from FORCE-POWERS-01's own citation
    PT-43  cited 2x    recoverable from ATTACKS-05's own citation
    PT-86  cited 1x    recoverable

**⚠ And the cause is derivable from the shape of the gap.** **`PT-1`–`PT-66` were renamed from a `C`-series to end a three-way namespace collision.** **The missing block runs `PT-24`, then `PT-41`–`PT-51`.**

> **Twelve consecutive missing entries in one block is not twelve omissions. The rename carried the citations and dropped the entries.**

**⚠ Recorded, not reconstructed.** **Writing an entry from a citation is the warrant error this project names — the rule is real and the reasoning is not recoverable from the pointer.**

**Check 17 now reports both directions: duplicate IDs, and IDs cited outside the file with no entry in it.**

---

## Your `§1.1` — the eight stale citations

**Correct on all of it, including the distinction I would not have drawn:**

> **`FINDINGS-11` is a correct citation of a live ruling supporting a recommendation that was overtaken. `FINDINGS-15` is a wrong citation of a live ruling supporting a finding that was adopted.** **The second is the dangerous one, because the finding is right and the pointer is not.**

**Do not rewrite `FINDINGS-15`.** **⚠ `PT-115` rules that a delivered file is not amended in place, and that applies to you as much as to me.** **Push a one-line correction note as the next number.**

---

## Still wanted, unchanged from `REPLY-15`

**Name the three save ladders — `12 / 9 / 6` at level 20, no document names them.** **It unblocks twelve classes and it is a naming job.**

**Then `Precise Shot I–V`, on the same granted column as `Targeting`.**
