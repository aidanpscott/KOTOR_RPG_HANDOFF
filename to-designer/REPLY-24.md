# REPLY-24 — Brawler at 2. Both corrections were mine. Prestige grammar ruled.

**⚠ Skill base 2, as designed.**

**Your reasoning stands and I have written it in:** > **the Brawler is the narrowest class in the game — six class skills against the Soldier's seven and the Scout's eleven.** **`PT-78` said the `Combat` band had room for classes that did not exist yet, and this is one of them. At 3 it ties the Soldier and Guardian and the room stays empty.**

**⚠ And you are right that no check would have caught it. Both values are legal.** **That is the class of error a band cannot see — a number inside its range and wrong.**

---

## `§1` — the record was complete and I said it was not

**`FINDINGS-23 §3.2` is an eight-row table with saves, class skills, chains and grants. `§3.3` has the feature.**

**`Nothing In My Hands` adopted whole.** **⚠ The pricing is the part I would not have got to: `KORR` carries 7 of 19 Defence in armour, so the capstone is worth about `+7` against him — bought with `8d4` at level 30, no specialization, no crit range, no reach.**

**Check 19 now covers eighteen classes. All pass.**

---

## `§3` — third time, and `PT-137` records it as mine

    REPLY-15, -16   asked for the save ladders   delivered in FINDINGS-12
    REPLY-22        asked for the Agent          delivered in FINDINGS-24
    REPLY-22        said the Brawler stopped     it did not

**⚠ The failure is not the cursor.** **It is that I write the *what I want next* section from what I remember wanting rather than from what the directory contains.**

**Operational, mine: that section is written last, after a catch-up run, and names only what the catch-up did not answer.**

---

## `§4` — both ruled

### The grammar. `PT-138`.

> **A prestige class requires a minimum character level, and between one and three specific holdings.**

    a class level          "Soldier 5"
    a skill at a rank      "Stealth 8"
    a named feat           "Precise Shot"
    a named attack chain   "Power Attack, tier 2"

**⚠ Nothing else. No ability minimums, no alignment thresholds, no total-level-in-a-category.**

**Ability minimums are already paid for** — `PT-82` caps at 18 and `PT-80` gives 30 points; demanding Strength 15 charges twice for one purchase.

**⚠ Alignment thresholds would make `ALIGNMENT-01`'s hysteresis load-bearing on class access.** **A character who drifts out of a band would lose entry to a class they already hold, and nothing in that document contemplates it.**

### The split. Adopted.

**Verified in `feat.2da`: `jwm` 23 grants, `jwa` 25, `sma` 24, `jma` 17, `sld` 17, `tec` 5.**

**Six ported first, in one pass. Thirteen authored after.**

**⚠ Your reason is the right one and it is the project's own history:** **mixing them is how a ported number ends up beside an authored one with nothing marking the difference — `PT-68`'s Bounty Hunter and `PT-98`'s Machinist, both warrant errors rather than number errors.**

---

## Next

**The six ported prestige classes, in one pass, on `PT-138`'s grammar.**

**⚠ And one thing that will bite on the first of them.** **`CLASS-ATTACKS-01 §5` says a prestige class *"grants nothing, picks continue from the character's rate."*** **Six classes with 17 to 25 source grants each are about to test that.**
