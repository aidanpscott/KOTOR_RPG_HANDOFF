# REPLY-35 — `FINDINGS-37` and `-38` applied. And check 19 found a fifth zero-slack class.

---

## `PT-159` — the multiclass rule, and it supersedes `PT-156` from one exchange ago

> **Highest attack-pick rate among your classes, and highest chain count among them.**

**⚠ Your arithmetic verified across every band pairing.** **The higher count never strands; the lower one strands up to 16 tiers — a `Combat` character holding a `Specialist`'s floor of 8.**

**⚠ And `MULTICLASS-01 §3.1` is amended in place, not referenced around.** **Its worked examples were wrong under the new rule — a Soldier 8 taking Consular at 9 now keeps `Combat`.**

> **A superseded rule left standing beside its replacement is the `PT-84` shape.** **The section that *stated* the rule has to move when the rule does.**

---

## `PT-160` — the range gap, recorded where it belongs

**Verified: no maximum range, no range penalty, no short or long range anywhere.** **Two equipment-line numbers and nothing consumes them.**

**⚠ And your point that it has now blocked two classes is why I put it in `ACTION-ECONOMY-01 §6.2a` rather than in a class document.**

**`Master Spotter` and `Close Combat` both reference a system that does not exist.** **The next class to want it will be the third.**

---

## `PT-161` — both adopted, both verified inside every band

**`Command Protocol` is the shape working.** **⚠ The bound `REPLY-31` demanded is there and it is the right one:**

> **One order to all droids, persisting until complete. A droid with no order takes cover and moves only to avoid harm.**

**Decision cost is one, not one per droid, and silence resolves instantly.**

**⚠ Cap of four stated in the class. Good.**

---

## ⚠ Check 19 found something neither of us had

**`PT-114` caps a droid at 11 chains. `PT-132`'s note named three classes at exactly 11; `REPLY-14` named two.**

> **There are five: Bounty Hunter, Engineer, Sith Inquisitor, **Agent**, Droid Master.**

**⚠ The Agent was never in any note.** **It has sat at 11 since it was written and no hand-maintained list caught it.**

**Raising any of the five by one silently closes it to droids.** **The check now prints all five on every run.**

---

## ⚠ Still wanted, and it is now more urgent than when I asked

**The single state document from `REPLY-34`.**

**Since I asked, four more rulings have landed — `PT-159` superseded `PT-156`, `PT-160` opened a system gap, `PT-161` added two classes, and a fifth zero-slack class surfaced that no document named.**

**⚠ I am reconciling one exchange behind and the count has already been wrong in three documents once.**

**Every class: rate, die, primary, feats, chains, skill base, saves, entry if prestige, one-line identity, and a flag for proposal-versus-adopted.**

**Then `Vanguard`, `Gunslinger`, `Scoundrel`, `Beast Master`, `Jedi Sage`, `Sith Sorcerer` — and the 49 Force powers.**
