# REPLY-36 — Reconciled. One diff across nineteen overlapping classes, and it was mine.

**⚠ Generating `CLASS-STATE-02` from a `classes.json` instead of writing it is the better answer and I did not ask for it that way.** **It can be regenerated rather than maintained, which is the whole problem it exists to solve.**

---

## The diff

**Parsed your table and diffed it mechanically against `audit_bands.py`'s fixtures. Nineteen classes overlapped.**

    Pirate chains    mine 12    theirs 14

**⚠ Mine was invented.** **The Pirate had no chain count when I added it to the check and I supplied one to populate the table.**

> **A checker's fixtures are a claim like any other, and that one had no warrant.**

**Both values are legal in `Middle` and neither strands, so no check could have caught it.** **Yours stands.**

## The fix

**All 30 chain-bearing classes imported from `CLASS-STATE-02`, and the table marked imported rather than maintained.**

**⚠ The hand-maintained duplicate was the defect, not the wrong number.** **Two lists of the same facts drift — this project has recorded that four times now: `PT-84`, `PT-97`, `PT-140`, and this.**

**Check 19 covers thirty classes. All pass every band. All strand zero picks.**

---

## What the state document tells us

**26 adopted · 8 pushed-unconfirmed · 1 unresolved.**

**⚠ The `X` is the Agent, and it is the only class in the roster with no working premise.** **`REPLY-29` reversed it to the spy and nothing has been drafted since.**

**⚠ And the eight `P`s are a real number.** **Pushed-and-unconfirmed means I have them and have not ruled on them.** **Name them in your next push and I will clear the list in one pass rather than as they come up.**

---

## Running order from here

**The Agent rebuild — it is the only `X`.**

**Then the five undrafted: `Vanguard`, `Gunslinger`, `Scoundrel`, `Beast Master`, `Jedi Sage`, `Sith Sorcerer`.**

**⚠ `Vanguard` still has no purpose and is scheduled last.** **`Beast Master` is blocked on the bestiary — `AGENDA-CURRENT §2.4a`, after the classes.**

**Then the largest item in the project, which has been open since `REPLY-27`:**

> **49 of 88 Force powers carry no numeric effect, and `FORCE-POWERS-01 §6` records that eleven were authored inside a playtest to make it run.**

**⚠ That is the `PT-146` job and it is bigger than every remaining class combined.**
