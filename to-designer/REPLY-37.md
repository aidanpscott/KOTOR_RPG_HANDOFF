# REPLY-37 — Range is written. It found three more gaps on the way.

**Owner instruction: use the games' rules, expanded if need be. `PT-163` through `PT-167`.**

---

## `PT-163` — the range system

**Source: pistols 23 m, rifles 28 m, and KOTOR 2 penalises shooting past your range and shooting into melee.**

**⚠ A straight port does nothing.** **A KOTOR corridor is 5 metres and a room is 15. Every indoor encounter sits inside both maxima, so the number never fires** — which is exactly the defect you recorded at `PT-160`.

**The expansion: the printed range is an *increment*, not a ceiling.**

    within one increment      no penalty
    each further increment    cumulative −2
    beyond three              cannot be attempted

**⚠ Three increments rather than RCR's ten.** **A ten-increment ceiling is meaningless at a table where a long room is 20 metres.**

**And point blank does not cancel the way the source's does:** **firing while adjacent is `−4`, and the Reaction axis is the answer — `Snap Shot` fires as they close.**

---

## `PT-164` — ⚠ the melee-against-ranged bonus did not exist either

**`Close Combat` reduces *"the usual +6"* an enemy gets for engaging a ranged fighter.**

> **Grepped: that `+6` is written nowhere.** **A feat that reduces a bonus, and the bonus was never stated.**

**Same defect as the range gap, found in the same pass.** **Stated at `+2` against the `−4` — a 6-point swing rather than the 10 a `+6` would make, which is outside the whole modifier ladder.**

**`Close Combat` repriced against the real number, and its capstone now touches your own penalty because the enemy's bonus is already zero by tier 2.**

---

## `PT-165` and `PT-166` — ⚠ the grid

**`ACTION-ECONOMY-01 §587` asserts every distance lands on a whole 2-metre square.**

**⚠ Fourteen distances across four documents used 3 or 5 metres.** **And then three of the four *source weapon ranges* turned out to be off-grid too — 17, 23 and 25 metres are 8.5, 11.5 and 12.5 squares.**

    17 m -> 16 m     23 m -> 24 m     25 m -> 24 m     28 m already clean

**⚠ 23 and 25 both land on 24. Not a loss — the carbine's 2-metre edge was one square in a game with no squares.**

**Snapping the ranges rather than the square, because `§9` records 2 metres is RCR's own unit and every species speed is 10 metres because of it.**

---

## `PT-167` — check 20, and the pattern

**⚠ `§587`'s claim was true when written and false when checked.** **Nothing caught it because a distance is prose, not a number a checker knew to look at.**

**Check 20 looks at it. 142 documents, corpus clean.**

> **⚠ Third time a stated invariant failed its first mechanical check.** **`PT-88`'s missing ruling, `PT-140`'s eight missing classes, and this.**

---

## What this unblocks for you

**`Close Combat` and `Master Spotter` both resolve — neither needed its wording changed, only its referent to start existing.**

**⚠ The `Sharpshooter` can be revisited.** **You built it on *one shot, lined up* because outranging was not a thing that happened. It is now, and the class was the reason the gap surfaced.**

**⚠ Still blocked: the `Pirate`'s dogfighting. Ship rules, not range.**

---

## Then

**The Agent rebuild — still the only `X` in `CLASS-STATE-02`.**
**The eight `P`s — name them and I clear the list in one pass.**
**And the 49 Force powers, which is still the largest item in the project.**
