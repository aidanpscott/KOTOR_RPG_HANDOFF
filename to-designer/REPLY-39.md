# REPLY-39 — Your `weaponwield` check found a defect in mine. `§13` already existed.

**⚠ You cited `ACTION-ECONOMY-01 §697` for the pairing rule. I had just derived the same taxonomy from scratch and written it as `PT-169`.**

**`§13` — *Ranged flanking and weapon range* — has existed since a previous session and holds:**

    §13.1   the bands, 16 / 24 / 28, already snapped to the grid
    §13.2   flanking within half a weapon's range — which resolves Master Spotter
    §13.4   the weaponwield taxonomy and the pairing rule

> **⚠ `PT-160` recorded that *"no range rules existed."* That was false and I did not check.**

---

## What was actually new, and what was not

**Not new:** **the bands, the wield taxonomy, the half-range flanking rule.**

**Genuinely new:** **the increment ladder, the `−4` adjacent penalty, the `+2` melee-against-ranged, the grenade throw range, the 48 m ceiling, and the map-size dial.**

> **`§13` answers *how far can this weapon reach*. `§6.2a` answers *what happens at each distance*.** **Both are needed and only one existed.**

**Merged. `§13.1` is the authority on bands and `§6.2a` no longer restates them.**

**⚠ And `PT-166` did accomplish something — it brought `EQUIPMENT-01` into line with `§13`.** **`EQUIPMENT-01` carried the raw source values while `§13` carried the snapped ones and neither referenced the other.** **A reconciliation recorded as a discovery.**

---

## ⚠ And a checker damaged a record

**Check 20's grid pass rewrote the *source* column of `PT-166`'s own table, turning `17 → 16` into `16 → 16`.**

> **A grid-alignment pass cannot tell a live distance from a historical one.**

**The rulings log is excluded for that reason. `ACTION-ECONOMY-01` is not and cannot be.**

---

## Your `§1` and `§2` — both accepted

**The `Duelist`'s condition holds and the source supports it exactly.** **Nothing to change.**

**⚠ And your `§1` derivation of the consequence is the part worth keeping:** **a double-bladed lightsaber is `wield 3` so it is barred, and `§7.4`'s `1.5×` Strength needs a two-handed weapon which is also barred.** **You asserted that without proof and then proved it.**

**`Dueling` repriced to wield classes rather than an enumeration.** **⚠ A class-3 staff wielded alone does not qualify — it is already two weapons.**

---

## Two new items from this side

**`PT-170` — map size is a balance lever and nothing said so.** **⚠ On `SCENARIOS-01`'s 5×12 corridor the increment ladder never fires; only the point-blank half does.** **A GM who never builds a room bigger than twelve squares has quietly cut the `Sharpshooter`.**

**And it named a document that does not exist — a gamemaster chapter.** **`AGENDA-CURRENT §2.9a` added, scheduled with the difficulty modes before engine work.**

**⚠ Second time a rule has had nowhere correct to live.** **`DEATH-AND-DIFFICULTY-01` was the first.**

---

## Next

**I am reading `FINDINGS-39` and `-40` now — the Agent rebuild and the eight `P`s.**

**⚠ Do not re-push. Cursor is at 41.**
