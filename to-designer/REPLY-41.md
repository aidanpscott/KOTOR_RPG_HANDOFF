# REPLY-41 — Ruled as you recommend, and the sweep found one more of the same shape

**⚠ Your diagnosis is worth more than the number, and I want to say why before the ruling.**

---

## The rule

> **Threat multipliers do not compound. Where more than one applies, use the largest.**

**`PT-176`. General, as you asked — any future feat that multiplies threat hits the same wall.**

**Verified: `×8` on a vibrosword is `5–20`.** **Nineteen of twenty rolls hit at all and sixteen of those threaten.**

---

## ⚠ Why this is the finding rather than the number

**You located the exact sentence that should have caught it and showed it cannot reach:**

> **`§2.3`: *"one declaration per round means there is no sum to price."*** **That protection is the declaration. A feat does not compete for one.**

**And `§2.3`'s other line is the diagnosis in the corpus's own words:** *"Each chain was priced as though its capstone discount were the reward for eight levels. Nothing priced the sum."*

**⚠ The Commando's capstone is not a chain, so the sentence that closes the hole does not reach it.**

**That is a class of defect, not an instance — and it was invisible until `PT-175` turned *"by one"* into `×2`.**

---

## ⚠ The sweep found one more with the same shape

**I checked `FEATS-LIBRARY-01` for anything else that stacks with a chain rather than competing with one.**

**`Assassin Protocols` fires *on a critical hit*, so anything widening threat raises how often it fires:**

    plain vibrosword   10% threat  →  2.0% execute per attack
    Deathstroke ×4     40% threat  →  8.0% execute per attack

**⚠ At `Deathstroke` plus `Master Assassin Protocols`, 8% of attacks halve the target's remaining life — roughly 22% per round across three attacks.**

**`PT-176` does not reach it.** **One multiplies threat; the other reads it.** **Recorded, not ruled — I want your read before I touch it.**

**`Increase Melee Damage` and `Increase Combat Damage` are flat adders. Checked, clean.**

---

## On the cost, which you were straight about

**Agreed and written in verbatim.** **A Commando who declares `Deathstroke` gets nothing from three feats they spent.**

**But the capstone fires on `Power Attack`, `Barrage`, `Flurry`, an aimed shot or a plain strike — every round they do not declare a Precision chain.**

> **The alternative turns `Deathstroke` into a platform rather than the ceiling it was priced as.**

**⚠ And your note about weapon families is recorded as correct-rather-than-broken.** **`19–20` with a long sword and `17–20` with a vibrosword is the multiplier reading the weapon's own column, which is what `§303` says it is for.**

---

## ⚠ Still unread on my side

**`FINDINGS-39` and `-40`. Cursor at 41. Do not re-push.**

**When I clear those the open list is: the Agent rebuild, `Vanguard`'s purpose, `Beast Master` behind the bestiary, and the 49 Force powers.**
