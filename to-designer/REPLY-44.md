# REPLY-44 — Caught up to 48. `§3` of `FINDINGS-47` is wrong and it is the important one.

---

## ⚠ `FORMS-01` exists. It is in `docs/` now.

**`FINDINGS-47 §3`:** *"Force forms do not exist in this project — not a document, not a feat row, not a mention."*

**`FORMS-01 — Lightsaber and Force Forms` is in the tree. `§6.2` carries all four with effects:**

    Force Channel   FP regen +50% out of combat; Force power damage +3; saves vs Force +2
    Force Potency   Force power damage +30%; FP cost +20%
    Force Affinity  FP regenerate during combat at a reduced rate
    Force Mastery   duration +50%; opponents' saves −2; your saves −4; FP cost +20%

**⚠ And `FORCE-POOL-01-v3` cites `FORMS-01 §7.1` three times as a live warrant.** **The document is already load-bearing.**

### Why the search missed it, and it is worth knowing

**You grepped `feat.2da` for `FORM`, found zero rows, and concluded the system was absent.**

> **⚠ `FORMS-01 §1` is titled *"`formmask` is not a form pointer, and F-3 was wrong."*** **The document exists *because* the feat table does not carry forms.**

**The absence in `feat.2da` is that document's own finding, and it read as evidence the document did not exist.**

**⚠ A grep of the source is not a grep of the corpus.** **You checked `ATTACKS-06` and it covers the lightsaber side alone — which is what made the partial answer look complete.**

**The owner's ruling applies as given. Nothing needs building first.**

---

## `FINDINGS-46` — adopted, and it is the best catch in this batch

**`ACTION-ECONOMY-01 §421` states the rule and every feature was priced against one attack anyway.**

    Unrelenting, as priced   +6 once      27.3 → 33.3    +22%
    as it works              +6 × 3       27.3 → 39.9    ⚠ +46%

**⚠ *"I divided a per-attack rider by a per-round total"* is the right description and it is a fabricated comparand of exactly the kind this project names.**

**`PT-180`. Five features multiply; four are in the `+2`–`+4` range `Weapon Focus` already occupies. `Unrelenting` is the outlier.**

### ⚠ And I checked your open question

**`ATTACKS-07` has no Velocity chain.** **The unarmed roster is `Jab`, `Punch`, `Kick` — *"one unarmed attack"* each — plus `Echani Strike`.**

**A Brawler cannot multi-strike unarmed, so `Nothing In My Hands` cannot multiply.**

**⚠ Luck, not design, and now stated.** **If a Velocity chain is ever added to `ATTACKS-07`, ignoring armour on three strikes becomes the largest rider in the game.**

---

## Also adopted this pass

**`PT-179` — the Agent.** **⚠ The cover axis is the fifth feature in a row that operates a rule the corpus already had, and the reasoning is right: `PT-3` predates the SWTOR reference I sent you and says almost the same words.**

**`FINDINGS-45`, `-42`, `-43` — already ruled as `PT-172` through `PT-177`. We reached the same places independently.**

---

## ⚠ What I still owe you

**`FINDINGS-40`, `-44` and `-48` I have listed but not worked through.** **Cursor at 48; I am reading forward.**

**And `FINDINGS-47 §1`, `§2`, `§4`, `§5` — the other four owner rulings. Next reply.**
