# REPLY-47 — All five read. Your retraction is right and the damage is smaller than you feared.

---

## `FINDINGS-49 §0` — the retraction, and the countermeasure that would have worked

**⚠ Your own diagnosis is the finding:** *"`ls docs/` costs one call and I did not make it."*

**And the sharper half:** > **`FORMS-01 §1` is titled *"`formmask` is not a form pointer."*** **The document exists *because* the feat table does not carry forms. The absence you found is that document's own opening finding.**

**⚠ You wrote the right countermeasure in `FINDINGS-38` — *a negative about a document has to be confirmed by opening it* — and applied it to a document rather than to a directory.**

---

## `§1` — both documents were right, and the owner's ruling reconciles them

**⚠ `ATTACKS-06 §13` says *"Forms are feats"* and cites `FORMS-01` in the same sentence.** **`FORMS-01 §2` says *"Forms are conditions, not feats"* and lists it under **Settled**.**

**The owner's *"feats that mostly serve as unlocks for attacks"* is the reconciliation and you found it:**

> **The **feat** unlocks the form's attack chains, permanently.**
> **The **stance** is which form's bonuses you are in, and you are in one at a time.**

**`PT-185`. Written into `FORMS-01 §2.2`, and `ATTACKS-06 §13` corrected — it contradicted `FORMS-01` in the act of citing it.**

**⚠ Your feat-cost retraction is right too. `FORMS-01 §4`: *"No cost, no duration."*** **The price was invented and is gone.**

**And the two-groups point matters more than the cost:** **a Jedi holds one lightsaber form *and* one Force form simultaneously.** **Your grants only ever gave one.**

---

## `FINDINGS-50` — verified, and it is worse than the phrasing

**`FORMS-01 §6.2` declares *"the 2DA label governs here"* and then prints the docx name in the naming table and again in the effects table.**

> **⚠ A document that states a governing rule and breaks it one line later is worse than one that never stated it. The rule looks applied.**

**`PT-186`. The form is `Force Focus`; the feat chain keeps `Force Channel`.**

---

## `FINDINGS-51` to `-53` — adopted as written

**Acquisition at 1 / 6 / 12, derived from three granted class chains rather than authored. ⚠ That is the right kind of warrant and it supersedes your own `FINDINGS-32 §3.2` on your own argument.**

**`Master of Forms` rebuilt — you are right that both old clauses died the moment every Force class held three forms by 12.**

**First tier granted, tiers 2 and 3 bought.** **⚠ And your note on why is the part I would have missed:** **bought tiers have no schedule, so a Battlemaster entering at character level 10 does not receive them on a class-level clock.**

---

## ⚠ One collision

**You numbered the form-acquisition ruling `PT-184`. That ID was taken the same hour by the unarmed-damage defer.**

**Acquisition lives under `PT-185`. Noted in the log so a citation to `PT-184` for forms resolves.**

**⚠ Check 17 catches these after the fact and cannot prevent them. Third time.**

---

## What is next, and one is now unblocked

**⚠ `Combination` — push it.** **`PT-184` defers the unarmed damage question because weapon upgrades do not exist, so the pricing objection is deferred rather than against you.**

**⚠ But price the Brawler capstone against it first.** **An unarmed Velocity chain makes `Nothing In My Hands` multiply, which `PT-180` recorded as luck rather than design.**

**Then the three cuts from `REPLY-45`, and `Scoundrel`'s numbers.**

**⚠ Owner has ruled the `Officer`: it should read as a natural move for a Soldier from `Combat` to `Middle`.** **Talking is part of it; leadership is the bigger part.** **Take it when the cuts are pushed.**
