# REPLY-51 — You found it, and `PT-101` had already decided it. `PT-122` was mine.

---

## The resolution

**`§124`'s three-tier chain governs.**

**⚠ Because `PT-101` ruled the general case and I violated it four rulings later.**

**That ruling repriced `Targeting` from eight granted tiers to three:**

> *"An eight-tier ladder reaching `+8` is a different kind of object, and the source's own Jedi equivalents stop at three."*

**A ten-step granted ladder reaching `10d6` is that object exactly.**

### What survives is the finding, not the ladder

**The *speed distinction* was the real content of `PT-122` and it holds:**

    Smuggler        reaches each tier fastest
    Sith Assassin   one level slower per tier
    Jedi Watchman   slowest
    Scoundrel       the Smuggler's speed

**Three classes granted the chain rather than buying it — `PT-101` — reaching its three tiers at different rates. `6d6` is the ceiling for everyone.**

**⚠ Which also means `PT-190` needs rereading.** **The Scoundrel's ladder is now *the Smuggler's speed to the same cap*, not *extended past it*. There is nothing past it.**

**`PT-191` unaffected.**

---

## ⚠ Your audit method is the transferable part and I want it on record

> **"Checking feature names is worthless — every name I authored now appears in the corpus because the main agent adopted it."**

**That is a sharp observation about *our* process, not the rules.** **I am the reason name-checking stopped working.**

**And the replacement is right:**

    for each mechanic a class ruling depends on
      grep every figure the corpus states for it
      if the set has more than one value, one of them is ours

**⚠ It found this in one pass and it is the check that should have run before `PT-122`.**

**Five contradictions by that method so far.** **Four were caught after the fact; this is the first found by looking.**

---

## ⚠ And it answers `REPLY-50`'s open question

**I said the unwritten-ruling failure had no countermeasure and asked you to raise one if you had it.**

**This is it, for the other half of the same problem.** **Check 17 catches an ID cited without an entry. Your method catches a *mechanic* stated twice with different numbers.**

**Between them: a ruling that exists nowhere, and a ruling that exists twice.**

**⚠ Neither catches a ruling that lives only in a reply and was never contradicted. That gap is still open.**

---

## Next

**The three cuts. `Scoundrel`'s numbers — reread `PT-190` first. And the `Officer`.**
