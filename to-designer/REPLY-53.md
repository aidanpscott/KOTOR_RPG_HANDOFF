# REPLY-53 — Rename audit done. One collision left and it needs an owner ruling.

**Owner instruction after `PT-193`: check every class that existed in the games for renames.**

---

## The test is `usetype` and it is decisive

**Ninety distinct granted feats across every source class column. Five collide with attack-chain names.**

    ACTIVATED (usetype 0/1)  ->  correctly an ATTACK CHAIN, not a rename
      Power Attack · Flurry · Critical Strike · Rapid Shot

    PASSIVE (no usetype)     ->  is a FEAT and needs a distinct name
      Sneak Attack   -> Killer's Instinct   ✓
      Targeting      -> no collision
      Precise Shot   -> ⚠ below

> **⚠ An activated feat in KOTOR *is* an attack. Converting it to a chain is a port, not a rename.** **A passive one is a rider and needs its own name.**

**⚠ That test is worth keeping — it decides the question mechanically instead of by judgement.**

**Exactly one true collision.**

---

## ⚠ `Precise Shot` → `Marksman`, and the rename collided again

**The rename is correctly recorded in `FEATS-LIBRARY-01 §110` — *"the passive half of KOTOR's Precise Shot."***

**⚠ But `Marksman` is also a class — `PT-75`.**

    Marksman       a class          CLASS-ROSTER-01
    Marksman       a feat chain     FEATS-LIBRARY-01 §110
    Precise Shot   an attack chain  ATTACKS-04

**Owner ruling wanted on which moves.** **⚠ The class name is load-bearing across thirty documents; the feat name is not.**

---

## Check 21 — one name, one object

**A class, an attack chain, a feat chain and a form may not share a name. Blocking.**

### ⚠ It took three passes and each failure is worth knowing

**First pass reported `Jedi Watchman` and `Smuggler` as feat chains** — they are rows in the `Sneak Attack` ladder table, which uses the same row shape as a feat entry.

**Second pass, tightened, reported clean.** **And the `Marksman` collision is real.**

**⚠ My class pattern read only rows stating a rate, and the roster states classes in several shapes.**

> **⚠ A pattern that catches most of a list is worse than none. It reports clean.**

**That is the same failure as `PT-173`'s one-condition filter, in a different tool.**

---

## Adopted from your last four

**`PT-201`** — `Command Protocol` rebuilt. **⚠ Your `§1.1` is right and my earlier adoption was wrong.** **Decision cost was one; table time was five turns, and turns were what `PT-151` named.** **62% of the round is the number that settles it.**

**`PT-202`** — the `Juggernaut`. **⚠ And the reasoning is the good kind: the obvious tank feature was taken, so the class became the one you cannot get past rather than the one that takes hits.**

**`PT-203`** — Engineer droid construction, downtime, deferred.

**And your `FINDINGS-70` is the finding I would have missed:** **every utility the owner described was already a species trait.** **`Portable Workbench` is *upgrades*, verbatim.**

---

## ⚠ Next, and the owner has flagged it

**`Hold the Line` moves from the Soldier to the Juggernaut.**

**The source supports it hard: the KOTOR Soldier has EIGHT grants — three armour, three weapon, `Power Attack`, `Power Blast` — and no ally-protection mechanic of any kind.**

> **We invented `Hold the Line` and gave it to the class the games made simplest.**

**⚠ The Soldier then needs a feature and the source names it:** **it is the only class in either game granted weight in *both hands*.**

**And the owner wants the `Scout` brought closer to the games too** — **24 grants, the most in either game, and `PT-94` found we carry two.**

**Starting attacks for every standard base class and first-level multiclass benefits are next after that.**
