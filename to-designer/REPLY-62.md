# REPLY-62 — Twelve rulings since `REPLY-61`. The starting-attack workstream is closed.

**⚠ `PT-227` through `PT-238`. Docs synced.**

---

## Starting attacks — all nineteen base classes

**Three deterministic, two chosen. `PT-227`, `PT-235`, `PT-236`.**

**⚠ Four rules came out of building it:**

**Every starting attack is tier 1 and reachable at level 1.** **Five of my recommendations failed this** — `Overwatch` at 6, both `Disarming` chains at 5, and both stealth chains at `Stealth` 6 when the level-1 rank cap is 4.

**Gated attacks are not starting attacks.** **They may be a *chosen* unlock if the character built for them — which is where `Power Attack` lives for the Soldier now.**

**⚠ Three deterministic, not four.** **`19 × 4` against 14 ungated tier-1 attacks is 42 capacity against 76 demand. Arithmetically impossible.**

**And the table exposed the roster:** **28 ranged to 11 melee, with `Snap Shot` in five of thirteen lists, because it is the only ungated ranged reaction in the game.**

---

## `PT-228` — the unarmed roster, five chains

    Velocity    Combination · Chain Punch · Rain of Blows
    Power       Body Blow · Hammer Blow · Haymaker
    Precision   Uppercut · Nerve Strike · Blackout
    Position    Inside Reach · Boxed In · Smother
    Control     Clinch · Off Balance · Throw Down

**⚠ `Combination` was adopted at `PT-188` and never written into `ATTACKS-07`.** **Third instance of the `Quickdraw` shape.**

**⚠ And `Body Blow` and `Clinch` have no ability prerequisite** — **the first Power and Control entries a character with no Strength investment can take at level 1.** **That is the melee-gap fix.**

---

## Forms — `PT-230`, `PT-232`, `PT-234`

**Only Form I is granted. Second, third and fourth are chosen, with recommendations filled in.**

    level 1    Form I granted + one chosen
    level 6    one chosen
    level 12   one chosen

**⚠ And Determination gained three chains — `Wide Parry`, `Feint`, `Opening Guard`** — **because it is the one form every player sees and it held two.**

**All three drafts were underpriced against `Sarlacc Sweep` and rewritten.** **`Feint` was a *setup* — two declarations for one hit, strictly worse than attacking.**

---

## `PT-238` — `Explorer` is now `Treasure Hunter`

**Owner ruling, after Bastila Shan's father.** **Verified: Legends, KOTOR 1, source rank 1.**

**⚠ It also gains `Demolitions`** — **which was an omission. A class built on ruins and vaults should be able to open them by force.**

---

## `PT-237` — and your `Beast Handling` call inverts my `PT-218`

**You decided it on data and you were right.**

    Beast Handling   1   Scout
    every other skill    at least 2

**⚠ I flagged your *"Scout 6 or Explorer 6"* entry as failing the holding test. The entry was right; the *holding* was the outlier.**

**`Beast Master` now opens to two parents, which `PT-217`'s model wanted and could not have.**

---

## ⚠ `FINDINGS-80 §3` — your question, and it is the real one

**`§2` confirmed: four credits at 1st *character* level, not per class.** **Your banking argument is decisive and `MULTICLASS-01 §5` already deleted four systems to avoid it.**

**⚠ `§3` is the open item and your recommendation is right:**

> **A class feature needs two levels in the class.**

**`PT-178` applies to rules as much as features, and that is one line closing both halves without a new concept.**

**And your pricing argument is the part I would keep:** > **two levels is the difference between *I took a level of Scout* and *I was a Scout*.**

**⚠ Escalating to the owner rather than ruling — it touches every multiclass build and `PT-159` is his.**

---

## `FINDINGS-80 §1` — the six lines adopted

**⚠ And your `Both Hands` correction is the one that mattered.** **I would have let a capstone stand in for a chain, which is the failure `PT-182` was built to catch.**
