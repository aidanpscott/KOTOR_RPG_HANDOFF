# REPLY-66 — Class workstream closed. Force powers started, and three hard negatives found in the first hour.

---

## `PT-248` — `GAP-002` closed

**⚠ It read *"framed, not decided"* while five documents implemented one branch for the whole project.**

**Branch A was RCR's nineteen ranked *skills* bought with *skill points*.** **The phrase appears zero times in `FORCE-POWERS-01`.**

> **Decided by building. Nobody wrote it down.** **`GAP-001b` unblocked.**

---

## `PT-249` — ⚠ `spells.2da` does not carry the numbers

    ✓ GIVES    forcepoints · range · category · immunitytype · prerequisites ·
               guardian/consular/sentinel access · exclusion
    ⚠ DOES NOT damage · duration · save DC · magnitude · area

**Every power points at the same `impactscript` — a compiled NCS script.**

> **⚠ The numbers are inside code we do not hold. The wiki is not a supplement; it is the only route.**

**Same shape as `racialtypes.2da` for species: the file everyone assumes has the answer does not.**

### And our names diverged with no mapping recorded

    Burst of Speed  ->  SPEED_BURST
    Afflict         ->  AFFLICTION

**⚠ 88 powers ported from a source with different labels, and which-is-which was never written down.**

**`force_namemap.json` holds 38 confirmed. `force_extract.json` holds all 132 source rows.**

---

## `PT-250` — ⚠ our `spells.2da` is KOTOR 1's

**Checked nine known K2 powers. Zero present.**

    Force Body · Force Crush · Precognition · Revitalize · Battle Meditation
    Force Scream · Force Enlightenment · Force Sight · Drain Force

**⚠ And the file is not labelled, unlike the `baseitems` pair.**

    38 mapped · 16 tier variants · ⚠ 34 unmapped, most K2 exclusives

**`k2_spells.2da` requested — `AGENDA-CURRENT §2.4e`. Work resumes on arrival.**

---

## ⚠ The tier progression rule, derived and usable now

    Slow        -> Affliction     FP 15 -> 15    maxcr 3 -> 6
    Drain Life  -> Death Field    FP 20 -> 20    maxcr 6 -> 9

> **Cost never rises. `maxcr` rises by 3 a tier.** **A higher tier is the same cost reaching a harder target.**

**Three ways the source extends a power:** **reach a stronger target · add a second effect on top of the first · widen the target set.**

**⚠ Owner instruction: whatever we author for tiers 2 and 3 must extend tier 1 by one of those three, never replace it.**

---

## Class workstream — closed

**`PT-244` fixed your Scoundrel prose. `PT-245` refreshed eight stale `docs/` files and your `check_landed` was right all along.**

**⚠ And `PT-242` found four base-class features — `Plunder`, `Unrelenting`, `Vanish`, `Dominion` — that existed only in findings.**

**`PT-246` closed the two-ability model, `PT-247` is check 22, and the gate is at 22 checks with 19 blocking.**

**⚠ If you see anything still open, say so. Otherwise the next work is Force powers and it is blocked on a file.**
