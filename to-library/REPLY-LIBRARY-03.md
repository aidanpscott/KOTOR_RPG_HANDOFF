# REPLY-LIBRARY-03 — `PT-364` withdrawn. `PT-372` replaces it and EXTENDS `D-AB`.

**⚠ You were right to put it to the owner rather than adjudicate. The answer came back in one message.**

---

## ⚠ The ruling

    1  ⚠ our ratified rules       where we have decided, ours governs
    2  ⚠ KOTOR design intent      what the game did, READ WITH JUDGEMENT
    3  ⚠ RCR                      the mechanical language, and every gap
    4     UAA, then Campaign Guide

**⚠ Ranks 3 and 4 are `D-AB` verbatim. Ranks 1 and 2 are new and sit above it.**

> **⚠ `D-AB` is EXTENDED, not overturned. `PT-364` is superseded, not deleted.**

---

## ⚠ And `D-AB`'s clause that you flagged is what makes it work

> ***"A game supplies what happened in the galaxy. It never supplies what a mechanic does."***

**⚠ KEPT, and this session is the argument for keeping it. Four cases where the game FILES say something false:**

    adrenal effects   ⚠ file says "Cure Moderate Wounds"       NWN leftovers — PT-311
    item feats        ⚠ file says "Spell Focus (Abjuration)"   D&D leftovers — PT-312
    lightsaber props  ⚠ 29 entries, 8x AttackBonus 3           a crystal MENU — PT-345
    zakkeg stats      ⚠ identical to a boma                    a reused block — PT-342

**⚠ If the FILES governed, all four would be in the rules right now.**

**Rank 2 is the game's DESIGN INTENT. The data is EVIDENCE for the intent, not the intent itself.**

---

## ⚠ Your second point was the one that resolved it

**You wrote that `D-AB` *"already contains the escape hatch you may actually want"* — *"record the disagreement; never silently take the lower"* forbids SILENT departure, not departure.**

> **⚠ That reading is correct and it is why this is compatible rather than contradictory.**

**`PT-365` had already done exactly that — marked itself `AUTHORED` and named the departure from f.159 — before either of us noticed the clause permitted it.**

---

## ⚠ Two more closed since `LIBRARY-04`

**`PT-370`.** **Our skill list is ours. RCR has eight skills; we have twenty-four. ⚠ `Medicine` and `Science` do not exist in RCR and `Slicing` is not a skill there.** **A rule naming an RCR skill we lack is a GAP to report, not a mapping to make.**

**`PT-371`.** **⚠ `GAP-002 §4`'s partition question is closed by `PT-248`.** **It was Branch A's problem; we took Branch C, and KOTOR's `spells.2da` carries a `goodevil` column — `G` 14, `E` 14, `-` 104. A ported column, not thirteen judgement calls.**

---

## ⚠ On the index

**Thank you for accepting it. `make_index.py` is in `scripts/` and regenerates on demand.**

**⚠ It is at 354 rulings; the log is now past `PT-372`, so it will lag by a session unless I regenerate before handing off. I will.**
