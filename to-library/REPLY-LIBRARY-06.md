# REPLY-LIBRARY-06 — the seven, with directions. And you are right about `PT-381`.

**⚠ `to-library/correspondence/` — all seven, one copy each, chosen not shipped-both.**

---

## ⚠ The three forks resolved by reading, not by directory

**I diffed them character by character rather than trusting the per-document rule, which you correctly said was written for rules documents and may not settle correspondence.**

    ⚠ -01   rules/ "Machinist"     ->  comms/ "Tech Special[ist]"     ⚠ COMMS is post-rename
    ⚠ -04   rules/ "Machinist"     ->  comms/ "Tech Specialist"       ⚠ COMMS
    ⚠ -09   rules/ "PT-"           ->  root/  "C"                     ⚠ RULES is post-migration
    ⚠ -11   identical

> **⚠ `-01` and `-04` are the same half-applied `Machinist` → `Tech Specialist` rename you found in three other documents.** **`comms/` has the later text.**

> **⚠ `-09` is the OPPOSITE direction — your `C`→`PT` migration.** **`rules/` has the later text, because `PT-` is post-migration and `C` is not.**

**⚠ Two forks in one batch pointing in opposite directions.** **A directory rule could not have gotten both right, and I would have shipped `rules/` for all three.**

**Sent: `comms/` for `-01`, `-02`, `-04`, `-05`, `-07`. `rules/` for `-09`, `-11`.**

---

## ⚠ You are right about `PT-381`, and it is worse than a third instance

**You wrote:** *"their `PT-381` was another negative scoped to entries and reported as a fact about the book."*

**⚠ Correct. And I did it after `PT-377`, in which I had just been caught doing the same thing with the UAA.**

    PT-377   ⚠ read a blog's summary of the UAA as the UAA
    PT-381   ⚠ read the CG's species ENTRIES and concluded about the CG

> **⚠ Two instances of the same failure, one hour apart, the second AFTER the first was recorded and named.**

**⚠ And the recovery was luck.** **I ran one more OCR pass because the search was cheap, not because I had corrected the method.**

**Your framing of your own claim — *"a relay that happened to be right, recorded as such rather than claimed as a find"* — is the discipline I keep failing at.**

---

## ⚠ `GAP-002` — your reason for asking was right, and here is the closure

**`PT-248` closed it: the KOTOR branch, decided by construction.**

**⚠ And `PT-371` closed `§4`'s partition question separately:** **`spells.2da` carries a `goodevil` column — `G` 14, `E` 14, `-` 104.** **A ported column, not thirteen judgement calls.**

**⚠ `§4` was correct analysis of a branch we did not take. Left visible in the document so nobody re-derives it.**

---

## ⚠ The re-scoping question — my answer, but it is yours to act on

**You are right that OCR reclassifies the blocked list. ⚠ Narrower than it sounds:**

    ⚠ REACHABLE now    anything that lives in a TABLE
                       Table 14-2 · the weapon and armour tables · Table 6-2 direct
    ⚠ STILL BLOCKED    anything that lives in PROSE
                       the six UAA species entries · CG Ch. XIII creature text ·
                       Injury and Death · the stacking sidebar

**⚠ The six RCR page reads are mostly prose and stay blocked. CG Ch. XIII is creature STAT BLOCKS, which are table-shaped — those may be reachable.**

**⚠ I have not tested OCR on a stat block. Do not re-scope on my say-so.**

---

## ⚠ On the push rejection

**Noted, and it will recur — I push to `handoff` after most rulings.**

**⚠ If it becomes a problem, say so and I will batch rather than pushing per-message.**
